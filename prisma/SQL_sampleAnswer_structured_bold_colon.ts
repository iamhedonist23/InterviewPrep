// ---- 200+ MySQL Interview Questions (Fresher to Advanced) ----
import { Difficulty, ExperienceLevel, InterviewType, PrismaClient } from "@prisma/client";

// ---- Categories ----
export const categories = [
  ["MySQL", "MySQL"]
] as const;

// ---- Topics ----
export const topics = [
  // ==================== BASICS (20) ====================
  ["MySQL", "What is MySQL and what are its main features?", "mysql-overview", "Define MySQL and list its key features.", "**Direct answer :**\nMySQL is an open-source relational database management system (RDBMS) that uses SQL. It is widely used for transactional applications and supports multiple storage engines, with InnoDB as the default in MySQL 8.4. Important capabilities include ACID transactions through InnoDB, foreign keys, indexes, replication, full-text search, JSON data, stored programs, and a broad SQL feature set.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is MySQL and what are its main features? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nMySQL is an open-source relational database management system (RDBMS) that uses SQL. It is widely used for transactional applications and supports multiple storage engines, with InnoDB as the default in MySQL 8.4. Important capabilities include ACID transactions through InnoDB, foreign keys, indexes, replication, full-text search, JSON data, stored programs, and a broad SQL feature set.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain whether the view is updatable, how underlying changes are reflected, how security is affected, and whether the resulting query plan has performance implications."],
  ["MySQL", "What is the difference between MySQL and PostgreSQL?", "mysql-vs-postgresql", "Compare the two popular RDBMS.", "**Direct answer :**\nBoth are mature open-source relational databases, but their strengths and ecosystems differ. MySQL is widely used for web and OLTP workloads and has a comparatively focused feature set, while PostgreSQL emphasizes standards, extensibility, and advanced SQL and data types. The correct choice depends on workload, operational requirements, team expertise, and required features rather than assuming one is universally faster.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between MySQL and PostgreSQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nBoth are mature open-source relational databases, but their strengths and ecosystems differ. MySQL is widely used for web and OLTP workloads and has a comparatively focused feature set, while PostgreSQL emphasizes standards, extensibility, and advanced SQL and data types. The correct choice depends on workload, operational requirements, team expertise, and required features rather than assuming one is universally faster.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What are the default ports for MySQL?", "mysql-default-port", "Specify the default port.", "**Direct answer :**\nThe default port for MySQL is 3306.\n\n**Why this is asked :**\nThis is asked to test whether you understand what are the default ports for MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nThe default port for MySQL is 3306.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "How do you connect to a MySQL database from the command line?", "mysql-connect-cli", "Explain the mysql command.", "**Direct answer :**\nUse `mysql -h host -P port -u username -p database_name`. You will be prompted for the password. For local connections, `mysql -u root -p`.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you connect to a mysql database from the command line? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nUse `mysql -h host -P port -u username -p database_name`. You will be prompted for the password. For local connections, `mysql -u root -p`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a storage engine in MySQL? Name some common ones.", "storage-engine", "Define and list engines.", "**Direct answer :**\nA storage engine handles the underlying data storage, indexing, and locking. Common engines: InnoDB (default, ACID, transactions, foreign keys), MyISAM (no transactions, full‑text, table‑level locking), MEMORY (in‑memory, fast), and ARCHIVE (compressed, append‑only).\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a storage engine in MySQL? Name some common ones. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA storage engine handles the underlying data storage, indexing, and locking. Common engines: InnoDB (default, ACID, transactions, foreign keys), MyISAM (no transactions, full‑text, table‑level locking), MEMORY (in‑memory, fast), and ARCHIVE (compressed, append‑only).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between InnoDB and MyISAM?", "innodb-vs-myisam", "Compare the two main engines.", "**Direct answer :**\nInnoDB supports transactions, ACID, row‑level locking, foreign keys, and crash recovery. MyISAM does not support transactions or foreign keys, uses table‑level locking, and is faster for read‑only or low‑write workloads. InnoDB is recommended for most applications.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between InnoDB and MyISAM? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nInnoDB supports transactions, ACID, row‑level locking, foreign keys, and crash recovery. MyISAM does not support transactions or foreign keys, uses table‑level locking, and is faster for read‑only or low‑write workloads. InnoDB is recommended for most applications.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a primary key? What are its properties?", "primary-key", "Define and explain.", "**Direct answer :**\nA primary key is a unique identifier for each row in a table. It must contain unique, non‑NULL values. A table can have only one primary key, which may be a single column or a composite of multiple columns.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a primary key? What are its properties? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA primary key is a unique identifier for each row in a table. It must contain unique, non‑NULL values. A table can have only one primary key, which may be a single column or a composite of multiple columns.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a foreign key and why is it important?", "foreign-key", "Define referential integrity.", "**Direct answer :**\nA foreign key is a column (or set of columns) that references the primary key of another table. It enforces referential integrity by ensuring that values in the foreign key column match existing values in the referenced table.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a foreign key and why is it important? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA foreign key is a column (or set of columns) that references the primary key of another table. It enforces referential integrity by ensuring that values in the foreign key column match existing values in the referenced table.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a unique key and how is it different from a primary key?", "unique-vs-primary", "Distinguish the two constraints.", "**Direct answer :**\nA UNIQUE constraint/index enforces uniqueness but is not the table's primary identifier. A table can have multiple UNIQUE constraints but only one PRIMARY KEY. In MySQL, a UNIQUE index permits multiple NULL values when the indexed column is nullable because NULL is not considered equal to another NULL for uniqueness purposes. A PRIMARY KEY is implicitly NOT NULL and uniquely identifies each row.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a unique key and how is it different from a primary key? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA UNIQUE constraint/index enforces uniqueness but is not the table's primary identifier. A table can have multiple UNIQUE constraints but only one PRIMARY KEY. In MySQL, a UNIQUE index permits multiple NULL values when the indexed column is nullable because NULL is not considered equal to another NULL for uniqueness purposes. A PRIMARY KEY is implicitly NOT NULL and uniquely identifies each row.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a NULL value in MySQL?", "null-value", "Explain the meaning of NULL.", "**Direct answer :**\nNULL represents an unknown or missing value. It is not equal to any value, including itself. Operations with NULL often return NULL, and you must use `IS NULL` or `IS NOT NULL` to test for it.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a NULL value in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nNULL represents an unknown or missing value. It is not equal to any value, including itself. Operations with NULL often return NULL, and you must use `IS NULL` or `IS NOT NULL` to test for it.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `SELECT` statement? Write a basic query.", "select-statement", "Demonstrate basic usage.", "**Direct answer :**\n`SELECT` retrieves data from one or more tables. Example: `SELECT column1, column2 FROM table WHERE condition ORDER BY column1;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `SELECT` statement? Write a basic query. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`SELECT` retrieves data from one or more tables. Example: `SELECT column1, column2 FROM table WHERE condition ORDER BY column1;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between `DELETE` and `TRUNCATE`?", "delete-vs-truncate", "Compare the two removal commands.", "**Direct answer :**\nDELETE is DML and can remove selected rows with WHERE; for transactional InnoDB tables it can be rolled back before COMMIT. TRUNCATE TABLE removes all rows and is DDL-like in MySQL, causes an implicit commit, and cannot be rolled back as a normal transactional DML operation. TRUNCATE is generally faster for removing the whole table contents and resets the AUTO_INCREMENT counter.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `DELETE` and `TRUNCATE`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nDELETE is DML and can remove selected rows with WHERE; for transactional InnoDB tables it can be rolled back before COMMIT. TRUNCATE TABLE removes all rows and is DDL-like in MySQL, causes an implicit commit, and cannot be rolled back as a normal transactional DML operation. TRUNCATE is generally faster for removing the whole table contents and resets the AUTO_INCREMENT counter.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `DISTINCT` keyword used for?", "distinct-clause", "Explain its purpose.", "**Direct answer :**\n`DISTINCT` removes duplicate rows from the result set. It can be used on a single column or a combination of columns.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `DISTINCT` keyword used for? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`DISTINCT` removes duplicate rows from the result set. It can be used on a single column or a combination of columns.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `LIMIT` clause?", "limit-clause", "Explain pagination.", "**Direct answer :**\n`LIMIT` restricts the number of rows returned. Example: `SELECT * FROM table LIMIT 10 OFFSET 20;` (skips 20 rows, returns next 10).\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `LIMIT` clause? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`LIMIT` restricts the number of rows returned. Example: `SELECT * FROM table LIMIT 10 OFFSET 20;` (skips 20 rows, returns next 10).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a comment in SQL? How do you write one in MySQL?", "sql-comments", "Show comment syntax.", "**Direct answer :**\nSingle‑line comments: `-- comment` or `# comment`. Multi‑line: `/* comment */`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a comment in SQL? How do you write one in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nSingle‑line comments: `-- comment` or `# comment`. Multi‑line: `/* comment */`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a `SHOW` command in MySQL? Give examples.", "show-commands", "List useful SHOW statements.", "**Direct answer :**\n`SHOW DATABASES;`, `SHOW TABLES;`, `SHOW COLUMNS FROM table;`, `SHOW INDEX FROM table;`, `SHOW CREATE TABLE table;`, `SHOW STATUS;`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `SHOW` command in MySQL? Give examples. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`SHOW DATABASES;`, `SHOW TABLES;`, `SHOW COLUMNS FROM table;`, `SHOW INDEX FROM table;`, `SHOW CREATE TABLE table;`, `SHOW STATUS;`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `DESCRIBE` command?", "describe-command", "Explain its usage.", "**Direct answer :**\n`DESCRIBE table_name;` (or `DESC`) displays the structure of a table, including column names, data types, and nullability.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `DESCRIBE` command? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`DESCRIBE table_name;` (or `DESC`) displays the structure of a table, including column names, data types, and nullability.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a database and a schema in MySQL?", "db-vs-schema", "Clarify the terms.", "**Direct answer :**\nIn MySQL, a database and a schema are synonymous – they are logical containers for tables, views, stored procedures, etc. You can use `CREATE DATABASE` or `CREATE SCHEMA` interchangeably.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a database and a schema in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nIn MySQL, a database and a schema are synonymous – they are logical containers for tables, views, stored procedures, etc. You can use `CREATE DATABASE` or `CREATE SCHEMA` interchangeably.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `USE` statement?", "use-statement", "Explain switching databases.", "**Direct answer :**\n`USE database_name;` selects the default database for subsequent queries.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `USE` statement? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`USE database_name;` selects the default database for subsequent queries.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between a table and a view?", "table-vs-view", "Compare the two objects.", "**Direct answer :**\nA table stores physical data. A view is a virtual table defined by a query; it does not store data but presents a filtered or computed representation of underlying tables.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between a table and a view? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA table stores physical data. A view is a virtual table defined by a query; it does not store data but presents a filtered or computed representation of underlying tables.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain whether the view is updatable, how underlying changes are reflected, how security is affected, and whether the resulting query plan has performance implications."],

  // ==================== DATA TYPES (15) ====================
  ["MySQL", "What are the common numeric data types in MySQL?", "mysql-numeric-types", "List and describe.", "**Direct answer :**\nInteger: `TINYINT`, `SMALLINT`, `MEDIUMINT`, `INT`, `BIGINT`. Fixed‑point: `DECIMAL`/`NUMERIC`. Floating‑point: `FLOAT`, `DOUBLE`. `BIT` for bit values.\n\n**Why this is asked :**\nThis is asked to test whether you understand what are the common numeric data types in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nInteger: `TINYINT`, `SMALLINT`, `MEDIUMINT`, `INT`, `BIGINT`. Fixed‑point: `DECIMAL`/`NUMERIC`. Floating‑point: `FLOAT`, `DOUBLE`. `BIT` for bit values.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between `CHAR` and `VARCHAR`?", "char-vs-varchar", "Compare fixed and variable length.", "**Direct answer :**\n`CHAR(n)` is fixed‑length (padded with spaces) and stores exactly n characters. `VARCHAR(n)` is variable‑length, uses 1 or 2 bytes for length prefix, and saves space for shorter strings. `VARCHAR` is generally preferred.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `CHAR` and `VARCHAR`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`CHAR(n)` is fixed‑length (padded with spaces) and stores exactly n characters. `VARCHAR(n)` is variable‑length, uses 1 or 2 bytes for length prefix, and saves space for shorter strings. `VARCHAR` is generally preferred.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `TEXT` data type? How does it differ from `VARCHAR`?", "text-vs-varchar", "Explain limitations.", "**Direct answer :**\n`TEXT` is a variable‑length string with a maximum size of 65,535 bytes (for `TEXT`), `MEDIUMTEXT` (16 MB), and `LONGTEXT` (4 GB). Unlike `VARCHAR`, `TEXT` columns cannot have a default value and are stored separately from the row, affecting performance.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `TEXT` data type? How does it differ from `VARCHAR`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`TEXT` is a variable‑length string with a maximum size of 65,535 bytes (for `TEXT`), `MEDIUMTEXT` (16 MB), and `LONGTEXT` (4 GB). Unlike `VARCHAR`, `TEXT` columns cannot have a default value and are stored separately from the row, affecting performance.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What are the date and time data types?", "mysql-datetime-types", "List the types.", "**Direct answer :**\n`DATE` (YYYY‑MM‑DD), `TIME` (HH:MM:SS), `DATETIME` (YYYY‑MM‑DD HH:MM:SS), `TIMESTAMP` (YYYY‑MM‑DD HH:MM:SS, with timezone conversion), `YEAR`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what are the date and time data types? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`DATE` (YYYY‑MM‑DD), `TIME` (HH:MM:SS), `DATETIME` (YYYY‑MM‑DD HH:MM:SS), `TIMESTAMP` (YYYY‑MM‑DD HH:MM:SS, with timezone conversion), `YEAR`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between `DATETIME` and `TIMESTAMP`?", "datetime-vs-timestamp", "Compare the two.", "**Direct answer :**\nDATETIME stores a date and time without automatic time-zone conversion. TIMESTAMP values are converted from the session time zone to UTC for storage and back to the session time zone for retrieval. TIMESTAMP has a smaller supported range than DATETIME, so DATETIME is often preferable when the value represents a wall-clock date/time outside TIMESTAMP's range or should not undergo time-zone conversion.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `DATETIME` and `TIMESTAMP`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nDATETIME stores a date and time without automatic time-zone conversion. TIMESTAMP values are converted from the session time zone to UTC for storage and back to the session time zone for retrieval. TIMESTAMP has a smaller supported range than DATETIME, so DATETIME is often preferable when the value represents a wall-clock date/time outside TIMESTAMP's range or should not undergo time-zone conversion.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `JSON` data type in MySQL?", "json-type", "Explain JSON storage.", "**Direct answer :**\nMySQL supports native JSON data type (introduced in 5.7). It allows efficient storage and querying of JSON documents, with functions like `JSON_EXTRACT`, `JSON_SET`, and indexing of generated columns.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `JSON` data type in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nMySQL supports native JSON data type (introduced in 5.7). It allows efficient storage and querying of JSON documents, with functions like `JSON_EXTRACT`, `JSON_SET`, and indexing of generated columns.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain when JSON is appropriate versus normalized columns, how JSON paths are queried, and how generated or functional indexing can support selective lookups."],
  ["MySQL", "What is the `ENUM` data type?", "enum-type", "Define and give usage.", "**Direct answer :**\n`ENUM` is a string object that can have only one value from a predefined list of allowed values. It is stored as an integer internally, saving space. Example: `ENUM('small','medium','large')`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `ENUM` data type? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`ENUM` is a string object that can have only one value from a predefined list of allowed values. It is stored as an integer internally, saving space. Example: `ENUM('small','medium','large')`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `SET` data type?", "set-type", "Explain multiple-choice string.", "**Direct answer :**\n`SET` is a string object that can have zero or more values from a predefined set. It is stored as a bitmask, allowing up to 64 members. Example: `SET('a','b','c')`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `SET` data type? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`SET` is a string object that can have zero or more values from a predefined set. It is stored as a bitmask, allowing up to 64 members. Example: `SET('a','b','c')`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `BOOLEAN` data type in MySQL?", "boolean-type", "Explain its implementation.", "**Direct answer :**\n`BOOLEAN` is a synonym for `TINYINT(1)`. A value of 0 is considered `FALSE`, non‑zero is `TRUE`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `BOOLEAN` data type in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`BOOLEAN` is a synonym for `TINYINT(1)`. A value of 0 is considered `FALSE`, non‑zero is `TRUE`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `BLOB` data type?", "blob-type", "Define binary storage.", "**Direct answer :**\n`BLOB` (Binary Large Object) stores binary data (e.g., images, files). Variants: `TINYBLOB`, `BLOB`, `MEDIUMBLOB`, `LONGBLOB`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `BLOB` data type? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`BLOB` (Binary Large Object) stores binary data (e.g., images, files). Variants: `TINYBLOB`, `BLOB`, `MEDIUMBLOB`, `LONGBLOB`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `DECIMAL` data type and when would you use it?", "decimal-type", "Explain exact numeric storage.", "**Direct answer :**\n`DECIMAL(M,D)` stores exact numeric values with fixed precision. It is used for financial calculations where exact decimal representation is required (e.g., `DECIMAL(10,2)` for currency).\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `DECIMAL` data type and when would you use it? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`DECIMAL(M,D)` stores exact numeric values with fixed precision. It is used for financial calculations where exact decimal representation is required (e.g., `DECIMAL(10,2)` for currency).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between `FLOAT` and `DOUBLE`?", "float-vs-double", "Compare precision and storage.", "**Direct answer :**\n`FLOAT` is a single‑precision (approx. 7 decimal digits) floating‑point number. `DOUBLE` is double‑precision (approx. 15 digits). Both are approximate and should not be used for monetary values.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `FLOAT` and `DOUBLE`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`FLOAT` is a single‑precision (approx. 7 decimal digits) floating‑point number. `DOUBLE` is double‑precision (approx. 15 digits). Both are approximate and should not be used for monetary values.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `BINARY` and `VARBINARY` type?", "binary-types", "Explain fixed and variable binary.", "**Direct answer :**\n`BINARY` and `VARBINARY` are similar to `CHAR` and `VARCHAR` but store byte strings rather than character strings. They are used for binary data that should not have character set conversion.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `BINARY` and `VARBINARY` type? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`BINARY` and `VARBINARY` are similar to `CHAR` and `VARCHAR` but store byte strings rather than character strings. They are used for binary data that should not have character set conversion.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `GEOMETRY` data type?", "geometry-type", "Explain spatial data.", "**Direct answer :**\n`GEOMETRY` is a base type for spatial data (points, lines, polygons). MySQL supports `POINT`, `LINESTRING`, `POLYGON`, and their collections. These types are used with GIS functions.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `GEOMETRY` data type? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`GEOMETRY` is a base type for spatial data (points, lines, polygons). MySQL supports `POINT`, `LINESTRING`, `POLYGON`, and their collections. These types are used with GIS functions.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `INET6` data type?", "inet6-type", "Explain IPv6 storage.", "**Direct answer :**\nMySQL does not provide a native data type named INET6. IPv4 and IPv6 addresses are commonly stored using binary data, such as VARBINARY(16), and MySQL provides functions such as INET6_ATON() and INET6_NTOA() to convert IPv4/IPv6 text representations to and from binary form.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `INET6` data type? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nMySQL does not provide a native data type named INET6. IPv4 and IPv6 addresses are commonly stored using binary data, such as VARBINARY(16), and MySQL provides functions such as INET6_ATON() and INET6_NTOA() to convert IPv4/IPv6 text representations to and from binary form.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],

  // ==================== DDL & DML (15) ====================
  ["MySQL", "What is the syntax to create a table? Provide an example.", "create-table", "Show basic table creation.", "**Direct answer :**\n`CREATE TABLE users (id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(100) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the syntax to create a table? Provide an example. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`CREATE TABLE users (id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(100) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `ALTER TABLE` command used for?", "alter-table", "Explain schema modifications.", "**Direct answer :**\n`ALTER TABLE` changes the structure of an existing table: add/drop columns, change data type, add/drop constraints, rename table, etc. Example: `ALTER TABLE users ADD COLUMN age INT;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `ALTER TABLE` command used for? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`ALTER TABLE` changes the structure of an existing table: add/drop columns, change data type, add/drop constraints, rename table, etc. Example: `ALTER TABLE users ADD COLUMN age INT;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "How do you add a foreign key constraint using `ALTER TABLE`?", "add-foreign-key", "Provide syntax.", "**Direct answer :**\n`ALTER TABLE orders ADD CONSTRAINT fk_customer FOREIGN KEY (customer_id) REFERENCES customers(id) ON DELETE CASCADE;`\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you add a foreign key constraint using `alter table`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`ALTER TABLE orders ADD CONSTRAINT fk_customer FOREIGN KEY (customer_id) REFERENCES customers(id) ON DELETE CASCADE;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a `DROP TABLE` command and what does it do?", "drop-table", "Explain removal.", "**Direct answer :**\nDROP TABLE removes the table definition and its data. For transactional storage engines such as InnoDB, DROP TABLE is a DDL statement that causes an implicit commit and is not something you normally undo with ROLLBACK. Therefore it should be treated as a destructive schema operation.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `DROP TABLE` command and what does it do? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nDROP TABLE removes the table definition and its data. For transactional storage engines such as InnoDB, DROP TABLE is a DDL statement that causes an implicit commit and is not something you normally undo with ROLLBACK. Therefore it should be treated as a destructive schema operation.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between `DROP TABLE` and `TRUNCATE TABLE`?", "drop-vs-truncate", "Compare the two.", "**Direct answer :**\n`DROP TABLE` removes the table definition and data. `TRUNCATE TABLE` removes all rows but retains the table structure; it is faster and resets auto‑increment.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `DROP TABLE` and `TRUNCATE TABLE`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`DROP TABLE` removes the table definition and data. `TRUNCATE TABLE` removes all rows but retains the table structure; it is faster and resets auto‑increment.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is an `INSERT` statement? Write an example.", "insert-statement", "Show insertion syntax.", "**Direct answer :**\nINSERT adds new rows to a table. The clearest form is `INSERT INTO users (name, age) VALUES ('John', 25);`. Listing target columns explicitly is recommended because it makes the statement resilient to column-order changes and easier to review.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is an `INSERT` statement? Write an example. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nINSERT adds new rows to a table. The clearest form is `INSERT INTO users (name, age) VALUES ('John', 25);`. Listing target columns explicitly is recommended because it makes the statement resilient to column-order changes and easier to review.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is `INSERT INTO ... SELECT`?", "insert-select", "Explain copying data.", "**Direct answer :**\n`INSERT INTO table2 (col1, col2) SELECT col1, col2 FROM table1 WHERE condition;` inserts the result of a query into another table.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is `INSERT INTO ... SELECT`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`INSERT INTO table2 (col1, col2) SELECT col1, col2 FROM table1 WHERE condition;` inserts the result of a query into another table.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "How do you update data in MySQL?", "update-statement", "Provide syntax.", "**Direct answer :**\n`UPDATE users SET age = 26 WHERE id = 1;` Always use a `WHERE` clause to avoid updating all rows.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you update data in mysql? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`UPDATE users SET age = 26 WHERE id = 1;` Always use a `WHERE` clause to avoid updating all rows.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "How do you delete data? Give an example.", "delete-statement", "Provide syntax.", "**Direct answer :**\n`DELETE FROM users WHERE id = 1;` Without `WHERE`, all rows are deleted (similar to `TRUNCATE` but slower and can be rolled back).\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you delete data? give an example. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`DELETE FROM users WHERE id = 1;` Without `WHERE`, all rows are deleted (similar to `TRUNCATE` but slower and can be rolled back).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `REPLACE` command?", "replace", "Explain insert or replace.", "**Direct answer :**\n`REPLACE INTO table (col1, col2) VALUES (1, 'a');` If a row with the same primary key exists, it is deleted and a new row is inserted; otherwise, it inserts. It is a combination of `DELETE` + `INSERT`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `REPLACE` command? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`REPLACE INTO table (col1, col2) VALUES (1, 'a');` If a row with the same primary key exists, it is deleted and a new row is inserted; otherwise, it inserts. It is a combination of `DELETE` + `INSERT`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is `ON DUPLICATE KEY UPDATE`?", "on-duplicate-key", "Explain upsert.", "**Direct answer :**\nMySQL supports an upsert pattern with `INSERT ... ON DUPLICATE KEY UPDATE`. If the insert would violate a PRIMARY KEY or UNIQUE index, MySQL performs the UPDATE clause instead. In current MySQL versions, prefer row aliases in new code rather than relying on the deprecated `VALUES(col)` form.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is `ON DUPLICATE KEY UPDATE`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nMySQL supports an upsert pattern with `INSERT ... ON DUPLICATE KEY UPDATE`. If the insert would violate a PRIMARY KEY or UNIQUE index, MySQL performs the UPDATE clause instead. In current MySQL versions, prefer row aliases in new code rather than relying on the deprecated `VALUES(col)` form.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a `SELECT ... FOR UPDATE`?", "select-for-update", "Explain row locking.", "**Direct answer :**\n`SELECT ... FOR UPDATE` locks the selected rows for updates, preventing other transactions from modifying them until the current transaction commits. Used in transactions for pessimistic locking.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `SELECT ... FOR UPDATE`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`SELECT ... FOR UPDATE` locks the selected rows for updates, preventing other transactions from modifying them until the current transaction commits. Used in transactions for pessimistic locking.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a `SELECT ... LOCK IN SHARE MODE`?", "lock-in-share-mode", "Explain shared lock.", "**Direct answer :**\n`SELECT ... FOR SHARE` is the modern syntax for a locking read that takes shared locks on qualifying InnoDB records; `LOCK IN SHARE MODE` is the older equivalent. Other transactions can generally read the locked records, but conflicting updates are blocked until the transaction ends. Use it only when a consistent, protected read is actually required.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `SELECT ... LOCK IN SHARE MODE`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`SELECT ... FOR SHARE` is the modern syntax for a locking read that takes shared locks on qualifying InnoDB records; `LOCK IN SHARE MODE` is the older equivalent. Other transactions can generally read the locked records, but conflicting updates are blocked until the transaction ends. Use it only when a consistent, protected read is actually required.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain what happens when two transactions access the same rows, which locks are taken, how isolation changes visibility, and how you would diagnose or retry a conflict safely."],
  ["MySQL", "What is the `UNION` operator?", "union", "Explain combining result sets.", "**Direct answer :**\n`UNION` combines the results of two or more `SELECT` statements and removes duplicate rows. `UNION ALL` does not remove duplicates and is faster.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `UNION` operator? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`UNION` combines the results of two or more `SELECT` statements and removes duplicate rows. `UNION ALL` does not remove duplicates and is faster.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a `JOIN`? Explain the different types.", "join-types", "List and describe.", "**Direct answer :**\n`INNER JOIN` returns rows with matching keys in both tables. `LEFT JOIN` (or `RIGHT JOIN`) returns all rows from one side and matching rows from the other; non‑matching rows are `NULL`. `CROSS JOIN` returns the Cartesian product.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `JOIN`? Explain the different types. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`INNER JOIN` returns rows with matching keys in both tables. `LEFT JOIN` (or `RIGHT JOIN`) returns all rows from one side and matching rows from the other; non‑matching rows are `NULL`. `CROSS JOIN` returns the Cartesian product.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare this JOIN with INNER/LEFT/CROSS JOINs, explain NULL results, and discuss how indexes and the execution plan affect join performance."],

  // ==================== JOINS & SUBQUERIES (15) ====================
  ["MySQL", "What is an `INNER JOIN`? Provide an example.", "inner-join", "Show usage.", "**Direct answer :**\n`SELECT o.id, c.name FROM orders o INNER JOIN customers c ON o.customer_id = c.id;` returns orders with existing customers.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is an `INNER JOIN`? Provide an example. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`SELECT o.id, c.name FROM orders o INNER JOIN customers c ON o.customer_id = c.id;` returns orders with existing customers.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare this JOIN with INNER/LEFT/CROSS JOINs, explain NULL results, and discuss how indexes and the execution plan affect join performance."],
  ["MySQL", "What is a `LEFT JOIN`? When would you use it?", "left-join", "Explain its purpose.", "**Direct answer :**\n`LEFT JOIN` returns all rows from the left table, with matching rows from the right table. If no match, right columns are `NULL`. Used when you need all records from the left table regardless of matches.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `LEFT JOIN`? When would you use it? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`LEFT JOIN` returns all rows from the left table, with matching rows from the right table. If no match, right columns are `NULL`. Used when you need all records from the left table regardless of matches.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare this JOIN with INNER/LEFT/CROSS JOINs, explain NULL results, and discuss how indexes and the execution plan affect join performance."],
  ["MySQL", "What is a `RIGHT JOIN`?", "right-join", "Explain.", "**Direct answer :**\nSimilar to `LEFT JOIN` but preserves all rows from the right table. `RIGHT JOIN` is less commonly used; you can achieve the same with `LEFT JOIN` by swapping tables.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `RIGHT JOIN`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nSimilar to `LEFT JOIN` but preserves all rows from the right table. `RIGHT JOIN` is less commonly used; you can achieve the same with `LEFT JOIN` by swapping tables.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare this JOIN with INNER/LEFT/CROSS JOINs, explain NULL results, and discuss how indexes and the execution plan affect join performance."],
  ["MySQL", "What is a `CROSS JOIN`?", "cross-join", "Define Cartesian product.", "**Direct answer :**\n`CROSS JOIN` returns the Cartesian product of two tables (every row from the first combined with every row from the second). It is rarely used without a `WHERE` condition.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `CROSS JOIN`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`CROSS JOIN` returns the Cartesian product of two tables (every row from the first combined with every row from the second). It is rarely used without a `WHERE` condition.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare this JOIN with INNER/LEFT/CROSS JOINs, explain NULL results, and discuss how indexes and the execution plan affect join performance."],
  ["MySQL", "What is a self‑join? Give an example.", "self-join", "Explain joining a table with itself.", "**Direct answer :**\nA self‑join joins a table to itself. Example: `SELECT e1.name AS employee, e2.name AS manager FROM employees e1 LEFT JOIN employees e2 ON e1.manager_id = e2.id;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a self‑join? Give an example. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA self‑join joins a table to itself. Example: `SELECT e1.name AS employee, e2.name AS manager FROM employees e1 LEFT JOIN employees e2 ON e1.manager_id = e2.id;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare this JOIN with INNER/LEFT/CROSS JOINs, explain NULL results, and discuss how indexes and the execution plan affect join performance."],
  ["MySQL", "What is a subquery? Write an example.", "subquery", "Define and demonstrate.", "**Direct answer :**\nA subquery is a query nested inside another query. Example: `SELECT name FROM employees WHERE salary > (SELECT AVG(salary) FROM employees);`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a subquery? Write an example. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA subquery is a query nested inside another query. Example: `SELECT name FROM employees WHERE salary > (SELECT AVG(salary) FROM employees);`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a correlated subquery?", "correlated-subquery", "Explain dependencies.", "**Direct answer :**\nA correlated subquery references columns from the outer query. It is executed once for each row processed by the outer query. Example: `SELECT name FROM employees e WHERE salary > (SELECT AVG(salary) FROM employees WHERE department = e.department);`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a correlated subquery? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA correlated subquery references columns from the outer query. It is executed once for each row processed by the outer query. Example: `SELECT name FROM employees e WHERE salary > (SELECT AVG(salary) FROM employees WHERE department = e.department);`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What are the different types of subqueries? (scalar, row, table)", "subquery-types", "Categorize subqueries.", "**Direct answer :**\nScalar subqueries return a single value; row subqueries return a single row; table subqueries return a result set (used in `FROM` or `EXISTS`).\n\n**Why this is asked :**\nThis is asked to test whether you understand what are the different types of subqueries? (scalar, row, table) beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nScalar subqueries return a single value; row subqueries return a single row; table subqueries return a result set (used in `FROM` or `EXISTS`).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `EXISTS` operator?", "exists", "Explain existence test.", "**Direct answer :**\n`EXISTS` tests whether a subquery returns any rows. Example: `SELECT name FROM customers c WHERE EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id);`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `EXISTS` operator? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`EXISTS` tests whether a subquery returns any rows. Example: `SELECT name FROM customers c WHERE EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id);`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `ANY` and `ALL` operators?", "any-all", "Explain comparisons with subqueries.", "**Direct answer :**\n`ANY` (or `SOME`) returns true if the condition holds for at least one value from the subquery. `ALL` returns true if the condition holds for all values. Example: `SELECT name FROM products WHERE price > ANY (SELECT price FROM products WHERE category = 'electronics');`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `ANY` and `ALL` operators? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`ANY` (or `SOME`) returns true if the condition holds for at least one value from the subquery. `ALL` returns true if the condition holds for all values. Example: `SELECT name FROM products WHERE price > ANY (SELECT price FROM products WHERE category = 'electronics');`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a derived table (inline view)?", "derived-table", "Define and give example.", "**Direct answer :**\nA derived table is a subquery used in the `FROM` clause. It must have an alias. Example: `SELECT * FROM (SELECT id, name FROM users WHERE age > 18) AS adult_users;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a derived table (inline view)? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA derived table is a subquery used in the `FROM` clause. It must have an alias. Example: `SELECT * FROM (SELECT id, name FROM users WHERE age > 18) AS adult_users;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between `JOIN` and `UNION`?", "join-vs-union", "Compare the two.", "**Direct answer :**\n`JOIN` combines columns from different tables based on a relationship. `UNION` combines rows from two or more queries, stacking results vertically.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `JOIN` and `UNION`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`JOIN` combines columns from different tables based on a relationship. `UNION` combines rows from two or more queries, stacking results vertically.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare this JOIN with INNER/LEFT/CROSS JOINs, explain NULL results, and discuss how indexes and the execution plan affect join performance."],
  ["MySQL", "What is the `USING` clause in joins?", "using-clause", "Explain shorthand for equality join.", "**Direct answer :**\n`JOIN ... USING (column)` is equivalent to `ON left.column = right.column` and merges the common column into a single column. Example: `SELECT * FROM orders JOIN customers USING (customer_id);`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `USING` clause in joins? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`JOIN ... USING (column)` is equivalent to `ON left.column = right.column` and merges the common column into a single column. Example: `SELECT * FROM orders JOIN customers USING (customer_id);`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a `NATURAL JOIN`?", "natural-join", "Explain automatic join.", "**Direct answer :**\n`NATURAL JOIN` automatically joins tables on columns with the same name. It is risky because it depends on column names and can lead to unintended results; avoid it in production.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `NATURAL JOIN`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`NATURAL JOIN` automatically joins tables on columns with the same name. It is risky because it depends on column names and can lead to unintended results; avoid it in production.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare this JOIN with INNER/LEFT/CROSS JOINs, explain NULL results, and discuss how indexes and the execution plan affect join performance."],
  ["MySQL", "How do you write a query to find employees with salary greater than their manager's salary?", "query-manager-salary", "Practical join/subquery.", "**Direct answer :**\nUsing a self‑join: `SELECT e.name FROM employees e JOIN employees m ON e.manager_id = m.id WHERE e.salary > m.salary;`\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you write a query to find employees with salary greater than their manager's salary? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nUsing a self‑join: `SELECT e.name FROM employees e JOIN employees m ON e.manager_id = m.id WHERE e.salary > m.salary;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],

  // ==================== FUNCTIONS (15) ====================
  ["MySQL", "What are aggregate functions? Name a few.", "aggregate-functions", "List common aggregates.", "**Direct answer :**\n`COUNT()`, `SUM()`, `AVG()`, `MAX()`, `MIN()`. They operate on a set of rows and return a single value.\n\n**Why this is asked :**\nThis is asked to test whether you understand what are aggregate functions? Name a few. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`COUNT()`, `SUM()`, `AVG()`, `MAX()`, `MIN()`. They operate on a set of rows and return a single value.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What is the `GROUP BY` clause?", "group-by", "Explain grouping.", "**Direct answer :**\n`GROUP BY` groups rows that have the same values in specified columns, allowing aggregate functions to be applied per group. Example: `SELECT department, AVG(salary) FROM employees GROUP BY department;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `GROUP BY` clause? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`GROUP BY` groups rows that have the same values in specified columns, allowing aggregate functions to be applied per group. Example: `SELECT department, AVG(salary) FROM employees GROUP BY department;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `HAVING` clause?", "having-clause", "Explain filtering groups.", "**Direct answer :**\n`HAVING` filters groups after `GROUP BY`, similar to `WHERE` but for aggregate conditions. Example: `SELECT department, AVG(salary) FROM employees GROUP BY department HAVING AVG(salary) > 50000;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `HAVING` clause? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`HAVING` filters groups after `GROUP BY`, similar to `WHERE` but for aggregate conditions. Example: `SELECT department, AVG(salary) FROM employees GROUP BY department HAVING AVG(salary) > 50000;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between `WHERE` and `HAVING`?", "where-vs-having", "Compare the two.", "**Direct answer :**\n`WHERE` filters rows before grouping; `HAVING` filters groups after grouping. `HAVING` can use aggregate functions; `WHERE` cannot.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `WHERE` and `HAVING`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`WHERE` filters rows before grouping; `HAVING` filters groups after grouping. `HAVING` can use aggregate functions; `WHERE` cannot.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What are string functions in MySQL? Give examples.", "string-functions", "List common string functions.", "**Direct answer :**\n`CONCAT()`, `SUBSTRING()`, `LENGTH()`, `UPPER()`, `LOWER()`, `TRIM()`, `REPLACE()`, `INSTR()`, `LOCATE()`, `LEFT()`, `RIGHT()`, `LPAD()`, `RPAD()`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what are string functions in MySQL? Give examples. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`CONCAT()`, `SUBSTRING()`, `LENGTH()`, `UPPER()`, `LOWER()`, `TRIM()`, `REPLACE()`, `INSTR()`, `LOCATE()`, `LEFT()`, `RIGHT()`, `LPAD()`, `RPAD()`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What is the `DATE_FORMAT` function?", "date-format", "Explain date formatting.", "**Direct answer :**\n`DATE_FORMAT(date, format)` formats a date according to a format string. Example: `SELECT DATE_FORMAT(NOW(), '%Y-%m-%d %H:%i:%s');`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `DATE_FORMAT` function? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`DATE_FORMAT(date, format)` formats a date according to a format string. Example: `SELECT DATE_FORMAT(NOW(), '%Y-%m-%d %H:%i:%s');`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What are date and time functions? Provide examples.", "date-time-functions", "List common ones.", "**Direct answer :**\n`NOW()`, `CURDATE()`, `CURTIME()`, `DATE_ADD()`, `DATE_SUB()`, `DATEDIFF()`, `DAY()`, `MONTH()`, `YEAR()`, `UNIX_TIMESTAMP()`, `FROM_UNIXTIME()`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what are date and time functions? Provide examples. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`NOW()`, `CURDATE()`, `CURTIME()`, `DATE_ADD()`, `DATE_SUB()`, `DATEDIFF()`, `DAY()`, `MONTH()`, `YEAR()`, `UNIX_TIMESTAMP()`, `FROM_UNIXTIME()`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What is the `IF` function in MySQL?", "if-function", "Explain conditional logic.", "**Direct answer :**\n`IF(condition, true_value, false_value)` returns the true or false value. Example: `SELECT IF(salary > 50000, 'High', 'Low') FROM employees;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `IF` function in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`IF(condition, true_value, false_value)` returns the true or false value. Example: `SELECT IF(salary > 50000, 'High', 'Low') FROM employees;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What is `CASE` statement? Provide an example.", "case-statement", "Explain conditional expressions.", "**Direct answer :**\n`CASE WHEN condition THEN result ... ELSE default END`. Example: `SELECT CASE WHEN age < 18 THEN 'Minor' WHEN age < 65 THEN 'Adult' ELSE 'Senior' END FROM people;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is `CASE` statement? Provide an example. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`CASE WHEN condition THEN result ... ELSE default END`. Example: `SELECT CASE WHEN age < 18 THEN 'Minor' WHEN age < 65 THEN 'Adult' ELSE 'Senior' END FROM people;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `COALESCE` function?", "coalesce", "Define and give usage.", "**Direct answer :**\n`COALESCE(value1, value2, ...)` returns the first non‑NULL value. Useful for providing defaults: `SELECT COALESCE(phone, 'No phone') FROM contacts;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `COALESCE` function? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`COALESCE(value1, value2, ...)` returns the first non‑NULL value. Useful for providing defaults: `SELECT COALESCE(phone, 'No phone') FROM contacts;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `NULLIF` function?", "nullif", "Explain its purpose.", "**Direct answer :**\n`NULLIF(expr1, expr2)` returns NULL if expr1 equals expr2, otherwise returns expr1. Example: `SELECT NULLIF(score, 0) FROM tests;` (treats 0 as NULL).\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `NULLIF` function? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`NULLIF(expr1, expr2)` returns NULL if expr1 equals expr2, otherwise returns expr1. Example: `SELECT NULLIF(score, 0) FROM tests;` (treats 0 as NULL).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What are the mathematical functions? Give examples.", "math-functions", "List common ones.", "**Direct answer :**\n`ABS()`, `CEIL()`, `FLOOR()`, `ROUND()`, `POW()`, `SQRT()`, `MOD()`, `RAND()`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what are the mathematical functions? Give examples. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`ABS()`, `CEIL()`, `FLOOR()`, `ROUND()`, `POW()`, `SQRT()`, `MOD()`, `RAND()`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What is the `CONCAT` function?", "concat", "Explain string concatenation.", "**Direct answer :**\n`CONCAT(str1, str2, ...)` concatenates strings. Example: `SELECT CONCAT(first_name, ' ', last_name) AS full_name FROM users;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `CONCAT` function? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`CONCAT(str1, str2, ...)` concatenates strings. Example: `SELECT CONCAT(first_name, ' ', last_name) AS full_name FROM users;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `SUBSTRING` function?", "substring", "Explain extracting substrings.", "**Direct answer :**\n`SUBSTRING(str, start, length)` extracts a substring. Example: `SELECT SUBSTRING('Hello World', 1, 5);` returns 'Hello'.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `SUBSTRING` function? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`SUBSTRING(str, start, length)` extracts a substring. Example: `SELECT SUBSTRING('Hello World', 1, 5);` returns 'Hello'.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `FIND_IN_SET` function?", "find-in-set", "Explain searching in SET.", "**Direct answer :**\n`FIND_IN_SET(str, strlist)` returns the position of the string within a comma‑separated list. Example: `SELECT FIND_IN_SET('b', 'a,b,c');` returns 2.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `FIND_IN_SET` function? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`FIND_IN_SET(str, strlist)` returns the position of the string within a comma‑separated list. Example: `SELECT FIND_IN_SET('b', 'a,b,c');` returns 2.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],

  // ==================== INDEXES & PERFORMANCE (15) ====================
  ["MySQL", "What is an index in MySQL? Why is it important?", "mysql-index", "Define and explain benefits.", "**Direct answer :**\nAn index is a data structure (typically B‑tree) that improves the speed of data retrieval. It is crucial for performance but adds overhead on DML operations. Indexes can be created on one or multiple columns.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is an index in MySQL? Why is it important? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nAn index is a data structure (typically B‑tree) that improves the speed of data retrieval. It is crucial for performance but adds overhead on DML operations. Indexes can be created on one or multiple columns.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "What are the different types of indexes in MySQL?", "index-types", "List the types.", "**Direct answer :**\nCommon MySQL index forms include PRIMARY KEY indexes, UNIQUE indexes, ordinary secondary indexes, FULLTEXT indexes, and SPATIAL indexes. InnoDB organizes its primary-key data in the clustered index; secondary indexes contain the primary-key value needed to locate the clustered record. The exact behavior and supported index features depend on the storage engine and data type.\n\n**Why this is asked :**\nThis is asked to test whether you understand what are the different types of indexes in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nCommon MySQL index forms include PRIMARY KEY indexes, UNIQUE indexes, ordinary secondary indexes, FULLTEXT indexes, and SPATIAL indexes. InnoDB organizes its primary-key data in the clustered index; secondary indexes contain the primary-key value needed to locate the clustered record. The exact behavior and supported index features depend on the storage engine and data type.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "What is a B‑Tree index and how does it work?", "btree-index", "Explain the structure.", "**Direct answer :**\nA B‑Tree index organizes data in a balanced tree structure, allowing efficient searches, range queries, and sorting. InnoDB uses B‑tree for its primary key and secondary indexes.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a B‑Tree index and how does it work? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA B‑Tree index organizes data in a balanced tree structure, allowing efficient searches, range queries, and sorting. InnoDB uses B‑tree for its primary key and secondary indexes.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "What is a composite index (multi‑column index)?", "composite-index", "Define and explain order importance.", "**Direct answer :**\nA composite index is an index on multiple columns. The order of columns matters: queries that filter on the leftmost prefix can use the index. Example: `INDEX(last_name, first_name)` supports searches on `last_name` and both.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a composite index (multi‑column index)? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA composite index is an index on multiple columns. The order of columns matters: queries that filter on the leftmost prefix can use the index. Example: `INDEX(last_name, first_name)` supports searches on `last_name` and both.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "When would you use a `UNIQUE` index?", "unique-index", "Explain uniqueness constraint.", "**Direct answer :**\nA `UNIQUE` index ensures that no two rows have the same value in the indexed column(s). It also speeds up lookups. Use it for alternate keys (e.g., email).\n\n**Why this is asked :**\nThis is asked to test whether you understand when would you use a `unique` index? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA `UNIQUE` index ensures that no two rows have the same value in the indexed column(s). It also speeds up lookups. Use it for alternate keys (e.g., email).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "What is a `FULLTEXT` index?", "fulltext-index", "Explain full‑text search.", "**Direct answer :**\n`FULLTEXT` indexes are used with `MATCH() AGAINST()` for natural‑language searches on text columns. They are available for `MyISAM` and `InnoDB` (since 5.6).\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `FULLTEXT` index? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`FULLTEXT` indexes are used with `MATCH() AGAINST()` for natural‑language searches on text columns. They are available for `MyISAM` and `InnoDB` (since 5.6).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "What is a `SPATIAL` index?", "spatial-index", "Explain GIS indexing.", "**Direct answer :**\n`SPATIAL` index is used on `GEOMETRY` columns for efficient spatial queries (e.g., `ST_Contains`, `ST_Distance`). Available for InnoDB (5.7+) and MyISAM.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `SPATIAL` index? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`SPATIAL` index is used on `GEOMETRY` columns for efficient spatial queries (e.g., `ST_Contains`, `ST_Distance`). Available for InnoDB (5.7+) and MyISAM.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "What is the difference between `INDEX` and `PRIMARY KEY` in terms of clustering?", "index-vs-primary-clustering", "Explain clustering.", "**Direct answer :**\nFor InnoDB, the PRIMARY KEY is the clustered index: the row's data is stored with the primary-key B-tree. A secondary index stores its indexed columns plus the primary-key value, so a query that needs non-indexed columns may require a second lookup into the clustered index. This is why a compact, stable primary key can matter for secondary-index size and I/O.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `INDEX` and `PRIMARY KEY` in terms of clustering? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nFor InnoDB, the PRIMARY KEY is the clustered index: the row's data is stored with the primary-key B-tree. A secondary index stores its indexed columns plus the primary-key value, so a query that needs non-indexed columns may require a second lookup into the clustered index. This is why a compact, stable primary key can matter for secondary-index size and I/O.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "How do you check if an index is being used?", "check-index-usage", "Explain `EXPLAIN`.", "**Direct answer :**\nUse `EXPLAIN SELECT ...` to see the execution plan. Look for `type` (e.g., `ref`, `range`, `index`) and `key` to see which index is used. If `type` is `ALL` (full table scan), the index is not used.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you check if an index is being used? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nUse `EXPLAIN SELECT ...` to see the execution plan. Look for `type` (e.g., `ref`, `range`, `index`) and `key` to see which index is used. If `type` is `ALL` (full table scan), the index is not used.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "What is the `EXPLAIN` command? How do you read its output?", "explain-command", "Explain query analysis.", "**Direct answer :**\n`EXPLAIN` shows how MySQL executes a query. Key columns: `id`, `select_type`, `table`, `type` (access method), `possible_keys`, `key`, `key_len`, `ref`, `rows` (estimate), `Extra` (e.g., `Using index` for covering index).\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `EXPLAIN` command? How do you read its output? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`EXPLAIN` shows how MySQL executes a query. Key columns: `id`, `select_type`, `table`, `type` (access method), `possible_keys`, `key`, `key_len`, `ref`, `rows` (estimate), `Extra` (e.g., `Using index` for covering index).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a covering index?", "covering-index", "Define index‑only scans.", "**Direct answer :**\nA covering index contains all the columns needed for a query, so MySQL can return data directly from the index without accessing the table rows. It improves performance. In `EXPLAIN`, you see `Using index`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a covering index? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA covering index contains all the columns needed for a query, so MySQL can return data directly from the index without accessing the table rows. It improves performance. In `EXPLAIN`, you see `Using index`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "How do you create and drop an index?", "create-drop-index", "Provide syntax.", "**Direct answer :**\n`CREATE INDEX index_name ON table (column);`; `DROP INDEX index_name ON table;`. For primary keys: `ALTER TABLE table ADD PRIMARY KEY (col);`\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you create and drop an index? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`CREATE INDEX index_name ON table (column);`; `DROP INDEX index_name ON table;`. For primary keys: `ALTER TABLE table ADD PRIMARY KEY (col);`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "What is a `HASH` index in MySQL? When is it used?", "hash-index", "Explain memory engine usage.", "**Direct answer :**\nMySQL's MEMORY storage engine supports HASH indexes for equality lookups and BTREE indexes for range-capable access. InnoDB's normal user-created indexes are B-tree based; it does have an internal adaptive hash index feature, but that is different from declaring a HASH index on an InnoDB table.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `HASH` index in MySQL? When is it used? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nMySQL's MEMORY storage engine supports HASH indexes for equality lookups and BTREE indexes for range-capable access. InnoDB's normal user-created indexes are B-tree based; it does have an internal adaptive hash index feature, but that is different from declaring a HASH index on an InnoDB table.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "What is the effect of too many indexes on DML operations?", "index-overhead", "Explain trade‑offs.", "**Direct answer :**\nEach index adds overhead on `INSERT`, `UPDATE`, and `DELETE` because the index must be updated. Too many indexes can slow down write performance and increase storage. It's a balance.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the effect of too many indexes on DML operations? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nEach index adds overhead on `INSERT`, `UPDATE`, and `DELETE` because the index must be updated. Too many indexes can slow down write performance and increase storage. It's a balance.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "What is the `OPTIMIZE TABLE` command?", "optimize-table", "Explain table maintenance.", "**Direct answer :**\n`OPTIMIZE TABLE` reorganizes the physical storage of a table and its indexes to reclaim unused space and defragment. It may use table‑locking (InnoDB supports online DDL).\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `OPTIMIZE TABLE` command? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`OPTIMIZE TABLE` reorganizes the physical storage of a table and its indexes to reclaim unused space and defragment. It may use table‑locking (InnoDB supports online DDL).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],

  // ==================== TRANSACTIONS & LOCKING (15) ====================
  ["MySQL", "What is a transaction in MySQL?", "mysql-transaction", "Define ACID properties.", "**Direct answer :**\nA transaction groups related database operations into an atomic unit. With InnoDB, transactions provide ACID properties: atomicity, consistency, isolation, and durability. Typical control statements are `START TRANSACTION`, `COMMIT`, and `ROLLBACK`. A good transaction keeps related changes together and commits only after all required work succeeds.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a transaction in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA transaction groups related database operations into an atomic unit. With InnoDB, transactions provide ACID properties: atomicity, consistency, isolation, and durability. Typical control statements are `START TRANSACTION`, `COMMIT`, and `ROLLBACK`. A good transaction keeps related changes together and commits only after all required work succeeds.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain what happens when two transactions access the same rows, which locks are taken, how isolation changes visibility, and how you would diagnose or retry a conflict safely."],
  ["MySQL", "What are the transaction isolation levels in MySQL?", "isolation-levels", "List and explain.", "**Direct answer :**\nInnoDB supports READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ, and SERIALIZABLE. REPEATABLE READ is the default. These levels trade concurrency and visibility of concurrent changes against stronger consistency guarantees; the exact behavior also depends on whether reads are consistent nonlocking reads or locking reads.\n\n**Why this is asked :**\nThis is asked to test whether you understand what are the transaction isolation levels in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nInnoDB supports READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ, and SERIALIZABLE. REPEATABLE READ is the default. These levels trade concurrency and visibility of concurrent changes against stronger consistency guarantees; the exact behavior also depends on whether reads are consistent nonlocking reads or locking reads.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain what happens when two transactions access the same rows, which locks are taken, how isolation changes visibility, and how you would diagnose or retry a conflict safely."],
  ["MySQL", "What is a dirty read?", "dirty-read", "Define the isolation issue.", "**Direct answer :**\nA dirty read occurs when a transaction reads data that has been modified by another transaction but not yet committed. It can lead to inconsistent results.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a dirty read? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA dirty read occurs when a transaction reads data that has been modified by another transaction but not yet committed. It can lead to inconsistent results.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a non‑repeatable read?", "non-repeatable-read", "Explain.", "**Direct answer :**\nA non‑repeatable read happens when a transaction reads the same row twice and gets different data because another transaction updated it between reads.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a non‑repeatable read? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA non‑repeatable read happens when a transaction reads the same row twice and gets different data because another transaction updated it between reads.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a phantom read?", "phantom-read", "Explain.", "**Direct answer :**\nA phantom read occurs when a transaction executes a query twice and the result set differs because another transaction inserted or deleted rows that match the query condition.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a phantom read? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA phantom read occurs when a transaction executes a query twice and the result set differs because another transaction inserted or deleted rows that match the query condition.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "How does InnoDB implement row‑level locking?", "innodb-row-locking", "Explain MVCC and locking.", "**Direct answer :**\nInnoDB uses multi‑version concurrency control (MVCC) for non‑locking reads. For writes, it uses row‑level locks (shared/exclusive) on index entries. It also supports gap locks and next‑key locks to prevent phantom reads.\n\n**Why this is asked :**\nThis is asked to test whether you understand how does innodb implement row‑level locking? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nInnoDB uses multi‑version concurrency control (MVCC) for non‑locking reads. For writes, it uses row‑level locks (shared/exclusive) on index entries. It also supports gap locks and next‑key locks to prevent phantom reads.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain what happens when two transactions access the same rows, which locks are taken, how isolation changes visibility, and how you would diagnose or retry a conflict safely."],
  ["MySQL", "What is a deadlock and how does MySQL handle it?", "mysql-deadlock", "Explain detection and resolution.", "**Direct answer :**\nA deadlock occurs when transactions wait on locks held by one another, forming a cycle. InnoDB detects deadlocks and rolls back one transaction so the others can proceed. Applications should handle the resulting error by retrying when appropriate, while engineers reduce deadlocks through consistent lock ordering, short transactions, suitable indexes, and avoiding unnecessary locking.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a deadlock and how does MySQL handle it? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA deadlock occurs when transactions wait on locks held by one another, forming a cycle. InnoDB detects deadlocks and rolls back one transaction so the others can proceed. Applications should handle the resulting error by retrying when appropriate, while engineers reduce deadlocks through consistent lock ordering, short transactions, suitable indexes, and avoiding unnecessary locking.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain what happens when two transactions access the same rows, which locks are taken, how isolation changes visibility, and how you would diagnose or retry a conflict safely."],
  ["MySQL", "What is the `AUTOCOMMIT` mode?", "autocommit", "Explain default behavior.", "**Direct answer :**\nMySQL has autocommit enabled by default. In autocommit mode, each statement is committed automatically when it succeeds, so a later ROLLBACK cannot undo a statement that has already committed. Use `START TRANSACTION`/`COMMIT`/`ROLLBACK`, or disable autocommit for a session, when several statements must succeed or fail together.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `AUTOCOMMIT` mode? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nMySQL has autocommit enabled by default. In autocommit mode, each statement is committed automatically when it succeeds, so a later ROLLBACK cannot undo a statement that has already committed. Use `START TRANSACTION`/`COMMIT`/`ROLLBACK`, or disable autocommit for a session, when several statements must succeed or fail together.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `SAVEPOINT` feature?", "savepoint", "Explain partial rollback.", "**Direct answer :**\n`SAVEPOINT` creates a point within a transaction to which you can roll back without aborting the entire transaction. Use `ROLLBACK TO SAVEPOINT sp;`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `SAVEPOINT` feature? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`SAVEPOINT` creates a point within a transaction to which you can roll back without aborting the entire transaction. Use `ROLLBACK TO SAVEPOINT sp;`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a lock wait timeout?", "lock-wait-timeout", "Define the setting.", "**Direct answer :**\n`innodb_lock_wait_timeout` controls how long an InnoDB transaction waits for a row lock before returning a lock-wait timeout error. It is distinct from deadlock detection: InnoDB normally detects deadlocks immediately, whereas a lock wait can persist until the timeout. The appropriate value depends on workload and application retry behavior.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a lock wait timeout? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`innodb_lock_wait_timeout` controls how long an InnoDB transaction waits for a row lock before returning a lock-wait timeout error. It is distinct from deadlock detection: InnoDB normally detects deadlocks immediately, whereas a lock wait can persist until the timeout. The appropriate value depends on workload and application retry behavior.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain what happens when two transactions access the same rows, which locks are taken, how isolation changes visibility, and how you would diagnose or retry a conflict safely."],
  ["MySQL", "What is the difference between table‑level and row‑level locking?", "table-vs-row-locking", "Compare the two.", "**Direct answer :**\nTable‑level locks (MyISAM) lock the entire table, reducing concurrency. Row‑level locks (InnoDB) lock only specific rows, allowing higher concurrency but with more overhead.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between table‑level and row‑level locking? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nTable‑level locks (MyISAM) lock the entire table, reducing concurrency. Row‑level locks (InnoDB) lock only specific rows, allowing higher concurrency but with more overhead.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain what happens when two transactions access the same rows, which locks are taken, how isolation changes visibility, and how you would diagnose or retry a conflict safely."],
  ["MySQL", "What is the `GET_LOCK()` function?", "get-lock", "Explain advisory locks.", "**Direct answer :**\n`GET_LOCK(str, timeout)` acquires an advisory lock at the application level. It can be used for custom coordination between sessions.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `GET_LOCK()` function? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`GET_LOCK(str, timeout)` acquires an advisory lock at the application level. It can be used for custom coordination between sessions.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain what happens when two transactions access the same rows, which locks are taken, how isolation changes visibility, and how you would diagnose or retry a conflict safely."],
  ["MySQL", "How do you see current locks and transactions?", "view-locks", "Explain `INFORMATION_SCHEMA`.", "**Direct answer :**\nFor current MySQL versions, use Performance Schema and Information Schema views to investigate transactions, locks, and waits. Depending on the version, useful objects include `performance_schema.data_locks`, `performance_schema.data_lock_waits`, and `information_schema.innodb_trx`. `SHOW ENGINE INNODB STATUS` is also valuable for deadlock and InnoDB diagnostics.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you see current locks and transactions? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nFor current MySQL versions, use Performance Schema and Information Schema views to investigate transactions, locks, and waits. Depending on the version, useful objects include `performance_schema.data_locks`, `performance_schema.data_lock_waits`, and `information_schema.innodb_trx`. `SHOW ENGINE INNODB STATUS` is also valuable for deadlock and InnoDB diagnostics.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain what happens when two transactions access the same rows, which locks are taken, how isolation changes visibility, and how you would diagnose or retry a conflict safely."],
  ["MySQL", "What is the `SHOW ENGINE INNODB STATUS` output used for?", "show-engine-status", "Explain diagnostic output.", "**Direct answer :**\nThis command provides a detailed report on InnoDB's internal state, including locks, transactions, and deadlock information. It is useful for troubleshooting.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `SHOW ENGINE INNODB STATUS` output used for? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nThis command provides a detailed report on InnoDB's internal state, including locks, transactions, and deadlock information. It is useful for troubleshooting.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between `READ COMMITTED` and `REPEATABLE READ`?", "read-committed-vs-repeatable-read", "Compare the two.", "**Direct answer :**\n`READ COMMITTED` allows non‑repeatable reads (different results in the same transaction). `REPEATABLE READ` guarantees that within a transaction, repeated reads return the same data (using MVCC snapshots).\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `READ COMMITTED` and `REPEATABLE READ`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`READ COMMITTED` allows non‑repeatable reads (different results in the same transaction). `REPEATABLE READ` guarantees that within a transaction, repeated reads return the same data (using MVCC snapshots).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],

  // ==================== STORED PROCEDURES & FUNCTIONS (15) ====================
  ["MySQL", "What is a stored procedure in MySQL?", "stored-procedure", "Define and explain benefits.", "**Direct answer :**\nA stored procedure is a precompiled collection of SQL statements that can be called by name. It improves performance (less network traffic) and code reusability, and can be used for complex business logic.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a stored procedure in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA stored procedure is a precompiled collection of SQL statements that can be called by name. It improves performance (less network traffic) and code reusability, and can be used for complex business logic.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What is the syntax to create a stored procedure?", "create-procedure", "Provide example.", "**Direct answer :**\n`CREATE PROCEDURE GetEmployees (IN dept_id INT) BEGIN SELECT * FROM employees WHERE department_id = dept_id; END;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the syntax to create a stored procedure? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`CREATE PROCEDURE GetEmployees (IN dept_id INT) BEGIN SELECT * FROM employees WHERE department_id = dept_id; END;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "How do you call a stored procedure?", "call-procedure", "Provide syntax.", "**Direct answer :**\n`CALL GetEmployees(5);`\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you call a stored procedure? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`CALL GetEmployees(5);`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What is the difference between a stored procedure and a function?", "procedure-vs-function", "Compare the two.", "**Direct answer :**\nA stored procedure is invoked with `CALL` and can use IN, OUT, and INOUT parameters. A stored function returns a value and can be used in SQL expressions, subject to MySQL's stored-function restrictions. They are different database-programming constructs, not simply two names for the same feature.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between a stored procedure and a function? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA stored procedure is invoked with `CALL` and can use IN, OUT, and INOUT parameters. A stored function returns a value and can be used in SQL expressions, subject to MySQL's stored-function restrictions. They are different database-programming constructs, not simply two names for the same feature.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What are the parameters types in stored procedures?", "procedure-parameters", "Explain IN, OUT, INOUT.", "**Direct answer :**\n`IN` (input, default), `OUT` (output, returns a value), `INOUT` (both).\n\n**Why this is asked :**\nThis is asked to test whether you understand what are the parameters types in stored procedures? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`IN` (input, default), `OUT` (output, returns a value), `INOUT` (both).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What is a stored function? Give an example.", "stored-function", "Define and show usage.", "**Direct answer :**\nA stored function returns a scalar value. Example: `CREATE FUNCTION GetFullName (fname VARCHAR(50), lname VARCHAR(50)) RETURNS VARCHAR(100) RETURN CONCAT(fname, ' ', lname);`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a stored function? Give an example. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA stored function returns a scalar value. Example: `CREATE FUNCTION GetFullName (fname VARCHAR(50), lname VARCHAR(50)) RETURNS VARCHAR(100) RETURN CONCAT(fname, ' ', lname);`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What is the difference between `CHAR` and `VARCHAR` in stored procedures?", "char-varchar-proc", "No difference in logic, but data types matter for parameters.", "**Direct answer :**\nIn stored procedures, the data type of parameters matters for performance and storage. `CHAR` is fixed‑length and may pad with spaces, while `VARCHAR` is variable‑length. However, the logic for using them is the same as in table definitions. Choose based on the expected length of the input values.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `CHAR` and `VARCHAR` in stored procedures? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nIn stored procedures, the data type of parameters matters for performance and storage. `CHAR` is fixed‑length and may pad with spaces, while `VARCHAR` is variable‑length. However, the logic for using them is the same as in table definitions. Choose based on the expected length of the input values.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a cursor in MySQL? How is it used?", "cursor", "Explain row‑by‑row processing.", "**Direct answer :**\nA cursor allows you to iterate through a result set row by row inside a stored procedure. Steps: `DECLARE`, `OPEN`, `FETCH`, `CLOSE`. Example: `DECLARE cur CURSOR FOR SELECT id FROM users;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a cursor in MySQL? How is it used? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA cursor allows you to iterate through a result set row by row inside a stored procedure. Steps: `DECLARE`, `OPEN`, `FETCH`, `CLOSE`. Example: `DECLARE cur CURSOR FOR SELECT id FROM users;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What is the `DECLARE ... HANDLER` statement?", "handler", "Explain error handling.", "**Direct answer :**\n`DECLARE handler_type HANDLER FOR condition_value ...` defines how to handle errors or warnings. For example, `DECLARE CONTINUE HANDLER FOR NOT FOUND SET done = TRUE;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `DECLARE ... HANDLER` statement? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`DECLARE handler_type HANDLER FOR condition_value ...` defines how to handle errors or warnings. For example, `DECLARE CONTINUE HANDLER FOR NOT FOUND SET done = TRUE;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What is the `IF` and `CASE` statement in stored procedures?", "if-case-proc", "Explain control flow.", "**Direct answer :**\nStored procedures support `IF ... THEN ... ELSEIF ... ELSE ... END IF;` and `CASE ... WHEN ... THEN ... ELSE ... END CASE;` for conditional logic.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `IF` and `CASE` statement in stored procedures? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nStored procedures support `IF ... THEN ... ELSEIF ... ELSE ... END IF;` and `CASE ... WHEN ... THEN ... ELSE ... END CASE;` for conditional logic.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What are loops in MySQL? (`WHILE`, `REPEAT`, `LOOP`)", "loops", "Explain loop constructs.", "**Direct answer :**\n`WHILE condition DO ... END WHILE;`, `REPEAT ... UNTIL condition END REPEAT;`, `LOOP ... END LOOP;` with `LEAVE` to exit.\n\n**Why this is asked :**\nThis is asked to test whether you understand what are loops in MySQL? (`WHILE`, `REPEAT`, `LOOP`) beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`WHILE condition DO ... END WHILE;`, `REPEAT ... UNTIL condition END REPEAT;`, `LOOP ... END LOOP;` with `LEAVE` to exit.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a prepared statement in MySQL?", "prepared-statement", "Explain dynamic SQL.", "**Direct answer :**\nPrepared statements separate SQL structure from parameter values and can reduce parsing overhead when the same statement is executed repeatedly. Parameter binding also helps prevent SQL injection when used correctly. In application code, prefer the driver's prepared/parameterized APIs; MySQL also provides SQL-level `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a prepared statement in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nPrepared statements separate SQL structure from parameter values and can reduce parsing overhead when the same statement is executed repeatedly. Parameter binding also helps prevent SQL injection when used correctly. In application code, prefer the driver's prepared/parameterized APIs; MySQL also provides SQL-level `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `DELIMITER` command used for in stored procedures?", "delimiter", "Explain changing the statement delimiter.", "**Direct answer :**\nThe `DELIMITER` command changes the default semicolon (;) to another character so that the entire procedure definition is sent as a single unit. Example: `DELIMITER // ... CREATE PROCEDURE ... //`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `DELIMITER` command used for in stored procedures? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nThe `DELIMITER` command changes the default semicolon (;) to another character so that the entire procedure definition is sent as a single unit. Example: `DELIMITER // ... CREATE PROCEDURE ... //`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "How do you view existing stored procedures?", "show-procedures", "List them.", "**Direct answer :**\n`SHOW PROCEDURE STATUS;` or query `INFORMATION_SCHEMA.ROUTINES`.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you view existing stored procedures? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`SHOW PROCEDURE STATUS;` or query `INFORMATION_SCHEMA.ROUTINES`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "How do you drop a stored procedure?", "drop-procedure", "Provide syntax.", "**Direct answer :**\n`DROP PROCEDURE IF EXISTS procedure_name;`\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you drop a stored procedure? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`DROP PROCEDURE IF EXISTS procedure_name;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],

  // ==================== TRIGGERS & EVENTS (10) ====================
  ["MySQL", "What is a trigger in MySQL?", "trigger-definition", "Define and explain its purpose.", "**Direct answer :**\nA trigger is a set of SQL statements that automatically execute in response to an INSERT, UPDATE, or DELETE operation on a table. They are used for auditing, validation, and maintaining derived data.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a trigger in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA trigger is a set of SQL statements that automatically execute in response to an INSERT, UPDATE, or DELETE operation on a table. They are used for auditing, validation, and maintaining derived data.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss trigger timing, transactional behavior, restrictions, hidden side effects, and when application/service logic is preferable to trigger-based business logic."],
  ["MySQL", "What are the trigger events and timing?", "trigger-events", "Explain BEFORE/AFTER and events.", "**Direct answer :**\nTrigger timing: `BEFORE` or `AFTER`. Events: `INSERT`, `UPDATE`, `DELETE`. Example: `CREATE TRIGGER before_employee_insert BEFORE INSERT ON employees FOR EACH ROW ...`\n\n**Why this is asked :**\nThis is asked to test whether you understand what are the trigger events and timing? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nTrigger timing: `BEFORE` or `AFTER`. Events: `INSERT`, `UPDATE`, `DELETE`. Example: `CREATE TRIGGER before_employee_insert BEFORE INSERT ON employees FOR EACH ROW ...`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss trigger timing, transactional behavior, restrictions, hidden side effects, and when application/service logic is preferable to trigger-based business logic."],
  ["MySQL", "What is the difference between a `BEFORE` and `AFTER` trigger?", "before-vs-after-trigger", "Compare execution timing.", "**Direct answer :**\n`BEFORE` trigger executes before the operation, allowing modification of the new row. `AFTER` trigger executes after the operation, used for actions that should not alter the row.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between a `BEFORE` and `AFTER` trigger? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`BEFORE` trigger executes before the operation, allowing modification of the new row. `AFTER` trigger executes after the operation, used for actions that should not alter the row.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss trigger timing, transactional behavior, restrictions, hidden side effects, and when application/service logic is preferable to trigger-based business logic."],
  ["MySQL", "Can you call a stored procedure from a trigger?", "trigger-call-procedure", "Yes, you can call a stored procedure from a trigger.", "**Direct answer :**\nA trigger can invoke a stored procedure with restrictions, but not every procedure is legal in a trigger. For example, a called procedure cannot return a result set to the client or use dynamic SQL in this context, and transaction-control statements such as COMMIT/ROLLBACK are not allowed in triggers. Keep trigger logic small and predictable because it runs as part of the triggering statement.\n\n**Why this is asked :**\nThis is asked to test whether you understand can you call a stored procedure from a trigger? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA trigger can invoke a stored procedure with restrictions, but not every procedure is legal in a trigger. For example, a called procedure cannot return a result set to the client or use dynamic SQL in this context, and transaction-control statements such as COMMIT/ROLLBACK are not allowed in triggers. Keep trigger logic small and predictable because it runs as part of the triggering statement.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss trigger timing, transactional behavior, restrictions, hidden side effects, and when application/service logic is preferable to trigger-based business logic."],
  ["MySQL", "What is an event scheduler in MySQL?", "event-scheduler", "Explain scheduled tasks.", "**Direct answer :**\nMySQL Event Scheduler allows you to schedule recurring tasks (e.g., daily backups, cleanup jobs). Use `CREATE EVENT` to define an event.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is an event scheduler in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nMySQL Event Scheduler allows you to schedule recurring tasks (e.g., daily backups, cleanup jobs). Use `CREATE EVENT` to define an event.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "How do you create and start an event?", "create-event", "Provide syntax.", "**Direct answer :**\n`CREATE EVENT daily_cleanup ON SCHEDULE EVERY 1 DAY DO DELETE FROM logs WHERE created_at < NOW() - INTERVAL 30 DAY;`\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you create and start an event? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`CREATE EVENT daily_cleanup ON SCHEDULE EVERY 1 DAY DO DELETE FROM logs WHERE created_at < NOW() - INTERVAL 30 DAY;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between a trigger and an event?", "trigger-vs-event", "Compare the two.", "**Direct answer :**\nTriggers fire automatically on data changes. Events are scheduled and run based on time, not data changes.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between a trigger and an event? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nTriggers fire automatically on data changes. Events are scheduled and run based on time, not data changes.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss trigger timing, transactional behavior, restrictions, hidden side effects, and when application/service logic is preferable to trigger-based business logic."],
  ["MySQL", "How do you drop a trigger?", "drop-trigger", "Syntax: `DROP TRIGGER trigger_name;`", "**Direct answer :**\nTo drop a trigger, use the command `DROP TRIGGER IF EXISTS trigger_name;`. It's good practice to use `IF EXISTS` to avoid errors if the trigger doesn't exist. You need the `TRIGGER` privilege on the table. Dropping a trigger removes it permanently; ensure it's not needed for any application logic.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you drop a trigger? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nTo drop a trigger, use the command `DROP TRIGGER IF EXISTS trigger_name;`. It's good practice to use `IF EXISTS` to avoid errors if the trigger doesn't exist. You need the `TRIGGER` privilege on the table. Dropping a trigger removes it permanently; ensure it's not needed for any application logic.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss trigger timing, transactional behavior, restrictions, hidden side effects, and when application/service logic is preferable to trigger-based business logic."],
  ["MySQL", "Can you use `ROLLBACK` inside a trigger?", "trigger-rollback", "Yes, if the trigger is in a transaction, you can rollback to undo the triggering statement (InnoDB).", "**Direct answer :**\nNo. MySQL triggers cannot explicitly use transaction-control statements such as `COMMIT` or `ROLLBACK`. If a trigger raises an error, the triggering statement can fail, and for transactional tables its changes are rolled back as part of statement/transaction error handling. Use application-level transaction control rather than trying to roll back from inside the trigger.\n\n**Why this is asked :**\nThis is asked to test whether you understand can you use `rollback` inside a trigger? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nNo. MySQL triggers cannot explicitly use transaction-control statements such as `COMMIT` or `ROLLBACK`. If a trigger raises an error, the triggering statement can fail, and for transactional tables its changes are rolled back as part of statement/transaction error handling. Use application-level transaction control rather than trying to roll back from inside the trigger.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss trigger timing, transactional behavior, restrictions, hidden side effects, and when application/service logic is preferable to trigger-based business logic."],
  ["MySQL", "What is the `NEW` and `OLD` pseudo‑records?", "new-old", "Explain accessing row values.", "**Direct answer :**\nIn triggers, `NEW` refers to the new row (for INSERT/UPDATE), `OLD` refers to the old row (for DELETE/UPDATE). You can access columns like `NEW.name`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `NEW` and `OLD` pseudo‑records? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nIn triggers, `NEW` refers to the new row (for INSERT/UPDATE), `OLD` refers to the old row (for DELETE/UPDATE). You can access columns like `NEW.name`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],

  // ==================== VIEWS (10) ====================
  ["MySQL", "What is a view in MySQL?", "view-definition", "Define virtual table.", "**Direct answer :**\nA view is a named, pre‑defined query that presents data from one or more tables as if it were a table. It does not store data but provides a filtered or aggregated perspective.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a view in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA view is a named, pre‑defined query that presents data from one or more tables as if it were a table. It does not store data but provides a filtered or aggregated perspective.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain whether the view is updatable, how underlying changes are reflected, how security is affected, and whether the resulting query plan has performance implications."],
  ["MySQL", "What is the difference between a view and a table?", "view-vs-table", "Compare storage and usage.", "**Direct answer :**\nA table stores physical data. A view is a virtual table defined by a query; changes to underlying tables are reflected in the view. Views can restrict access to sensitive columns.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between a view and a table? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA table stores physical data. A view is a virtual table defined by a query; changes to underlying tables are reflected in the view. Views can restrict access to sensitive columns.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain whether the view is updatable, how underlying changes are reflected, how security is affected, and whether the resulting query plan has performance implications."],
  ["MySQL", "What is a materialized view? Does MySQL support it?", "materialized-view", "Explain and mention workarounds.", "**Direct answer :**\nA materialized view stores the result of a query physically and refreshes it according to a chosen strategy. MySQL does not provide a native `CREATE MATERIALIZED VIEW` feature. A common workaround is a table populated or refreshed by scheduled jobs or application logic, with indexes designed for the read workload.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a materialized view? Does MySQL support it? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA materialized view stores the result of a query physically and refreshes it according to a chosen strategy. MySQL does not provide a native `CREATE MATERIALIZED VIEW` feature. A common workaround is a table populated or refreshed by scheduled jobs or application logic, with indexes designed for the read workload.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain whether the view is updatable, how underlying changes are reflected, how security is affected, and whether the resulting query plan has performance implications."],
  ["MySQL", "How do you create a view? Provide an example.", "create-view", "Syntax: `CREATE VIEW view_name AS SELECT ...`; Example: `CREATE VIEW high_salary AS SELECT name, salary FROM employees WHERE salary > 100000;`", "**Direct answer :**\nTo create a view, use `CREATE VIEW view_name AS SELECT ...`. For example, `CREATE VIEW high_salary AS SELECT name, salary FROM employees WHERE salary > 100000;` creates a view showing high‑salary employees. Views are virtual tables that improve security and simplify queries. They don’t store data; they are saved queries that reflect live data.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you create a view? provide an example. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nTo create a view, use `CREATE VIEW view_name AS SELECT ...`. For example, `CREATE VIEW high_salary AS SELECT name, salary FROM employees WHERE salary > 100000;` creates a view showing high‑salary employees. Views are virtual tables that improve security and simplify queries. They don’t store data; they are saved queries that reflect live data.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain whether the view is updatable, how underlying changes are reflected, how security is affected, and whether the resulting query plan has performance implications."],
  ["MySQL", "Can you insert/update/delete through a view?", "updatable-view", "Explain conditions.", "**Direct answer :**\nA view is updatable if it contains no joins, aggregates, `DISTINCT`, `GROUP BY`, `HAVING`, subqueries, etc. In many cases, updates are not allowed; you can use `WITH CHECK OPTION` to enforce constraints.\n\n**Why this is asked :**\nThis is asked to test whether you understand can you insert/update/delete through a view? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA view is updatable if it contains no joins, aggregates, `DISTINCT`, `GROUP BY`, `HAVING`, subqueries, etc. In many cases, updates are not allowed; you can use `WITH CHECK OPTION` to enforce constraints.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain whether the view is updatable, how underlying changes are reflected, how security is affected, and whether the resulting query plan has performance implications."],
  ["MySQL", "What is the `WITH CHECK OPTION` clause?", "with-check-option", "Explain its use.", "**Direct answer :**\nWhen creating a view, `WITH CHECK OPTION` prevents updates that would cause rows to disappear from the view. It ensures that any inserted or updated row satisfies the view's `WHERE` condition.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `WITH CHECK OPTION` clause? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nWhen creating a view, `WITH CHECK OPTION` prevents updates that would cause rows to disappear from the view. It ensures that any inserted or updated row satisfies the view's `WHERE` condition.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "How do you drop a view?", "drop-view", "Syntax: `DROP VIEW view_name;`", "**Direct answer :**\nTo drop a view, use `DROP VIEW IF EXISTS view_name;`. Using `IF EXISTS` avoids errors if the view doesn't exist. Dropping a view removes its definition but does not affect the underlying tables.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you drop a view? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nTo drop a view, use `DROP VIEW IF EXISTS view_name;`. Using `IF EXISTS` avoids errors if the view doesn't exist. Dropping a view removes its definition but does not affect the underlying tables.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain whether the view is updatable, how underlying changes are reflected, how security is affected, and whether the resulting query plan has performance implications."],
  ["MySQL", "What is the `SHOW CREATE VIEW` command?", "show-create-view", "Display the view definition.", "**Direct answer :**\n`SHOW CREATE VIEW view_name;` shows the query that defines the view.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `SHOW CREATE VIEW` command? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`SHOW CREATE VIEW view_name;` shows the query that defines the view.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain whether the view is updatable, how underlying changes are reflected, how security is affected, and whether the resulting query plan has performance implications."],
  ["MySQL", "Can you use a view as a base for another view?", "nested-view", "Yes, views can be based on other views, though performance may suffer.", "**Direct answer :**\nYes, a view can reference another view. This can improve abstraction and reuse, but deeply layered views can make query plans and troubleshooting harder and may complicate optimization. Use them when they improve maintainability, and inspect the resulting execution plan for important workloads.\n\n**Why this is asked :**\nThis is asked to test whether you understand can you use a view as a base for another view? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nYes, a view can reference another view. This can improve abstraction and reuse, but deeply layered views can make query plans and troubleshooting harder and may complicate optimization. Use them when they improve maintainability, and inspect the resulting execution plan for important workloads.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain whether the view is updatable, how underlying changes are reflected, how security is affected, and whether the resulting query plan has performance implications."],
  ["MySQL", "What are the advantages of using views?", "view-advantages", "List benefits.", "**Direct answer :**\nViews provide security (hide columns), simplification (hide complex joins), data abstraction (present only necessary data), and consistency (centralize logic).\n\n**Why this is asked :**\nThis is asked to test whether you understand what are the advantages of using views? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nViews provide security (hide columns), simplification (hide complex joins), data abstraction (present only necessary data), and consistency (centralize logic).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain whether the view is updatable, how underlying changes are reflected, how security is affected, and whether the resulting query plan has performance implications."],

  // ==================== BACKUP & RESTORE (10) ====================
  ["MySQL", "How do you backup a MySQL database?", "mysql-backup", "Explain `mysqldump`.", "**Direct answer :**\nUse `mysqldump -u username -p database_name > backup.sql`. For all databases: `mysqldump --all-databases > all_backup.sql`. Include options like `--single-transaction` for consistency.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you backup a mysql database? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nUse `mysqldump -u username -p database_name > backup.sql`. For all databases: `mysqldump --all-databases > all_backup.sql`. Include options like `--single-transaction` for consistency.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss restore testing, consistency, point-in-time recovery, binary-log retention, RPO/RTO, and how you would validate that a backup is actually recoverable."],
  ["MySQL", "How do you restore a backup?", "mysql-restore", "Explain restore methods.", "**Direct answer :**\nUse `mysql -u username -p database_name < backup.sql`. If the backup contains `CREATE DATABASE`, you can restore without specifying a database.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you restore a backup? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nUse `mysql -u username -p database_name < backup.sql`. If the backup contains `CREATE DATABASE`, you can restore without specifying a database.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss restore testing, consistency, point-in-time recovery, binary-log retention, RPO/RTO, and how you would validate that a backup is actually recoverable."],
  ["MySQL", "What is the difference between logical and physical backup?", "logical-vs-physical-backup", "Compare the two.", "**Direct answer :**\nLogical backup (mysqldump) exports SQL statements, is portable, but slower for large databases. Physical backup (copying data files) is faster but less portable and requires the server to be stopped.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between logical and physical backup? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nLogical backup (mysqldump) exports SQL statements, is portable, but slower for large databases. Physical backup (copying data files) is faster but less portable and requires the server to be stopped.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss restore testing, consistency, point-in-time recovery, binary-log retention, RPO/RTO, and how you would validate that a backup is actually recoverable."],
  ["MySQL", "What is `mysqldump` and what are its common options?", "mysqldump-options", "List useful options.", "**Direct answer :**\n`--single-transaction` (consistent snapshot for InnoDB), `--no-data` (schema only), `--no-create-info` (data only), `--add-drop-table`, `--routines`, `--triggers`, `--events`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is `mysqldump` and what are its common options? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`--single-transaction` (consistent snapshot for InnoDB), `--no-data` (schema only), `--no-create-info` (data only), `--add-drop-table`, `--routines`, `--triggers`, `--events`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the `mysqlpump` utility?", "mysqlpump", "Explain the newer backup tool.", "**Direct answer :**\n`mysqlpump` is a logical backup utility from MySQL 5.7/8.0 that provided parallelism and filtering capabilities. It was deprecated in MySQL 8.0.31 and removed in MySQL 8.4, so current deployments should use supported backup approaches such as `mysqldump` for logical dumps or MySQL Enterprise Backup/other appropriate physical backup tooling for production recovery requirements.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `mysqlpump` utility? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`mysqlpump` is a logical backup utility from MySQL 5.7/8.0 that provided parallelism and filtering capabilities. It was deprecated in MySQL 8.0.31 and removed in MySQL 8.4, so current deployments should use supported backup approaches such as `mysqldump` for logical dumps or MySQL Enterprise Backup/other appropriate physical backup tooling for production recovery requirements.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "How do you backup a single table?", "backup-single-table", "Command: `mysqldump -u user -p db_name table_name > table.sql`", "**Direct answer :**\nTo backup a single table, use `mysqldump -u username -p database_name table_name > backup.sql`. This creates a file containing the SQL statements to recreate that table and its data. It's useful for selective backups or migration of individual tables.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you backup a single table? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nTo backup a single table, use `mysqldump -u username -p database_name table_name > backup.sql`. This creates a file containing the SQL statements to recreate that table and its data. It's useful for selective backups or migration of individual tables.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss restore testing, consistency, point-in-time recovery, binary-log retention, RPO/RTO, and how you would validate that a backup is actually recoverable."],
  ["MySQL", "What is the purpose of the `binary log` (binlog) in backup?", "binary-log", "Explain point‑in‑time recovery.", "**Direct answer :**\nThe binary log records all changes to the database. It enables point‑in‑time recovery: you can restore a full backup and then replay binlog events to reach a specific time.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the purpose of the `binary log` (binlog) in backup? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nThe binary log records all changes to the database. It enables point‑in‑time recovery: you can restore a full backup and then replay binlog events to reach a specific time.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is `mysqlbinlog` used for?", "mysqlbinlog", "Explain the tool.", "**Direct answer :**\n`mysqlbinlog` is a utility to read the binary log files in a human‑readable format. It can be used to replay changes or to view transaction history.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is `mysqlbinlog` used for? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`mysqlbinlog` is a utility to read the binary log files in a human‑readable format. It can be used to replay changes or to view transaction history.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss restore testing, consistency, point-in-time recovery, binary-log retention, RPO/RTO, and how you would validate that a backup is actually recoverable."],
  ["MySQL", "How do you perform a point‑in‑time recovery?", "pitr", "Explain the process.", "**Direct answer :**\n1. Restore the last full backup. 2. Use `mysqlbinlog` to apply binary logs from the time of the backup up to the desired time: `mysqlbinlog --start-datetime=\"...\" binlog.000001 | mysql -u root -p`.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you perform a point‑in‑time recovery? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n1. Restore the last full backup. 2. Use `mysqlbinlog` to apply binary logs from the time of the backup up to the desired time: `mysqlbinlog --start-datetime=\"...\" binlog.000001 | mysql -u root -p`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss restore testing, consistency, point-in-time recovery, binary-log retention, RPO/RTO, and how you would validate that a backup is actually recoverable."],
  ["MySQL", "What is the `--single-transaction` option in mysqldump?", "single-transaction", "Explain its importance.", "**Direct answer :**\n`mysqldump --single-transaction` starts a transaction to obtain a consistent snapshot for transactional tables such as InnoDB without taking a global table lock for the dump. It is useful for reducing application disruption during logical backups. It does not make nontransactional tables such as MyISAM transactionally consistent, and concurrent DDL can still affect the dump.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `--single-transaction` option in mysqldump? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`mysqldump --single-transaction` starts a transaction to obtain a consistent snapshot for transactional tables such as InnoDB without taking a global table lock for the dump. It is useful for reducing application disruption during logical backups. It does not make nontransactional tables such as MyISAM transactionally consistent, and concurrent DDL can still affect the dump.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain what happens when two transactions access the same rows, which locks are taken, how isolation changes visibility, and how you would diagnose or retry a conflict safely."],

  // ==================== SECURITY & USERS (10) ====================
  ["MySQL", "How do you create a new user in MySQL?", "create-user", "Provide syntax.", "**Direct answer :**\n`CREATE USER 'username'@'host' IDENTIFIED BY 'password';` Example: `CREATE USER 'john'@'localhost' IDENTIFIED BY 'securepass';`\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you create a new user in mysql? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`CREATE USER 'username'@'host' IDENTIFIED BY 'password';` Example: `CREATE USER 'john'@'localhost' IDENTIFIED BY 'securepass';`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss least privilege, authentication versus authorization, TLS in transit, account scope, and how you would avoid exposing sensitive data or credentials."],
  ["MySQL", "How do you grant privileges to a user?", "grant-privileges", "Explain `GRANT`.", "**Direct answer :**\nUse `GRANT` to assign only the privileges an account actually needs, for example `GRANT SELECT, INSERT ON database.* TO 'username'@'host';`. With modern MySQL, privilege changes made through `CREATE USER`, `GRANT`, `REVOKE`, and related account statements take effect through the server's privilege system; you generally do not need `FLUSH PRIVILEGES` after normal GRANT/REVOKE operations.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you grant privileges to a user? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nUse `GRANT` to assign only the privileges an account actually needs, for example `GRANT SELECT, INSERT ON database.* TO 'username'@'host';`. With modern MySQL, privilege changes made through `CREATE USER`, `GRANT`, `REVOKE`, and related account statements take effect through the server's privilege system; you generally do not need `FLUSH PRIVILEGES` after normal GRANT/REVOKE operations.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss least privilege, authentication versus authorization, TLS in transit, account scope, and how you would avoid exposing sensitive data or credentials."],
  ["MySQL", "How do you revoke privileges?", "revoke-privileges", "Explain `REVOKE`.", "**Direct answer :**\n`REVOKE SELECT ON database.* FROM 'username'@'host';`\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you revoke privileges? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`REVOKE SELECT ON database.* FROM 'username'@'host';`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss least privilege, authentication versus authorization, TLS in transit, account scope, and how you would avoid exposing sensitive data or credentials."],
  ["MySQL", "What are the different levels of privileges?", "privilege-levels", "Explain global, database, table, column.", "**Direct answer :**\nGlobal (`*.*`), database (`db.*`), table (`db.table`), column (specific columns), and routine (`PROCEDURE`, `FUNCTION`).\n\n**Why this is asked :**\nThis is asked to test whether you understand what are the different levels of privileges? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nGlobal (`*.*`), database (`db.*`), table (`db.table`), column (specific columns), and routine (`PROCEDURE`, `FUNCTION`).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss least privilege, authentication versus authorization, TLS in transit, account scope, and how you would avoid exposing sensitive data or credentials."],
  ["MySQL", "What is the `root` user and why should you be careful with it?", "root-user", "Explain security.", "**Direct answer :**\nThe `root` user has all privileges. It should have a strong password and be used sparingly. Create separate application users with minimal required privileges.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `root` user and why should you be careful with it? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nThe `root` user has all privileges. It should have a strong password and be used sparingly. Create separate application users with minimal required privileges.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss least privilege, authentication versus authorization, TLS in transit, account scope, and how you would avoid exposing sensitive data or credentials."],
  ["MySQL", "How do you change a user's password?", "change-password", "Syntax: `ALTER USER 'user'@'host' IDENTIFIED BY 'new_password';`", "**Direct answer :**\nUse `ALTER USER 'username'@'host' IDENTIFIED BY 'new_password';` to change an account password. This is the normal account-management statement; avoid directly modifying MySQL system tables. Choose strong credentials and the authentication configuration appropriate for the deployment.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you change a user's password? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nUse `ALTER USER 'username'@'host' IDENTIFIED BY 'new_password';` to change an account password. This is the normal account-management statement; avoid directly modifying MySQL system tables. Choose strong credentials and the authentication configuration appropriate for the deployment.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between `DROP USER` and `DELETE FROM mysql.user`?", "drop-user-vs-delete", "Explain the proper way.", "**Direct answer :**\nAlways use `DROP USER` to remove a user. Directly deleting from `mysql.user` may leave orphaned privileges and is not recommended.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `DROP USER` and `DELETE FROM mysql.user`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nAlways use `DROP USER` to remove a user. Directly deleting from `mysql.user` may leave orphaned privileges and is not recommended.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss least privilege, authentication versus authorization, TLS in transit, account scope, and how you would avoid exposing sensitive data or credentials."],
  ["MySQL", "What is the `mysql` system database used for?", "mysql-db", "Explain its purpose.", "**Direct answer :**\nThe `mysql` database contains system tables that store user accounts, privileges, and server metadata. It is essential for authentication and authorization.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `mysql` system database used for? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nThe `mysql` database contains system tables that store user accounts, privileges, and server metadata. It is essential for authentication and authorization.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is SSL/TLS encryption in MySQL?", "ssl-encryption", "Explain secure connections.", "**Direct answer :**\nMySQL supports encrypted connections using SSL/TLS to protect data in transit. You can require SSL for certain users with `REQUIRE SSL`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is SSL/TLS encryption in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nMySQL supports encrypted connections using SSL/TLS to protect data in transit. You can require SSL for certain users with `REQUIRE SSL`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss least privilege, authentication versus authorization, TLS in transit, account scope, and how you would avoid exposing sensitive data or credentials."],
  ["MySQL", "What is the `FLUSH PRIVILEGES` command?", "flush-privileges", "Explain its use.", "**Direct answer :**\n`FLUSH PRIVILEGES` tells MySQL to reload privilege information from the grant tables. It is mainly relevant when grant-table data has been changed directly, which is not the normal account-management path. Changes made through statements such as `GRANT`, `REVOKE`, `CREATE USER`, and `ALTER USER` do not require an extra `FLUSH PRIVILEGES`.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `FLUSH PRIVILEGES` command? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`FLUSH PRIVILEGES` tells MySQL to reload privilege information from the grant tables. It is mainly relevant when grant-table data has been changed directly, which is not the normal account-management path. Changes made through statements such as `GRANT`, `REVOKE`, `CREATE USER`, and `ALTER USER` do not require an extra `FLUSH PRIVILEGES`.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss least privilege, authentication versus authorization, TLS in transit, account scope, and how you would avoid exposing sensitive data or credentials."],

  // ==================== ADVANCED FEATURES (15) ====================
  ["MySQL", "What is JSON support in MySQL? What functions are available?", "mysql-json", "Explain JSON capabilities.", "**Direct answer :**\nMySQL supports JSON data type and many functions: `JSON_EXTRACT`, `JSON_UNQUOTE`, `JSON_KEYS`, `JSON_LENGTH`, `JSON_CONTAINS`, `JSON_ARRAY`, `JSON_OBJECT`, `JSON_TABLE` (8.0+), and generated columns on JSON fields for indexing.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is JSON support in MySQL? What functions are available? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nMySQL supports JSON data type and many functions: `JSON_EXTRACT`, `JSON_UNQUOTE`, `JSON_KEYS`, `JSON_LENGTH`, `JSON_CONTAINS`, `JSON_ARRAY`, `JSON_OBJECT`, `JSON_TABLE` (8.0+), and generated columns on JSON fields for indexing.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain when JSON is appropriate versus normalized columns, how JSON paths are queried, and how generated or functional indexing can support selective lookups."],
  ["MySQL", "How do you create an index on a JSON column?", "json-index", "Use generated columns.", "**Direct answer :**\nYou can create a generated column that extracts a value from JSON and then index that column. Example: `ALTER TABLE t ADD COLUMN v INT GENERATED ALWAYS AS (JSON_EXTRACT(jdoc, '$.id')) STORED; CREATE INDEX idx_v ON t(v);`\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you create an index on a json column? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nYou can create a generated column that extracts a value from JSON and then index that column. Example: `ALTER TABLE t ADD COLUMN v INT GENERATED ALWAYS AS (JSON_EXTRACT(jdoc, '$.id')) STORED; CREATE INDEX idx_v ON t(v);`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain index selectivity, composite-index column order, covering indexes, write overhead, and how you would verify the optimizer's choice with `EXPLAIN`."],
  ["MySQL", "What is `WITH` clause (Common Table Expression) in MySQL 8.0?", "cte", "Explain CTEs.", "**Direct answer :**\nA Common Table Expression (CTE) is a named temporary result set defined with `WITH`. It improves readability and supports recursion. Example: `WITH cte AS (SELECT ...) SELECT * FROM cte;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is `WITH` clause (Common Table Expression) in MySQL 8.0? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA Common Table Expression (CTE) is a named temporary result set defined with `WITH`. It improves readability and supports recursion. Example: `WITH cte AS (SELECT ...) SELECT * FROM cte;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is recursive CTE and how is it used?", "recursive-cte", "Explain recursive queries.", "**Direct answer :**\nRecursive CTEs allow hierarchical queries (e.g., tree traversal). Syntax: `WITH RECURSIVE cte AS (SELECT ... UNION ALL SELECT ...)`. Used for organizational charts, bill of materials.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is recursive CTE and how is it used? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nRecursive CTEs allow hierarchical queries (e.g., tree traversal). Syntax: `WITH RECURSIVE cte AS (SELECT ... UNION ALL SELECT ...)`. Used for organizational charts, bill of materials.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a window function? Provide an example.", "window-functions", "Explain and give example.", "**Direct answer :**\nWindow functions perform calculations across a set of rows related to the current row without grouping. Examples: `ROW_NUMBER()`, `RANK()`, `DENSE_RANK()`, `LEAD()`, `LAG()`. Example: `SELECT id, sales, ROW_NUMBER() OVER (ORDER BY sales DESC) AS rank FROM orders;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a window function? Provide an example. beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nWindow functions perform calculations across a set of rows related to the current row without grouping. Examples: `ROW_NUMBER()`, `RANK()`, `DENSE_RANK()`, `LEAD()`, `LAG()`. Example: `SELECT id, sales, ROW_NUMBER() OVER (ORDER BY sales DESC) AS rank FROM orders;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to compare stored procedures, functions, and application-side logic, and to explain error handling, transaction boundaries, maintainability, and performance."],
  ["MySQL", "What is the difference between `ROW_NUMBER()` and `RANK()`?", "row_number-vs-rank", "Compare the two.", "**Direct answer :**\n`ROW_NUMBER()` assigns a unique sequential number to each row in a partition. `RANK()` gives the same rank to equal values and skips subsequent ranks. `DENSE_RANK()` does not skip ranks.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `ROW_NUMBER()` and `RANK()`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`ROW_NUMBER()` assigns a unique sequential number to each row in a partition. `RANK()` gives the same rank to equal values and skips subsequent ranks. `DENSE_RANK()` does not skip ranks.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is `GROUP_CONCAT()` and when is it used?", "group-concat", "Explain string aggregation.", "**Direct answer :**\n`GROUP_CONCAT()` concatenates values from multiple rows into a single string, often used with `GROUP BY`. Example: `SELECT department, GROUP_CONCAT(name) FROM employees GROUP BY department;`\n\n**Why this is asked :**\nThis is asked to test whether you understand what is `GROUP_CONCAT()` and when is it used? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`GROUP_CONCAT()` concatenates values from multiple rows into a single string, often used with `GROUP BY`. Example: `SELECT department, GROUP_CONCAT(name) FROM employees GROUP BY department;`\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is `FULLTEXT` search and how do you use it?", "fulltext-search", "Explain natural language search.", "**Direct answer :**\nTo use full‑text search, create a `FULLTEXT` index on text columns. Query with `MATCH(columns) AGAINST('search terms' IN NATURAL LANGUAGE MODE)`. Also supports boolean mode and query expansion.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is `FULLTEXT` search and how do you use it? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nTo use full‑text search, create a `FULLTEXT` index on text columns. Query with `MATCH(columns) AGAINST('search terms' IN NATURAL LANGUAGE MODE)`. Also supports boolean mode and query expansion.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is `InnoDB`'s `memcached` plugin?", "memcached-plugin", "Explain NoSQL interface.", "**Direct answer :**\nInnoDB provides a memcached API that allows direct access to InnoDB tables via the memcached protocol, enabling in‑memory caching and persistence.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is `InnoDB`'s `memcached` plugin? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nInnoDB provides a memcached API that allows direct access to InnoDB tables via the memcached protocol, enabling in‑memory caching and persistence.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a `foreign key` and what are the actions `CASCADE`, `SET NULL`, `RESTRICT`, `NO ACTION`?", "foreign-key-actions", "Explain referential actions.", "**Direct answer :**\n`ON DELETE CASCADE` deletes child rows when parent is deleted. `SET NULL` sets foreign key to NULL. `RESTRICT` prevents deletion if child exists. `NO ACTION` is similar to RESTRICT.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `foreign key` and what are the actions `CASCADE`, `SET NULL`, `RESTRICT`, `NO ACTION`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`ON DELETE CASCADE` deletes child rows when parent is deleted. `SET NULL` sets foreign key to NULL. `RESTRICT` prevents deletion if child exists. `NO ACTION` is similar to RESTRICT.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is the difference between `CHARACTER SET` and `COLLATION`?", "charset-vs-collation", "Explain encoding and sorting rules.", "**Direct answer :**\nCharacter set defines the set of symbols and their encoding (e.g., UTF8). Collation defines how strings are compared and sorted (e.g., `utf8_general_ci` for case‑insensitive).\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the difference between `CHARACTER SET` and `COLLATION`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nCharacter set defines the set of symbols and their encoding (e.g., UTF8). Collation defines how strings are compared and sorted (e.g., `utf8_general_ci` for case‑insensitive).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is `utf8mb4` and why should you use it?", "utf8mb4", "Explain full Unicode support.", "**Direct answer :**\n`utf8mb4` is MySQL's true UTF‑8 encoding (4 bytes per character), supporting all Unicode characters including emojis. The older `utf8` only supports BMP (3 bytes).\n\n**Why this is asked :**\nThis is asked to test whether you understand what is `utf8mb4` and why should you use it? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\n`utf8mb4` is MySQL's true UTF‑8 encoding (4 bytes per character), supporting all Unicode characters including emojis. The older `utf8` only supports BMP (3 bytes).\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is partitioning in MySQL? What are the types?", "partitioning-types", "Explain table partitioning.", "**Direct answer :**\nPartitioning splits a table into smaller segments based on a key. Types: `RANGE`, `LIST`, `HASH`, `KEY`. It can improve performance and manageability. Available in InnoDB.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is partitioning in MySQL? What are the types? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nPartitioning splits a table into smaller segments based on a key. Types: `RANGE`, `LIST`, `HASH`, `KEY`. It can improve performance and manageability. Available in InnoDB.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is a `Generated Column` in MySQL?", "generated-column", "Explain virtual and stored columns.", "**Direct answer :**\nA generated column's value is computed from other columns. It can be `VIRTUAL` (computed on read) or `STORED` (physically stored). Useful for indexing JSON values or derived data.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is a `Generated Column` in MySQL? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nA generated column's value is computed from other columns. It can be `VIRTUAL` (computed on read) or `STORED` (physically stored). Useful for indexing JSON values or derived data.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
  ["MySQL", "What is `XA` transactions?", "xa-transactions", "Explain distributed transactions.", "**Direct answer :**\nXA transactions allow multiple databases to participate in a single distributed transaction, using a two‑phase commit protocol. MySQL supports XA with InnoDB.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is `XA` transactions? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nXA transactions allow multiple databases to participate in a single distributed transaction, using a two‑phase commit protocol. MySQL supports XA with InnoDB.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain what happens when two transactions access the same rows, which locks are taken, how isolation changes visibility, and how you would diagnose or retry a conflict safely."],

["MySQL", "How do you kill a running query?", "kill-query", "Use `KILL [CONNECTION] thread_id;`. Find the thread ID from `SHOW PROCESSLIST`.", "**Direct answer :**\nTo kill a running query, first identify the thread ID using `SHOW PROCESSLIST` or `SELECT * FROM information_schema.processlist`. Then execute `KILL thread_id;` or `KILL CONNECTION thread_id;` (the latter is the default). Use `KILL QUERY thread_id;` to terminate only the current query without dropping the connection. Killing a query can cause transactions to roll back, so use it carefully, especially in production.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you kill a running query? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nTo kill a running query, first identify the thread ID using `SHOW PROCESSLIST` or `SELECT * FROM information_schema.processlist`. Then execute `KILL thread_id;` or `KILL CONNECTION thread_id;` (the latter is the default). Use `KILL QUERY thread_id;` to terminate only the current query without dropping the connection. Killing a query can cause transactions to roll back, so use it carefully, especially in production.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
["MySQL", "How do you check the size of a database or table?", "table-size", "Query `information_schema.tables`. Example: `SELECT table_schema, SUM(data_length + index_length) FROM information_schema.tables GROUP BY table_schema;`", "**Direct answer :**\nTo check the size of a database or table, query the `information_schema.tables` table. For a table, use `SELECT table_name, ROUND(((data_length + index_length) / 1024 / 1024), 2) AS 'Size (MB)' FROM information_schema.tables WHERE table_schema = 'your_db' AND table_name = 'your_table';`. For all tables in a database, aggregate by `table_schema`. This gives you the storage used by data and indexes. You can also use `SHOW TABLE STATUS` for a quick overview.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you check the size of a database or table? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nTo check the size of a database or table, query the `information_schema.tables` table. For a table, use `SELECT table_name, ROUND(((data_length + index_length) / 1024 / 1024), 2) AS 'Size (MB)' FROM information_schema.tables WHERE table_schema = 'your_db' AND table_name = 'your_table';`. For all tables in a database, aggregate by `table_schema`. This gives you the storage used by data and indexes. You can also use `SHOW TABLE STATUS` for a quick overview.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
["MySQL", "How do you handle a corrupted table?", "corrupted-table", "Use `CHECK TABLE` and `REPAIR TABLE` for MyISAM; for InnoDB, restore from backup or use `innodb_force_recovery`.", "**Direct answer :**\nFor MyISAM tables, you can run `CHECK TABLE table_name;` to detect corruption, and `REPAIR TABLE table_name;` to fix it (though it may cause data loss). For InnoDB, corruption often requires restoring from a backup, as InnoDB does not support `REPAIR`. You can try setting `innodb_force_recovery` in the MySQL config to start the server and dump the data, but this is risky and should be used as a last resort. Always have a recent backup.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you handle a corrupted table? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nFor MyISAM tables, you can run `CHECK TABLE table_name;` to detect corruption, and `REPAIR TABLE table_name;` to fix it (though it may cause data loss). For InnoDB, corruption often requires restoring from a backup, as InnoDB does not support `REPAIR`. You can try setting `innodb_force_recovery` in the MySQL config to start the server and dump the data, but this is risky and should be used as a last resort. Always have a recent backup.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
["MySQL", "How do you monitor replication lag?", "replication-lag", "Check `Seconds_Behind_Master` from `SHOW SLAVE STATUS\\G`.", "**Direct answer :**\nFor classic asynchronous replication, `SHOW REPLICA STATUS\\G` is the current terminology and reports fields such as `Seconds_Behind_Source`, along with I/O and SQL/applier thread state. That value is only an estimate and can be NULL or misleading in some situations. Production monitoring should also inspect replica execution errors, transaction/apply state, and workload-specific replication metrics.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you monitor replication lag? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nFor classic asynchronous replication, `SHOW REPLICA STATUS\\G` is the current terminology and reports fields such as `Seconds_Behind_Source`, along with I/O and SQL/applier thread state. That value is only an estimate and can be NULL or misleading in some situations. Production monitoring should also inspect replica execution errors, transaction/apply state, and workload-specific replication metrics.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to explain source/replica roles, asynchronous behavior, lag causes, failover considerations, and which status or performance metrics you would monitor."],
["MySQL", "How do you import a large SQL file efficiently?", "import-large-file", "Use `mysql` with options `--max_allowed_packet` and `--net_buffer_length`. Also, consider using `SOURCE` command within mysql or splitting the file.", "**Direct answer :**\nFor a large SQL dump, use the `mysql` client and ensure the client/server packet and timeout settings are appropriate for the dump. Avoid blindly setting very large values; diagnose actual packet or timeout errors. For very large datasets, use a backup/import method designed for the dataset size and monitor disk space, redo/undo pressure, transaction size, and replication impact.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you import a large sql file efficiently? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nFor a large SQL dump, use the `mysql` client and ensure the client/server packet and timeout settings are appropriate for the dump. Avoid blindly setting very large values; diagnose actual packet or timeout errors. For very large datasets, use a backup/import method designed for the dataset size and monitor disk space, redo/undo pressure, transaction size, and replication impact.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
["MySQL", "What are the privileges needed to use `LOAD DATA INFILE`?", "load-data-privileges", "Requires `FILE` privilege or `SUPER` for local files.", "**Direct answer :**\nFor `LOAD DATA INFILE`, the `FILE` privilege is required because the server reads a file from the server host, and `secure_file_priv` can restrict permitted directories. `LOAD DATA LOCAL INFILE` reads from the client host and is governed by the `local_infile` client/server settings; it does not require FILE privilege merely to read the client-side file.\n\n**Why this is asked :**\nThis is asked to test whether you understand what are the privileges needed to use `LOAD DATA INFILE`? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nFor `LOAD DATA INFILE`, the `FILE` privilege is required because the server reads a file from the server host, and `secure_file_priv` can restrict permitted directories. `LOAD DATA LOCAL INFILE` reads from the client host and is governed by the `local_infile` client/server settings; it does not require FILE privilege merely to read the client-side file.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready to discuss least privilege, authentication versus authorization, TLS in transit, account scope, and how you would avoid exposing sensitive data or credentials."],
["MySQL", "How do you generate a CSV from a query?", "select-into-outfile", "Use `SELECT ... INTO OUTFILE '/path/file.csv' FIELDS TERMINATED BY ',' ENCLOSED BY '\"' LINES TERMINATED BY '\\n' FROM table;`", "**Direct answer :**\nTo export query results as a CSV, use `SELECT ... INTO OUTFILE '/path/to/file.csv' FIELDS TERMINATED BY ',' ENCLOSED BY '\"' LINES TERMINATED BY '\\n' FROM your_table;`. Ensure the directory is writable by the MySQL server and the `--secure-file-priv` variable allows it. You can also use `mysqldump` with `--tab` or use `mysql -e \"SELECT ...\" > output.csv` with the appropriate options. Remember to escape special characters if needed.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you generate a csv from a query? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nTo export query results as a CSV, use `SELECT ... INTO OUTFILE '/path/to/file.csv' FIELDS TERMINATED BY ',' ENCLOSED BY '\"' LINES TERMINATED BY '\\n' FROM your_table;`. Ensure the directory is writable by the MySQL server and the `--secure-file-priv` variable allows it. You can also use `mysqldump` with `--tab` or use `mysql -e \"SELECT ...\" > output.csv` with the appropriate options. Remember to escape special characters if needed.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
["MySQL", "How do you change the storage engine of a table?", "alter-engine", "Use `ALTER TABLE table_name ENGINE=InnoDB;`", "**Direct answer :**\nTo change the storage engine, run `ALTER TABLE table_name ENGINE=InnoDB;` (or any other supported engine). This will rebuild the table, which can take time and lock the table (depending on the engine). For InnoDB, you can use `ALTER TABLE ... ENGINE=InnoDB;` to convert from MyISAM. Be aware that not all features are supported across engines (e.g., foreign keys only in InnoDB). Always test the conversion on a staging environment first.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you change the storage engine of a table? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nTo change the storage engine, run `ALTER TABLE table_name ENGINE=InnoDB;` (or any other supported engine). This will rebuild the table, which can take time and lock the table (depending on the engine). For InnoDB, you can use `ALTER TABLE ... ENGINE=InnoDB;` to convert from MyISAM. Be aware that not all features are supported across engines (e.g., foreign keys only in InnoDB). Always test the conversion on a staging environment first.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
["MySQL", "How do you check the MySQL server version?", "mysql-version", "`SELECT VERSION();` or `mysql --version`.", "**Direct answer :**\nYou can check the MySQL server version by running `SELECT VERSION();` in a SQL client, or from the command line with `mysql --version` (which shows the client version) or `mysqld --version` (server version). Also, `SHOW VARIABLES LIKE 'version';` provides the same. Knowing the version is important for compatibility and feature availability.\n\n**Why this is asked :**\nThis is asked to test whether you understand how do you check the mysql server version? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nYou can check the MySQL server version by running `SELECT VERSION();` in a SQL client, or from the command line with `mysql --version` (which shows the client version) or `mysqld --version` (server version). Also, `SHOW VARIABLES LIKE 'version';` provides the same. Knowing the version is important for compatibility and feature availability.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."],
["MySQL", "What is the `INFORMATION_SCHEMA.KEY_COLUMN_USAGE` view used for?", "key-column-usage", "It provides information about key columns, including foreign keys and constraints.", "**Direct answer :**\nThe `INFORMATION_SCHEMA.KEY_COLUMN_USAGE` view shows which columns are part of constraints (like primary keys, unique keys, and foreign keys). It includes `CONSTRAINT_NAME`, `TABLE_NAME`, `COLUMN_NAME`, `REFERENCED_TABLE_NAME`, `REFERENCED_COLUMN_NAME`, etc. This is useful for exploring database relationships, documenting foreign key dependencies, and debugging constraint issues. For example, you can query it to see all foreign keys referencing a specific table.\n\n**Why this is asked :**\nThis is asked to test whether you understand what is the `INFORMATION_SCHEMA.KEY_COLUMN_USAGE` view used for? beyond memorizing syntax. A strong MySQL answer should connect the concept to correctness, concurrency, performance, maintainability, or operational safety where relevant.\n\n**Breaking it down :**\nThe `INFORMATION_SCHEMA.KEY_COLUMN_USAGE` view shows which columns are part of constraints (like primary keys, unique keys, and foreign keys). It includes `CONSTRAINT_NAME`, `TABLE_NAME`, `COLUMN_NAME`, `REFERENCED_TABLE_NAME`, `REFERENCED_COLUMN_NAME`, etc. This is useful for exploring database relationships, documenting foreign key dependencies, and debugging constraint issues. For example, you can query it to see all foreign keys referencing a specific table.\nThe important interview distinction is to explain not only what the feature does, but when you would use it and what trade-offs or limitations you would consider. For SQL examples, pay attention to NULL behavior, transaction boundaries, indexes, storage-engine behavior, and the actual execution plan rather than assuming a statement is always fast or safe.\n\n**How to structure your spoken answer :**\nStart with a one-sentence definition or decision. Then give the key mechanics in two or three points, followed by a small SQL example or practical scenario when it helps. Finish by stating the main limitation or trade-off. For example, for a production issue, explain what you would measure or verify before changing configuration or schema.\n\n**Follow-up angle to anticipate :**\nBe ready for a follow-up asking you to compare this with a closely related MySQL feature, explain its behavior under concurrency, or show a practical SQL example. If performance is involved, expect questions about indexes and `EXPLAIN`; if correctness is involved, expect questions about transactions, constraints, locking, and failure handling."]
] as const;

const prisma = new PrismaClient();

const buildWhyInterviewersAsk = (question: string, shortDescription: string) =>
  `Interviewers ask this to check whether you can explain MySQL concepts clearly and connect them to practical engineering decisions. ${shortDescription} A strong response should address the purpose, the relevant trade‑offs, and how you would verify the result rather than reciting a command or definition.`;

const buildCommonMistakes = (question: string) => [
  `Giving a memorized definition without explaining how it applies to: ${question}`,
  "Listing MySQL commands without explaining the safety, performance, or operational trade‑off.",
  "Ignoring security boundaries, persistence, failure handling, or how the solution would be tested.",
];

async function main() {
  const category = await prisma.category.upsert({
    where: { slug: "mysql" },
    update: { name: "MySQL", group: "Technology", description: "MySQL interview questions." },
    create: { name: "MySQL", slug: "mysql", group: "Technology", description: "MySQL interview questions." },
  });
  const subcategory = await prisma.subcategory.upsert({
    where: { categoryId_slug: { categoryId: category.id, slug: "mysql" } },
    update: {},
    create: { name: "MySQL", slug: "mysql", categoryId: category.id },
  });

  for (let topicIndex = 0; topicIndex < topics.length; topicIndex += 1) {
    const [, question, slug, shortDescription, sampleAnswer] = topics[topicIndex];
    const commonMistakes = buildCommonMistakes(question);
    const followUpQuestions = [
      topics[(topicIndex + 1) % topics.length][1],
      topics[(topicIndex + 2) % topics.length][1],
      topics[(topicIndex + 3) % topics.length][1],
    ];
    await prisma.interviewQuestion.upsert({
      where: { slug },
      update: {
        question,
        categoryId: category.id,
        subcategoryId: subcategory.id,
        experienceLevel: ExperienceLevel.MID_LEVEL,
        difficulty: Difficulty.MEDIUM,
        interviewType: InterviewType.TECHNICAL,
        shortDescription,
        explanation: buildWhyInterviewersAsk(question, shortDescription),
        sampleAnswer,
        detailedAnswer: sampleAnswer,
        keyPoints: [],
        commonMistakes,
        followUpQuestions,
        tags: ["MySQL"],
        isPublished: true,
      },
      create: {
        question,
        slug,
        categoryId: category.id,
        subcategoryId: subcategory.id,
        experienceLevel: ExperienceLevel.MID_LEVEL,
        difficulty: Difficulty.MEDIUM,
        interviewType: InterviewType.TECHNICAL,
        shortDescription,
        explanation: buildWhyInterviewersAsk(question, shortDescription),
        sampleAnswer,
        detailedAnswer: sampleAnswer,
        keyPoints: [],
        commonMistakes,
        followUpQuestions,
        tags: ["MySQL"],
        isPublished: true,
      },
    });
  }

  console.log(`Imported ${topics.length} MySQL questions.`);
}

main()
  .catch((error) => {
    console.error(error);
    process.exitCode = 1;
  })
  .finally(() => prisma.$disconnect());

export const slugify = (value: string) => value.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
