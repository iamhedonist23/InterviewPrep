// ---- 200+ React Interview Questions (Fresher to Advanced) ----
import { Difficulty, ExperienceLevel, InterviewType, PrismaClient } from "@prisma/client";

// ---- Categories ----
export const categories = [
  ["React", "React"]
] as const;

// ---- Topics ----
export const topics = [
  // ==================== BASICS (20) ====================
  ["React", "What is React and what are its main features?", "react-overview", "Define React and list its key features.", "**Direct answer :** React is a JavaScript library for building user interfaces from reusable components. Modern React is declarative and component-based, supports Hooks, and can render to different environments. JSX is optional syntax that makes component markup convenient; the reconciliation process helps React update the UI efficiently. Server rendering and static generation are typically provided by React-based frameworks rather than by React alone.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** React is a JavaScript library for building user interfaces from reusable components. Modern React is declarative and component-based, supports Hooks, and can render to different environments. JSX is optional syntax that makes component markup convenient; the reconciliation process helps React update the UI efficiently. Server rendering and static generation are typically provided by React-based frameworks rather than by React alone.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking how React rendering and reconciliation differ from directly manipulating the DOM."],
  ["React", "What is the difference between React and React Native?", "react-vs-react-native", "Compare the two frameworks.", "**Direct answer :** React targets user interfaces for the web and normally renders to the browser DOM. React Native uses React concepts and JSX but renders to native platform views rather than HTML DOM elements. They share concepts such as components, props, state, and Hooks, but their platform APIs, styling systems, and rendering targets differ.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** React targets user interfaces for the web and normally renders to the browser DOM. React Native uses React concepts and JSX but renders to native platform views rather than HTML DOM elements. They share concepts such as components, props, state, and Hooks, but their platform APIs, styling systems, and rendering targets differ.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is JSX?", "jsx-definition", "Define JSX and its purpose.", "**Direct answer :** JSX is a JavaScript syntax extension for describing UI in a markup-like form. It is transformed by a compiler into JavaScript that creates React elements; the exact generated code depends on the configured JSX transform, so it is more precise to say JSX is transformed rather than always saying it becomes `React.createElement()` calls.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** JSX is a JavaScript syntax extension for describing UI in a markup-like form. It is transformed by a compiler into JavaScript that creates React elements; the exact generated code depends on the configured JSX transform, so it is more precise to say JSX is transformed rather than always saying it becomes `React.createElement()` calls.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking whether JSX is required and what the compiler does with it."],
  ["React", "What is the difference between a functional component and a class component?", "functional-vs-class", "Compare the two component types.", "**Direct answer :** A function component is a JavaScript function that returns React UI and can use Hooks. A class component extends `React.Component` and uses class lifecycle APIs and instance state. Modern React development generally favors function components and Hooks, but class components remain relevant when maintaining older code, including traditional Error Boundaries.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** A function component is a JavaScript function that returns React UI and can use Hooks. A class component extends `React.Component` and uses class lifecycle APIs and instance state. Modern React development generally favors function components and Hooks, but class components remain relevant when maintaining older code, including traditional Error Boundaries.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the virtual DOM and how does it work?", "virtual-dom", "Explain the concept.", "**Direct answer :** The virtual DOM is a useful mental model for React's in-memory representation of UI. When inputs such as state or props change, React renders a new element tree, reconciles it with the previous tree, and commits the necessary DOM changes. React's goal is not simply to make every DOM update minimal; it also uses scheduling and rendering techniques to keep applications responsive.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** The virtual DOM is a useful mental model for React's in-memory representation of UI. When inputs such as state or props change, React renders a new element tree, reconciles it with the previous tree, and commits the necessary DOM changes. React's goal is not simply to make every DOM update minimal; it also uses scheduling and rendering techniques to keep applications responsive.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking whether the virtual DOM is always faster than direct DOM manipulation."],
  ["React", "What is the difference between props and state?", "props-vs-state", "Compare the two.", "**Direct answer :** Props are inputs passed into a component by its parent, while state is component-managed data that can change over time. Neither should be mutated directly. Updating state schedules a new render, and new props can cause a component to render again. Props can effectively change when the parent renders with different values, so they are not permanently read-only across the lifetime of a component.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** Props are inputs passed into a component by its parent, while state is component-managed data that can change over time. Neither should be mutated directly. Updating state schedules a new render, and new props can cause a component to render again. Props can effectively change when the parent renders with different values, so they are not permanently read-only across the lifetime of a component.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking where state should live and why direct mutation is a problem."],
  ["React", "What is the `key` prop and why is it important?", "key-prop", "Explain its role in lists.", "**Direct answer :** A `key` gives React a stable identity for an item among its siblings during reconciliation. A good key comes from stable data such as a database ID. Array indexes can be acceptable for truly static lists whose order never changes, but they are risky when items are inserted, removed, or reordered because component state can become associated with the wrong item.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** A `key` gives React a stable identity for an item among its siblings during reconciliation. A good key comes from stable data such as a database ID. Array indexes can be acceptable for truly static lists whose order never changes, but they are risky when items are inserted, removed, or reordered because component state can become associated with the wrong item.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking why array indexes can break component state when a list is reordered."],
  ["React", "How do you handle events in React?", "event-handling", "Explain synthetic events.", "**Direct answer :** React event handlers are functions passed to props such as `onClick` and `onChange`. React provides a consistent event interface, and modern React no longer uses the old pooled SyntheticEvent behavior. In function components, handlers normally do not require `this` binding; class components may need binding or class-field arrow functions.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** React event handlers are functions passed to props such as `onClick` and `onChange`. React provides a consistent event interface, and modern React no longer uses the old pooled SyntheticEvent behavior. In function components, handlers normally do not require `this` binding; class components may need binding or class-field arrow functions.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `children` prop?", "children-prop", "Explain the special prop.", "**Direct answer :** `children` is the prop React uses for content nested between a component's opening and closing tags. It can be a single element, text, a number, an array, or other renderable React content. Components use `children` to implement composition, for example a `Card` that decides the layout while the caller supplies the content.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `children` is the prop React uses for content nested between a component's opening and closing tags. It can be a single element, text, a number, an array, or other renderable React content. Components use `children` to implement composition, for example a `Card` that decides the layout while the caller supplies the content.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between a controlled and uncontrolled component?", "controlled-vs-uncontrolled", "Compare form handling.", "**Direct answer :** A controlled form element gets its current value from React state and reports changes through an event handler, making React the source of truth. An uncontrolled element keeps its current value in the DOM and is usually accessed with a ref, often using `defaultValue` or `defaultChecked`. Controlled inputs are useful when the UI needs validation, conditional behavior, or synchronized state; uncontrolled inputs can be simpler for some forms.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** A controlled form element gets its current value from React state and reports changes through an event handler, making React the source of truth. An uncontrolled element keeps its current value in the DOM and is usually accessed with a ref, often using `defaultValue` or `defaultChecked`. Controlled inputs are useful when the UI needs validation, conditional behavior, or synchronized state; uncontrolled inputs can be simpler for some forms.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you create a ref in React?", "ref-creation", "Explain `useRef` and `createRef`.", "**Direct answer :** For a function component, `useRef(initialValue)` creates a ref object whose `.current` value persists across renders. For class components or code using the older API, `createRef()` creates a ref object. A ref can point to a DOM node or hold a mutable value that should not trigger rendering. In React 19, `useRef` requires an argument.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** For a function component, `useRef(initialValue)` creates a ref object whose `.current` value persists across renders. For class components or code using the older API, `createRef()` creates a ref object. A ref can point to a DOM node or hold a mutable value that should not trigger rendering. In React 19, `useRef` requires an argument.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `useState` hook?", "usestate", "Explain the hook.", "**Direct answer :** `useState` adds a state variable to a function component and returns `[state, setState]`. Calling the setter schedules an update; it does not mutate the current render's state value. When the next state depends on the previous state, use the functional form such as `setCount(c => c + 1)`. Objects and arrays in state should be replaced rather than mutated.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `useState` adds a state variable to a function component and returns `[state, setState]`. Calling the setter schedules an update; it does not mutate the current render's state value. When the next state depends on the previous state, use the functional form such as `setCount(c => c + 1)`. Objects and arrays in state should be replaced rather than mutated. A strong implementation should also account for dependency behavior, component ownership, cleanup where applicable, and the effect on rendering rather than treating the API as an isolated feature.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `useEffect` hook?", "useeffect", "Explain the side‑effect hook.", "**Direct answer :** `useEffect` lets a component synchronize with an external system such as a network connection, subscription, timer, browser API, or third-party widget. React runs the setup after a commit when its dependencies require it, and an optional cleanup runs before the effect is re-run and when the component is removed. Effects are not a general replacement for derived values or ordinary application data flow.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `useEffect` lets a component synchronize with an external system such as a network connection, subscription, timer, browser API, or third-party widget. React runs the setup after a commit when its dependencies require it, and an optional cleanup runs before the effect is re-run and when the component is removed. Effects are not a general replacement for derived values or ordinary application data flow.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking about dependency arrays, cleanup, and why an Effect may run more than once in development."],
  ["React", "What is the `useContext` hook?", "usecontext", "Explain the context hook.", "**Direct answer :** `useContext` reads and subscribes a component to a context value. The context is created with `createContext` and normally supplied by a matching provider higher in the tree. When the provider's value changes, consumers using that context can re-render. Context is useful for values such as theme or locale, but it does not automatically solve complex state-management or performance concerns.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `useContext` reads and subscribes a component to a context value. The context is created with `createContext` and normally supplied by a matching provider higher in the tree. When the provider's value changes, consumers using that context can re-render. Context is useful for values such as theme or locale, but it does not automatically solve complex state-management or performance concerns. A strong implementation should also account for dependency behavior, component ownership, cleanup where applicable, and the effect on rendering rather than treating the API as an isolated feature.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `useReducer` hook?", "usereducer", "Explain the reducer hook.", "**Direct answer :** `useReducer` manages state through a reducer function: `(state, action) => newState`. The hook returns the current state and a `dispatch` function. It is useful when state transitions are numerous, related, or easier to describe as explicit actions. The reducer should be pure, and side effects such as network requests generally belong outside the reducer.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `useReducer` manages state through a reducer function: `(state, action) => newState`. The hook returns the current state and a `dispatch` function. It is useful when state transitions are numerous, related, or easier to describe as explicit actions. The reducer should be pure, and side effects such as network requests generally belong outside the reducer. A strong implementation should also account for dependency behavior, component ownership, cleanup where applicable, and the effect on rendering rather than treating the API as an isolated feature.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `useCallback` hook?", "usecallback", "Explain memoization of functions.", "**Direct answer :** `useCallback` caches a function definition between renders until its dependencies change. It is mainly useful when function identity matters, for example when passing a callback to a memoized child or using the function as a Hook dependency. It does not make the function itself faster, and adding it everywhere can make code harder to understand.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `useCallback` caches a function definition between renders until its dependencies change. It is mainly useful when function identity matters, for example when passing a callback to a memoized child or using the function as a Hook dependency. It does not make the function itself faster, and adding it everywhere can make code harder to understand.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking whether `useCallback` prevents a child from rendering in every case."],
  ["React", "What is the `useMemo` hook?", "usememo", "Explain memoization of values.", "**Direct answer :** `useMemo` caches the result of a calculation between renders until its dependencies change. It is a performance optimization, not a correctness mechanism. It is most useful for genuinely expensive calculations or when stable object/value identity helps another optimization. Modern React also has compiler-based automatic memoization options, so manual `useMemo` should be applied based on measured need rather than as a default rule.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `useMemo` caches the result of a calculation between renders until its dependencies change. It is a performance optimization, not a correctness mechanism. It is most useful for genuinely expensive calculations or when stable object/value identity helps another optimization. Modern React also has compiler-based automatic memoization options, so manual `useMemo` should be applied based on measured need rather than as a default rule.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking when memoization is actually worth its complexity."],
  ["React", "What is the `useRef` hook?", "useref", "Explain the ref hook.", "**Direct answer :** `useRef(initialValue)` returns a stable object whose `.current` value persists across renders. Changing `.current` does not trigger a render. Refs are commonly used for DOM nodes, timers, previous values, or integration with imperative browser APIs. In React 19, `useRef` requires an argument, and refs are intended as an escape hatch rather than a replacement for state.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `useRef(initialValue)` returns a stable object whose `.current` value persists across renders. Changing `.current` does not trigger a render. Refs are commonly used for DOM nodes, timers, previous values, or integration with imperative browser APIs. In React 19, `useRef` requires an argument, and refs are intended as an escape hatch rather than a replacement for state. A strong implementation should also account for dependency behavior, component ownership, cleanup where applicable, and the effect on rendering rather than treating the API as an isolated feature.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is a custom hook?", "custom-hook", "Define custom hooks.", "**Direct answer :** A custom Hook is a JavaScript function whose name conventionally starts with `use` and that can call other Hooks. It extracts reusable stateful logic, not UI itself. For example, a custom Hook can encapsulate subscription, fetching, or form behavior and return the state and actions a component needs. Each component calling the Hook gets its own Hook state unless the Hook connects to shared external state.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** A custom Hook is a JavaScript function whose name conventionally starts with `use` and that can call other Hooks. It extracts reusable stateful logic, not UI itself. For example, a custom Hook can encapsulate subscription, fetching, or form behavior and return the state and actions a component needs. Each component calling the Hook gets its own Hook state unless the Hook connects to shared external state. A strong implementation should also account for dependency behavior, component ownership, cleanup where applicable, and the effect on rendering rather than treating the API as an isolated feature.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What are the rules of hooks?", "hooks-rules", "List the rules.", "**Direct answer :** Hooks must be called at the top level of a React function component or custom Hook. Do not call them inside conditions, loops, nested functions, event handlers, or after an early return. React relies on the consistent order of Hook calls across renders to associate Hook state with the correct call.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** Hooks must be called at the top level of a React function component or custom Hook. Do not call them inside conditions, loops, nested functions, event handlers, or after an early return. React relies on the consistent order of Hook calls across renders to associate Hook state with the correct call. A strong implementation should also account for dependency behavior, component ownership, cleanup where applicable, and the effect on rendering rather than treating the API as an isolated feature.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],

  // ==================== STATE MANAGEMENT (15) ====================
  ["React", "What is the Context API and when should you use it?", "context-api", "Explain context.", "**Direct answer :** Context lets a component receive a value from a distant ancestor without manually threading props through every intermediate component. It is a good fit for cross-cutting values such as theme, locale, or an authenticated-user object. Context is not inherently a global state-management solution; frequent changes can cause many consumers to update, so state should be structured and providers split when appropriate.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** Context lets a component receive a value from a distant ancestor without manually threading props through every intermediate component. It is a good fit for cross-cutting values such as theme, locale, or an authenticated-user object. Context is not inherently a global state-management solution; frequent changes can cause many consumers to update, so state should be structured and providers split when appropriate.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is Redux and how does it work with React?", "redux", "Explain Redux integration.", "**Direct answer :** Redux is a predictable state-management library centered on a store, actions, and reducers. In a React application, React-Redux provides hooks such as `useSelector` and `useDispatch`. A dispatch describes an event or intended state change, middleware can handle cross-cutting or asynchronous behavior, and reducers calculate the next state without mutating the existing state.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** Redux is a predictable state-management library centered on a store, actions, and reducers. In a React application, React-Redux provides hooks such as `useSelector` and `useDispatch`. A dispatch describes an event or intended state change, middleware can handle cross-cutting or asynchronous behavior, and reducers calculate the next state without mutating the existing state.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between Redux and Context API?", "redux-vs-context", "Compare the two.", "**Direct answer :** Context and Redux solve different problems. Context is a React mechanism for making a value available to descendants, while Redux is a state-management architecture and library with centralized state, reducers, middleware, and developer tooling. Context can be enough for simple shared values; Redux can be useful when an application needs explicit state transitions, complex coordination, or rich debugging. Neither is automatically the correct choice for every large or small app.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** Context and Redux solve different problems. Context is a React mechanism for making a value available to descendants, while Redux is a state-management architecture and library with centralized state, reducers, middleware, and developer tooling. Context can be enough for simple shared values; Redux can be useful when an application needs explicit state transitions, complex coordination, or rich debugging. Neither is automatically the correct choice for every large or small app.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is a reducer in Redux?", "reducer-definition", "Define a reducer.", "**Direct answer :** A Redux reducer is a pure function that receives the previous state and an action and returns the next state. It should not perform network calls, mutate the existing state, or depend on unpredictable side effects. With Redux Toolkit, reducers can use Immer-based syntax that looks mutating while producing immutable state updates underneath.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** A Redux reducer is a pure function that receives the previous state and an action and returns the next state. It should not perform network calls, mutate the existing state, or depend on unpredictable side effects. With Redux Toolkit, reducers can use Immer-based syntax that looks mutating while producing immutable state updates underneath.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What are actions in Redux?", "actions-definition", "Define actions.", "**Direct answer :** A Redux action describes something that happened or a requested state transition. A traditional action is a plain object with a `type` field and may contain additional data such as a payload. Actions are dispatched to the store, pass through middleware, and are then handled by reducers. Redux Toolkit's `createSlice` can generate action creators and action types for you.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** A Redux action describes something that happened or a requested state transition. A traditional action is a plain object with a `type` field and may contain additional data such as a payload. Actions are dispatched to the store, pass through middleware, and are then handled by reducers. Redux Toolkit's `createSlice` can generate action creators and action types for you.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is a store in Redux?", "store-definition", "Define the store.", "**Direct answer :** A Redux store holds the application's Redux state and coordinates dispatching actions and notifying subscribers. Modern Redux applications normally create the store with Redux Toolkit's `configureStore`, rather than the older `createStore` API. Components usually interact with the store through React-Redux hooks instead of calling store methods directly.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** A Redux store holds the application's Redux state and coordinates dispatching actions and notifying subscribers. Modern Redux applications normally create the store with Redux Toolkit's `configureStore`, rather than the older `createStore` API. Components usually interact with the store through React-Redux hooks instead of calling store methods directly.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is middleware in Redux? Give examples.", "redux-middleware", "Explain middleware.", "**Direct answer :** Redux middleware sits in the dispatch pipeline between the caller and the reducer. It can inspect or transform actions, dispatch additional actions, perform logging, or coordinate asynchronous workflows. Middleware should be used for cross-cutting dispatch behavior and side effects rather than putting side effects inside reducers.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** Redux middleware sits in the dispatch pipeline between the caller and the reducer. It can inspect or transform actions, dispatch additional actions, perform logging, or coordinate asynchronous workflows. Middleware should be used for cross-cutting dispatch behavior and side effects rather than putting side effects inside reducers.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is Redux Thunk?", "redux-thunk", "Explain the middleware.", "**Direct answer :** Redux Thunk allows dispatching a function, often called a thunk, instead of only a plain action object. The thunk can receive `dispatch` and `getState` and perform asynchronous work before dispatching normal actions. Redux Toolkit includes thunk middleware by default in its standard configuration, making this pattern available without separately installing `redux-thunk` in typical setups.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** Redux Thunk allows dispatching a function, often called a thunk, instead of only a plain action object. The thunk can receive `dispatch` and `getState` and perform asynchronous work before dispatching normal actions. Redux Toolkit includes thunk middleware by default in its standard configuration, making this pattern available without separately installing `redux-thunk` in typical setups. A strong implementation should also account for dependency behavior, component ownership, cleanup where applicable, and the effect on rendering rather than treating the API as an isolated feature.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is Zustand and how does it differ from Redux?", "zustand", "Explain the state library.", "**Direct answer :** Zustand is a lightweight state-management library that exposes store state through hooks and does not require a React Provider for its normal store usage. Components can subscribe to selected slices of state, which can reduce unnecessary updates. Compared with Redux, it generally has less ceremony, while Redux Toolkit provides a more structured architecture and a larger established ecosystem for some teams.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** Zustand is a lightweight state-management library that exposes store state through hooks and does not require a React Provider for its normal store usage. Components can subscribe to selected slices of state, which can reduce unnecessary updates. Compared with Redux, it generally has less ceremony, while Redux Toolkit provides a more structured architecture and a larger established ecosystem for some teams.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is a selector in Redux?", "selector-definition", "Define selectors.", "**Direct answer :** A selector is a function that reads or derives a specific piece of application state. In Redux, selectors help components depend on the data they need instead of knowing the entire state shape. Memoized selectors, for example with Reselect or Redux Toolkit's selector utilities, can avoid repeating expensive derived calculations when their inputs have not changed.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** A selector is a function that reads or derives a specific piece of application state. In Redux, selectors help components depend on the data they need instead of knowing the entire state shape. Memoized selectors, for example with Reselect or Redux Toolkit's selector utilities, can avoid repeating expensive derived calculations when their inputs have not changed. A strong implementation should also account for dependency behavior, component ownership, cleanup where applicable, and the effect on rendering rather than treating the API as an isolated feature.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between `useSelector` and `useStore`?", "useselector-vs-usestore", "Compare the hooks.", "**Direct answer :** `useSelector` is a React-Redux hook that reads a selected value from the Redux store and subscribes the component to changes in that selected result. `useStore` is commonly associated with libraries such as Zustand; it is not the standard React-Redux hook for reading the Redux store. If the question means React-Redux, use `useSelector` for component subscriptions and `useDispatch` for dispatching.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** `useSelector` is a React-Redux hook that reads a selected value from the Redux store and subscribes the component to changes in that selected result. `useStore` is commonly associated with libraries such as Zustand; it is not the standard React-Redux hook for reading the Redux store. If the question means React-Redux, use `useSelector` for component subscriptions and `useDispatch` for dispatching.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `Provider` component in Redux?", "provider-definition", "Explain the provider.", "**Direct answer :** React-Redux's `<Provider>` makes a Redux store available to descendant components through React context. Components using `useSelector`, `useDispatch`, or related React-Redux APIs can then access that store. It is normally placed near the application's root and should receive the store instance as its `store` prop.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** React-Redux's `<Provider>` makes a Redux store available to descendant components through React context. Components using `useSelector`, `useDispatch`, or related React-Redux APIs can then access that store. It is normally placed near the application's root and should receive the store instance as its `store` prop.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What are the benefits of using a state management library?", "state-management-benefits", "List benefits.", "**Direct answer :** A state-management library can provide explicit state ownership, predictable transitions, reusable patterns, and tooling for debugging complex application state. The main benefit is not simply centralizing everything; good state management keeps state close to where it is needed and provides clear boundaries for shared state. Choosing a library should depend on the application's complexity rather than on React requiring one.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** A state-management library can provide explicit state ownership, predictable transitions, reusable patterns, and tooling for debugging complex application state. The main benefit is not simply centralizing everything; good state management keeps state close to where it is needed and provides clear boundaries for shared state. Choosing a library should depend on the application's complexity rather than on React requiring one.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between `localStorage` and `Redux` for state persistence?", "localstorage-vs-redux", "Compare persistence options.", "**Direct answer :** Redux and `localStorage` solve different problems. Redux manages in-memory application state and updates components through subscriptions, while `localStorage` is a browser persistence API containing string key/value data. You can persist selected Redux state to storage, but sensitive credentials generally should not be placed in `localStorage` merely for convenience because JavaScript-accessible storage is exposed to XSS risks.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** Redux and `localStorage` solve different problems. Redux manages in-memory application state and updates components through subscriptions, while `localStorage` is a browser persistence API containing string key/value data. You can persist selected Redux state to storage, but sensitive credentials generally should not be placed in `localStorage` merely for convenience because JavaScript-accessible storage is exposed to XSS risks.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is `Recoil` and how does it compare to Redux?", "recoil", "Explain Recoil.", "**Direct answer :** Recoil is a state-management library built around atoms and selectors, designed for fine-grained subscriptions in React applications. However, it is not a core React API, and teams should evaluate its current maintenance and ecosystem status before adopting it. The important interview distinction is the state model: independent pieces of state can be composed through derived selectors rather than requiring one monolithic store.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** Recoil is a state-management library built around atoms and selectors, designed for fine-grained subscriptions in React applications. However, it is not a core React API, and teams should evaluate its current maintenance and ecosystem status before adopting it. The important interview distinction is the state model: independent pieces of state can be composed through derived selectors rather than requiring one monolithic store.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],

  // ==================== PERFORMANCE (15) ====================
  ["React", "How does React optimize performance?", "react-performance-optimization", "Explain optimization techniques.", "**Direct answer :** React performance optimization starts with measuring where rendering or computation is expensive. Useful techniques include keeping state local, avoiding unnecessary Effects, splitting large components, using stable keys, code-splitting, and applying `memo`, `useMemo`, or `useCallback` where they address a measured problem. React's rendering model is not itself a guarantee that every update is minimal, so profiling should guide optimization.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** React performance optimization starts with measuring where rendering or computation is expensive. Useful techniques include keeping state local, avoiding unnecessary Effects, splitting large components, using stable keys, code-splitting, and applying `memo`, `useMemo`, or `useCallback` where they address a measured problem. React's rendering model is not itself a guarantee that every update is minimal, so profiling should guide optimization.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is `React.memo` and when should you use it?", "react-memo", "Explain the higher‑order component.", "**Direct answer :** `memo` can skip re-rendering a component when its props are unchanged according to its comparison. It does not prevent re-renders caused by the component's own state or by context it consumes. It is most useful when a component is expensive to render and often receives the same props; it is not automatically beneficial for every component.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** `memo` can skip re-rendering a component when its props are unchanged according to its comparison. It does not prevent re-renders caused by the component's own state or by context it consumes. It is most useful when a component is expensive to render and often receives the same props; it is not automatically beneficial for every component.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between `React.memo` and `useMemo`?", "memo-vs-usememo", "Compare the two.", "**Direct answer :** `memo` memoizes a component's rendering based on its props, while `useMemo` caches the result of a calculation inside a component. They can be used together: `useMemo` can stabilize a derived value passed to a memoized child. Both are performance optimizations and should not be required for correctness.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** `memo` memoizes a component's rendering based on its props, while `useMemo` caches the result of a calculation inside a component. They can be used together: `useMemo` can stabilize a derived value passed to a memoized child. Both are performance optimizations and should not be required for correctness.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the reconciliation algorithm?", "reconciliation", "Explain the diffing process.", "**Direct answer :** Reconciliation is React's process of comparing the newly rendered element tree with the previous one to determine what should be committed. Element type and keys provide important identity information, especially in lists. React can then preserve component state where identity is preserved and replace or update parts of the tree when identity changes.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** Reconciliation is React's process of comparing the newly rendered element tree with the previous one to determine what should be committed. Element type and keys provide important identity information, especially in lists. React can then preserve component state where identity is preserved and replace or update parts of the tree when identity changes.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is a `key` and how does it affect reconciliation?", "key-reconciliation", "Explain the role of keys in diffing.", "**Direct answer :** Keys provide stable identity for siblings during reconciliation. When a list is reordered, inserted into, or filtered, stable keys help React match each item with the correct existing component instance. Using an unstable key such as a generated random value can cause unnecessary remounts, while using an index can associate state with the wrong item after reordering.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** Keys provide stable identity for siblings during reconciliation. When a list is reordered, inserted into, or filtered, stable keys help React match each item with the correct existing component instance. Using an unstable key such as a generated random value can cause unnecessary remounts, while using an index can associate state with the wrong item after reordering.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you lazy load components in React?", "lazy-loading", "Explain code splitting.", "**Direct answer :** `lazy` lets React defer loading a component's code until it is rendered. It commonly works with dynamic `import()` and a surrounding `<Suspense>` boundary that provides fallback UI while the code is loading. This is a form of code splitting that can reduce the JavaScript needed for the initial route.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** `lazy` lets React defer loading a component's code until it is rendered. It commonly works with dynamic `import()` and a surrounding `<Suspense>` boundary that provides fallback UI while the code is loading. This is a form of code splitting that can reduce the JavaScript needed for the initial route. A strong implementation should also account for dependency behavior, component ownership, cleanup where applicable, and the effect on rendering rather than treating the API as an isolated feature.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `Suspense` component?", "suspense", "Explain the fallback UI.", "**Direct answer :** `Suspense` lets React display a fallback while part of the UI is not ready to render, such as a component loaded with `lazy` or other supported Suspense-enabled resources. It is a coordination mechanism for loading states, not a generic promise-catching wrapper for arbitrary asynchronous code. Data-fetching behavior depends on the framework or library integration.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** `Suspense` lets React display a fallback while part of the UI is not ready to render, such as a component loaded with `lazy` or other supported Suspense-enabled resources. It is a coordination mechanism for loading states, not a generic promise-catching wrapper for arbitrary asynchronous code. Data-fetching behavior depends on the framework or library integration. A strong implementation should also account for dependency behavior, component ownership, cleanup where applicable, and the effect on rendering rather than treating the API as an isolated feature.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between `useCallback` and `useMemo`?", "usecallback-vs-usememo", "Compare the hooks.", "**Direct answer :** `useCallback` caches a function definition, while `useMemo` caches the result of a calculation. Both use dependency lists and both are performance optimizations. Use them when referential stability or avoiding an expensive calculation matters; do not add them automatically to every component.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `useCallback` caches a function definition, while `useMemo` caches the result of a calculation. Both use dependency lists and both are performance optimizations. Use them when referential stability or avoiding an expensive calculation matters; do not add them automatically to every component.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How can you avoid unnecessary re‑renders in React?", "avoid-renders", "Explain strategies.", "**Direct answer :** First distinguish unnecessary renders from necessary renders: React may re-render a component even when the resulting DOM changes little, and that is not automatically a performance problem. Profile the application, keep transient state local, avoid unnecessary Effects, split components, and use `memo`, `useMemo`, or `useCallback` when they measurably reduce expensive work.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** First distinguish unnecessary renders from necessary renders: React may re-render a component even when the resulting DOM changes little, and that is not automatically a performance problem. Profile the application, keep transient state local, avoid unnecessary Effects, split components, and use `memo`, `useMemo`, or `useCallback` when they measurably reduce expensive work.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the purpose of `React.StrictMode`?", "strict-mode", "Explain the tool.", "**Direct answer :** `<StrictMode>` enables development-only checks that help reveal unsafe patterns and accidental side effects. In modern React, it can intentionally re-run certain render/effect-related logic in development to expose code that is not resilient to repeated setup and cleanup. This behavior is not a production double-render guarantee; it is a development diagnostic tool.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `<StrictMode>` enables development-only checks that help reveal unsafe patterns and accidental side effects. In modern React, it can intentionally re-run certain render/effect-related logic in development to expose code that is not resilient to repeated setup and cleanup. This behavior is not a production double-render guarantee; it is a development diagnostic tool.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `useTransition` hook?", "usetransition", "Explain concurrent rendering.", "**Direct answer :** `useTransition` lets you mark certain state updates as non-blocking transitions. React can then prioritize urgent updates, such as typing, while working on the transition in the background. It returns `isPending` and `startTransition`. It does not make expensive JavaScript computations intrinsically faster, and updates that control text input values should not normally be placed inside a transition.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** `useTransition` lets you mark certain state updates as non-blocking transitions. React can then prioritize urgent updates, such as typing, while working on the transition in the background. It returns `isPending` and `startTransition`. It does not make expensive JavaScript computations intrinsically faster, and updates that control text input values should not normally be placed inside a transition.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `useDeferredValue` hook?", "usedeferredvalue", "Explain deferred value.", "**Direct answer :** `useDeferredValue` returns a version of a value that React may update later so urgent UI can stay responsive. It is useful when displaying a non-critical result depends on a value that changes frequently, such as search input. It does not debounce network requests by itself and should not be treated as a replacement for request cancellation or server-side query optimization.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** `useDeferredValue` returns a version of a value that React may update later so urgent UI can stay responsive. It is useful when displaying a non-critical result depends on a value that changes frequently, such as search input. It does not debounce network requests by itself and should not be treated as a replacement for request cancellation or server-side query optimization.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is `React.PureComponent`?", "purecomponent", "Explain the class component.", "**Direct answer :** `PureComponent` is a class component base class that performs a shallow comparison of props and state to decide whether to skip a re-render. It is roughly analogous to using `memo` for a function component, although the APIs differ. It is a legacy-oriented optimization for class components; new code generally uses function components and Hooks.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** `PureComponent` is a class component base class that performs a shallow comparison of props and state to decide whether to skip a re-render. It is roughly analogous to using `memo` for a function component, although the APIs differ. It is a legacy-oriented optimization for class components; new code generally uses function components and Hooks.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `useLayoutEffect` hook?", "uselayouteffect", "Explain the hook.", "**Direct answer :** `useLayoutEffect` runs after React has committed DOM mutations but before the browser paints. It is appropriate when you must measure layout or synchronously adjust something before the user sees it. Because it can block painting, prefer `useEffect` when layout timing is not required, and remember that layout effects can be problematic in server-rendered environments.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** `useLayoutEffect` runs after React has committed DOM mutations but before the browser paints. It is appropriate when you must measure layout or synchronously adjust something before the user sees it. Because it can block painting, prefer `useEffect` when layout timing is not required, and remember that layout effects can be problematic in server-rendered environments.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `useImperativeHandle` hook?", "useimperativehandle", "Explain the hook.", "**Direct answer :** `useImperativeHandle` customizes the value exposed through a ref, allowing a component to expose a small imperative API such as `focus()` instead of its entire DOM node. It should be used sparingly because React generally favors declarative props. In React 19, function components can receive `ref` as a prop, so `forwardRef` is no longer necessary for new function components.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** `useImperativeHandle` customizes the value exposed through a ref, allowing a component to expose a small imperative API such as `focus()` instead of its entire DOM node. It should be used sparingly because React generally favors declarative props. In React 19, function components can receive `ref` as a prop, so `forwardRef` is no longer necessary for new function components.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking when an imperative API is justified instead of declarative props."],

  // ==================== ADVANCED PATTERNS (15) ====================
  ["React", "What is a higher‑order component (HOC)?", "hoc", "Define HOC.", "**Direct answer :** A higher-order component is a function that accepts a component and returns another component with added behavior or data. It was a common way to reuse cross-cutting logic before Hooks became standard. HOCs can still be useful for certain libraries and legacy code, but custom Hooks and composition are often clearer for application logic.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** A higher-order component is a function that accepts a component and returns another component with added behavior or data. It was a common way to reuse cross-cutting logic before Hooks became standard. HOCs can still be useful for certain libraries and legacy code, but custom Hooks and composition are often clearer for application logic.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the render props pattern?", "render-props", "Explain the pattern.", "**Direct answer :** The render-props pattern passes a function as a prop so a component can provide data or behavior to that function while the caller decides what UI to render. It is a form of inversion of rendering control and was historically useful for sharing stateful logic. Hooks now often provide a simpler alternative for reusable logic.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** The render-props pattern passes a function as a prop so a component can provide data or behavior to that function while the caller decides what UI to render. It is a form of inversion of rendering control and was historically useful for sharing stateful logic. Hooks now often provide a simpler alternative for reusable logic.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What are compound components?", "compound-components", "Explain the pattern.", "**Direct answer :** Compound components are a group of related components that cooperate through shared state, often via context. For example, a `Tabs` parent can coordinate `Tabs.List`, `Tabs.Trigger`, and `Tabs.Panel`. The pattern gives consumers a flexible declarative API while keeping coordination logic inside the component family.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** Compound components are a group of related components that cooperate through shared state, often via context. For example, a `Tabs` parent can coordinate `Tabs.List`, `Tabs.Trigger`, and `Tabs.Panel`. The pattern gives consumers a flexible declarative API while keeping coordination logic inside the component family.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is a portal in React?", "portal", "Explain portals.", "**Direct answer :** A React portal renders children into a DOM node outside the DOM hierarchy of the component that created them, using `createPortal`. This is useful for modals, tooltips, and overlays that need to escape clipping or stacking contexts. The portal changes the physical DOM location, but the React tree relationship remains, so context and React event propagation still follow the React tree.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** A React portal renders children into a DOM node outside the DOM hierarchy of the component that created them, using `createPortal`. This is useful for modals, tooltips, and overlays that need to escape clipping or stacking contexts. The portal changes the physical DOM location, but the React tree relationship remains, so context and React event propagation still follow the React tree.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What are error boundaries and how do they work?", "error-boundaries", "Explain error handling.", "**Direct answer :** An Error Boundary catches errors thrown during rendering, lifecycle methods, and constructors in its descendant tree and renders fallback UI. Traditional Error Boundaries are implemented with class lifecycle methods such as `getDerivedStateFromError` and `componentDidCatch`; there is no built-in function-component Error Boundary API. They do not replace try/catch for event-handler or arbitrary asynchronous errors.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** An Error Boundary catches errors thrown during rendering, lifecycle methods, and constructors in its descendant tree and renders fallback UI. Traditional Error Boundaries are implemented with class lifecycle methods such as `getDerivedStateFromError` and `componentDidCatch`; there is no built-in function-component Error Boundary API. They do not replace try/catch for event-handler or arbitrary asynchronous errors.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking which kinds of errors Error Boundaries do not catch and how to handle those."],
  ["React", "What is the `forwardRef` function?", "forwardref", "Explain forwarding refs.", "**Direct answer :** In React 19, function components can receive `ref` as a prop, so new code generally does not need `forwardRef`. `forwardRef` remains a legacy API during the transition and is useful for understanding existing code. When used, it allows a component to expose a DOM node or imperative handle to its parent.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** In React 19, function components can receive `ref` as a prop, so new code generally does not need `forwardRef`. `forwardRef` remains a legacy API during the transition and is useful for understanding existing code. When used, it allows a component to expose a DOM node or imperative handle to its parent.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking what changed for refs in React 19."],
  ["React", "What are fragments and why are they used?", "fragments", "Explain fragments.", "**Direct answer :** A Fragment groups multiple React children without adding an extra DOM element. The short syntax is `<>...</>`, while `<Fragment>` can be given a `key` when rendering keyed fragment lists. Fragments are useful when an extra wrapper would affect layout, semantics, or CSS.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** A Fragment groups multiple React children without adding an extra DOM element. The short syntax is `<>...</>`, while `<Fragment>` can be given a `key` when rendering keyed fragment lists. Fragments are useful when an extra wrapper would affect layout, semantics, or CSS.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `useDebugValue` hook?", "usedebugvalue", "Explain the hook.", "**Direct answer :** `useDebugValue` lets a custom Hook expose a readable label or formatted value in React DevTools. It is primarily a developer-experience feature and has no effect on the rendered UI. It is most useful for reusable custom Hooks whose internal state would otherwise be difficult to understand while debugging.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `useDebugValue` lets a custom Hook expose a readable label or formatted value in React DevTools. It is primarily a developer-experience feature and has no effect on the rendered UI. It is most useful for reusable custom Hooks whose internal state would otherwise be difficult to understand while debugging.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you create a context with a default value?", "context-default-value", "Explain.", "**Direct answer :** `createContext(defaultValue)` defines the fallback value React returns to a consumer when there is no matching provider above it. The default is not used as a live provider state and does not change when a provider elsewhere changes. A sensible default can also make isolated component tests easier.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `createContext(defaultValue)` defines the fallback value React returns to a consumer when there is no matching provider above it. The default is not used as a live provider state and does not change when a provider elsewhere changes. A sensible default can also make isolated component tests easier.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between `Context.Provider` and `Context.Consumer`?", "provider-vs-consumer", "Compare the two.", "**Direct answer :** A context provider supplies a value to descendants, while `Context.Consumer` reads that value through a render function. Modern function components usually use `useContext` instead of `Consumer`, which is often clearer. In React 19, the context object itself can also be rendered as a provider, while `<SomeContext.Provider>` remains supported.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** A context provider supplies a value to descendants, while `Context.Consumer` reads that value through a render function. Modern function components usually use `useContext` instead of `Consumer`, which is often clearer. In React 19, the context object itself can also be rendered as a provider, while `<SomeContext.Provider>` remains supported.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is `React.cloneElement` and when would you use it?", "cloneelement", "Explain the API.", "**Direct answer :** `cloneElement` creates a new React element using an existing element as a starting point and can override or add props, including children. It can be useful in certain library patterns, but it is considered a legacy API and can make data flow harder to follow. Prefer explicit props, context, or composition when those provide a clearer design.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `cloneElement` creates a new React element using an existing element as a starting point and can override or add props, including children. It can be useful in certain library patterns, but it is considered a legacy API and can make data flow harder to follow. Prefer explicit props, context, or composition when those provide a clearer design.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `children` prop and how can you map over it?", "children-map", "Explain manipulation.", "**Direct answer :** `React.Children.map` safely iterates over the opaque `children` structure, including the cases where there is one child, many children, or no children. It is useful when a component needs to transform or augment children. However, `cloneElement`-based child manipulation can become fragile, so explicit APIs or context are often preferable for complex component relationships.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** `React.Children.map` safely iterates over the opaque `children` structure, including the cases where there is one child, many children, or no children. It is useful when a component needs to transform or augment children. However, `cloneElement`-based child manipulation can become fragile, so explicit APIs or context are often preferable for complex component relationships.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What are the pros and cons of using Redux vs Context API?", "redux-vs-context-pros-cons", "Discuss trade‑offs.", "**Direct answer :** Context is lightweight and built into React, but changing a provider value can update consumers that depend on that context. Redux provides a store architecture with selective subscriptions, middleware, and mature tooling. The right choice depends on state shape and update patterns; it is better to separate contexts or colocate state than to introduce Redux solely because an application is large.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** Context is lightweight and built into React, but changing a provider value can update consumers that depend on that context. Redux provides a store architecture with selective subscriptions, middleware, and mature tooling. The right choice depends on state shape and update patterns; it is better to separate contexts or colocate state than to introduce Redux solely because an application is large.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you handle forms in React?", "react-forms", "Explain form handling.", "**Direct answer :** React forms can be controlled, where React state is the source of truth, or uncontrolled, where the DOM owns the current value and refs are used when needed. Controlled forms are convenient for dynamic validation and conditional UI; uncontrolled forms can reduce state wiring. For complex forms, a form library can manage registration, validation, and submission while the server still performs authoritative validation.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** React forms can be controlled, where React state is the source of truth, or uncontrolled, where the DOM owns the current value and refs are used when needed. Controlled forms are convenient for dynamic validation and conditional UI; uncontrolled forms can reduce state wiring. For complex forms, a form library can manage registration, validation, and submission while the server still performs authoritative validation.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],

  // ==================== ROUTING (10) ====================
  ["React", "What is React Router and why is it used?", "react-router", "Explain the library.", "**Direct answer :** React Router is a routing library for React applications. It maps URLs to UI, supports nested routes, navigation, route parameters, loaders/actions in its data APIs, and other routing concerns. The exact API depends on the React Router version, so interview answers should state the version when discussing specific components or patterns.\n\n**Why this is asked :** Interviewers ask this to check that you understand client-side navigation as well as the browser and accessibility behavior behind it, not just the router API.\n\n**Breaking it down :** React Router is a routing library for React applications. It maps URLs to UI, supports nested routes, navigation, route parameters, loaders/actions in its data APIs, and other routing concerns. The exact API depends on the React Router version, so interview answers should state the version when discussing specific components or patterns. In practice, keep route definitions consistent, preserve normal browser semantics, and test navigation, refreshes, parameters, and nested layouts.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between `BrowserRouter` and `HashRouter`?", "browserrouter-vs-hashrouter", "Compare the two.", "**Direct answer :** `BrowserRouter` uses normal URL paths with the browser History API, producing clean URLs such as `/about`; the server must be configured to serve the application entry point for client-side routes on refresh. `HashRouter` stores the route after `#`, which avoids server-side route fallback requirements but produces hash URLs and has different SEO/navigation characteristics.\n\n**Why this is asked :** Interviewers ask this to check that you understand client-side navigation as well as the browser and accessibility behavior behind it, not just the router API.\n\n**Breaking it down :** `BrowserRouter` uses normal URL paths with the browser History API, producing clean URLs such as `/about`; the server must be configured to serve the application entry point for client-side routes on refresh. `HashRouter` stores the route after `#`, which avoids server-side route fallback requirements but produces hash URLs and has different SEO/navigation characteristics. In practice, keep route definitions consistent, preserve normal browser semantics, and test navigation, refreshes, parameters, and nested layouts.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `Route` component and how does it work?", "route-component", "Explain routing.", "**Direct answer :** In React Router's declarative API, a `Route` associates a URL pattern with an element and is normally declared inside `Routes`. When the current location matches, the router renders the associated branch. Route nesting also establishes layout and child-route relationships, which can be rendered through `Outlet`.\n\n**Why this is asked :** Interviewers ask this to check that you understand client-side navigation as well as the browser and accessibility behavior behind it, not just the router API.\n\n**Breaking it down :** In React Router's declarative API, a `Route` associates a URL pattern with an element and is normally declared inside `Routes`. When the current location matches, the router renders the associated branch. Route nesting also establishes layout and child-route relationships, which can be rendered through `Outlet`. In practice, keep route definitions consistent, preserve normal browser semantics, and test navigation, refreshes, parameters, and nested layouts.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `Link` component and how is it different from an `<a>` tag?", "link-vs-anchor", "Compare navigation.", "**Direct answer :** `Link` provides client-side navigation within a React Router application and avoids a normal full-document navigation for supported internal routes. An HTML `<a>` element is the standard web primitive and is appropriate for external URLs and cases where a normal document navigation is desired. For accessibility and semantics, links should remain links rather than being simulated with buttons.\n\n**Why this is asked :** Interviewers ask this to check that you understand client-side navigation as well as the browser and accessibility behavior behind it, not just the router API.\n\n**Breaking it down :** `Link` provides client-side navigation within a React Router application and avoids a normal full-document navigation for supported internal routes. An HTML `<a>` element is the standard web primitive and is appropriate for external URLs and cases where a normal document navigation is desired. For accessibility and semantics, links should remain links rather than being simulated with buttons. In practice, keep route definitions consistent, preserve normal browser semantics, and test navigation, refreshes, parameters, and nested layouts.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `useNavigate` hook?", "usenavigate", "Explain the hook.", "**Direct answer :** `useNavigate` returns a navigation function for imperative navigation, such as redirecting after a successful action. It can navigate by path or history delta and supports options such as replacement. Declarative navigation is often preferable when navigation can be expressed directly in the rendered UI.\n\n**Why this is asked :** Interviewers ask this to check that you understand client-side navigation as well as the browser and accessibility behavior behind it, not just the router API.\n\n**Breaking it down :** `useNavigate` returns a navigation function for imperative navigation, such as redirecting after a successful action. It can navigate by path or history delta and supports options such as replacement. Declarative navigation is often preferable when navigation can be expressed directly in the rendered UI. In practice, keep route definitions consistent, preserve normal browser semantics, and test navigation, refreshes, parameters, and nested layouts.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `useParams` hook?", "useparams", "Explain the hook.", "**Direct answer :** `useParams` reads dynamic route parameters from the currently matched route. For a route such as `/users/:id`, the returned `id` is typically a string because it comes from the URL. The component should validate or convert it as needed before using it as a database or API identifier.\n\n**Why this is asked :** Interviewers ask this to check that you understand client-side navigation as well as the browser and accessibility behavior behind it, not just the router API.\n\n**Breaking it down :** `useParams` reads dynamic route parameters from the currently matched route. For a route such as `/users/:id`, the returned `id` is typically a string because it comes from the URL. The component should validate or convert it as needed before using it as a database or API identifier. In practice, keep route definitions consistent, preserve normal browser semantics, and test navigation, refreshes, parameters, and nested layouts.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `useLocation` hook?", "uselocation", "Explain the hook.", "**Direct answer :** `useLocation` returns the current location object, including information such as pathname, search, hash, state, and a location key depending on the router version. It is useful when UI should react to navigation changes. Query-string parsing is usually done from `location.search` with `URLSearchParams` or a routing utility.\n\n**Why this is asked :** Interviewers ask this to check that you understand client-side navigation as well as the browser and accessibility behavior behind it, not just the router API.\n\n**Breaking it down :** `useLocation` returns the current location object, including information such as pathname, search, hash, state, and a location key depending on the router version. It is useful when UI should react to navigation changes. Query-string parsing is usually done from `location.search` with `URLSearchParams` or a routing utility. In practice, keep route definitions consistent, preserve normal browser semantics, and test navigation, refreshes, parameters, and nested layouts.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between `Navigate` and `useNavigate`?", "navigate-vs-usenavigate", "Compare the two.", "**Direct answer :** `Navigate` is a React Router component used for declarative navigation when rendered. `useNavigate` is a Hook that returns a function for imperative navigation from component logic or event handlers. A good rule is to use the component when the redirect is a rendering condition and the Hook when navigation is triggered by an action or workflow.\n\n**Why this is asked :** Interviewers ask this to check that you understand client-side navigation as well as the browser and accessibility behavior behind it, not just the router API.\n\n**Breaking it down :** `Navigate` is a React Router component used for declarative navigation when rendered. `useNavigate` is a Hook that returns a function for imperative navigation from component logic or event handlers. A good rule is to use the component when the redirect is a rendering condition and the Hook when navigation is triggered by an action or workflow. In practice, keep route definitions consistent, preserve normal browser semantics, and test navigation, refreshes, parameters, and nested layouts.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you handle nested routes in React Router?", "nested-routes", "Explain nested routing.", "**Direct answer :** Nested routes model a parent-child URL and UI hierarchy. A parent route can render a layout and place an `<Outlet>` where the matched child route appears. This allows shared navigation, headers, or loaders/layout behavior to remain in the parent while only the child content changes.\n\n**Why this is asked :** Interviewers ask this to check that you understand client-side navigation as well as the browser and accessibility behavior behind it, not just the router API.\n\n**Breaking it down :** Nested routes model a parent-child URL and UI hierarchy. A parent route can render a layout and place an `<Outlet>` where the matched child route appears. This allows shared navigation, headers, or loaders/layout behavior to remain in the parent while only the child content changes. In practice, keep route definitions consistent, preserve normal browser semantics, and test navigation, refreshes, parameters, and nested layouts.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `Outlet` component?", "outlet", "Explain the component.", "**Direct answer :** `Outlet` is the placeholder where React Router renders the element for the currently matched child route. It is commonly placed in a parent layout component. The parent remains mounted while child routes change, which makes it useful for shared navigation and layout UI.\n\n**Why this is asked :** Interviewers ask this to check that you understand client-side navigation as well as the browser and accessibility behavior behind it, not just the router API.\n\n**Breaking it down :** `Outlet` is the placeholder where React Router renders the element for the currently matched child route. It is commonly placed in a parent layout component. The parent remains mounted while child routes change, which makes it useful for shared navigation and layout UI. In practice, keep route definitions consistent, preserve normal browser semantics, and test navigation, refreshes, parameters, and nested layouts.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],

  // ==================== TESTING (10) ====================
  ["React", "What is Jest and how is it used with React?", "jest", "Explain the testing framework.", "**Direct answer :** Jest is a JavaScript testing framework commonly used for unit and integration-oriented tests, with features such as assertions, mocks, spies, fake timers, and snapshot testing. In a React project, it is usually paired with a DOM testing environment and a UI testing library rather than treating component internals as the primary testing target.\n\n**Why this is asked :** Interviewers ask this to assess whether you can write tests that are reliable, meaningful, and focused on observable behavior rather than implementation details.\n\n**Breaking it down :** Jest is a JavaScript testing framework commonly used for unit and integration-oriented tests, with features such as assertions, mocks, spies, fake timers, and snapshot testing. In a React project, it is usually paired with a DOM testing environment and a UI testing library rather than treating component internals as the primary testing target. A useful test should verify the observable result, wait for asynchronous updates explicitly, and avoid coupling assertions to internal implementation details.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is React Testing Library?", "react-testing-library", "Explain the library.", "**Direct answer :** React Testing Library provides utilities for rendering React components and querying the resulting UI in ways that resemble user interaction. Its guiding approach is to test observable behavior and accessible semantics rather than component implementation details. Queries such as `getByRole` are often preferred because they encourage accessible UI.\n\n**Why this is asked :** Interviewers ask this to assess whether you can write tests that are reliable, meaningful, and focused on observable behavior rather than implementation details.\n\n**Breaking it down :** React Testing Library provides utilities for rendering React components and querying the resulting UI in ways that resemble user interaction. Its guiding approach is to test observable behavior and accessible semantics rather than component implementation details. Queries such as `getByRole` are often preferred because they encourage accessible UI. A useful test should verify the observable result, wait for asynchronous updates explicitly, and avoid coupling assertions to internal implementation details.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking why role-based queries are preferred over testing implementation details."],
  ["React", "What is the difference between unit testing and integration testing in React?", "unit-vs-integration-testing", "Compare test types.", "**Direct answer :** A unit test focuses on a small unit of behavior with limited dependencies, while an integration test verifies that multiple pieces work together. In React, a component test that renders a form, interacts with it, and verifies an API-related UI state can be more integration-oriented than a purely isolated unit test. A balanced test suite prioritizes important user-visible behavior.\n\n**Why this is asked :** Interviewers ask this to assess whether you can write tests that are reliable, meaningful, and focused on observable behavior rather than implementation details.\n\n**Breaking it down :** A unit test focuses on a small unit of behavior with limited dependencies, while an integration test verifies that multiple pieces work together. In React, a component test that renders a form, interacts with it, and verifies an API-related UI state can be more integration-oriented than a purely isolated unit test. A balanced test suite prioritizes important user-visible behavior.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What are snapshots in Jest?", "snapshots", "Explain snapshot testing.", "**Direct answer :** A Jest snapshot stores a serialized representation of a value, often rendered output, and compares later results with the saved snapshot. Snapshots can detect unintended changes, but large snapshots are difficult to review and can encourage blindly updating files. Small, intentional snapshots or explicit assertions are usually easier to maintain.\n\n**Why this is asked :** Interviewers ask this to assess whether you can write tests that are reliable, meaningful, and focused on observable behavior rather than implementation details.\n\n**Breaking it down :** A Jest snapshot stores a serialized representation of a value, often rendered output, and compares later results with the saved snapshot. Snapshots can detect unintended changes, but large snapshots are difficult to review and can encourage blindly updating files. Small, intentional snapshots or explicit assertions are usually easier to maintain.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you test asynchronous code in React?", "async-testing", "Explain async testing.", "**Direct answer :** For asynchronous React behavior, wait for the user-visible result rather than relying on arbitrary delays. React Testing Library provides `findBy*` queries and `waitFor` for this purpose, and its user interactions are designed to work with React's update lifecycle. Mock network boundaries using the approach appropriate to the test stack, and assert loading, success, and failure states.\n\n**Why this is asked :** Interviewers ask this to assess whether you can write tests that are reliable, meaningful, and focused on observable behavior rather than implementation details.\n\n**Breaking it down :** For asynchronous React behavior, wait for the user-visible result rather than relying on arbitrary delays. React Testing Library provides `findBy*` queries and `waitFor` for this purpose, and its user interactions are designed to work with React's update lifecycle. Mock network boundaries using the approach appropriate to the test stack, and assert loading, success, and failure states. A useful test should verify the observable result, wait for asynchronous updates explicitly, and avoid coupling assertions to internal implementation details.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `render` function in React Testing Library?", "render-function", "Explain the utility.", "**Direct answer :** React Testing Library's `render` function renders a React element into a test DOM and returns utilities for querying and interacting with it. The recommended approach is usually to query the accessible UI with methods such as `getByRole`, `findByRole`, or `queryByRole` rather than reaching into implementation-specific containers.\n\n**Why this is asked :** Interviewers ask this to assess whether you can write tests that are reliable, meaningful, and focused on observable behavior rather than implementation details.\n\n**Breaking it down :** React Testing Library's `render` function renders a React element into a test DOM and returns utilities for querying and interacting with it. The recommended approach is usually to query the accessible UI with methods such as `getByRole`, `findByRole`, or `queryByRole` rather than reaching into implementation-specific containers.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `fireEvent` and `userEvent` utilities?", "fireevent-userevent", "Explain event simulation.", "**Direct answer :** `fireEvent` dispatches individual DOM events, while `userEvent` models higher-level user interactions such as typing and clicking and may trigger the sequence of events a real interaction produces. For most user-facing tests, `userEvent` is preferred because it more closely represents how users interact with the UI. `fireEvent` remains useful for lower-level event cases.\n\n**Why this is asked :** Interviewers ask this to assess whether you can write tests that are reliable, meaningful, and focused on observable behavior rather than implementation details.\n\n**Breaking it down :** `fireEvent` dispatches individual DOM events, while `userEvent` models higher-level user interactions such as typing and clicking and may trigger the sequence of events a real interaction produces. For most user-facing tests, `userEvent` is preferred because it more closely represents how users interact with the UI. `fireEvent` remains useful for lower-level event cases. A useful test should verify the observable result, wait for asynchronous updates explicitly, and avoid coupling assertions to internal implementation details.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you mock a module in Jest?", "jest-mock", "Explain mocking.", "**Direct answer :** `jest.mock()` replaces a module with a mock implementation for a test. This can isolate a component from a network client, analytics module, or other dependency. Good tests mock external boundaries deliberately rather than mocking every internal function, and the mock should reflect the behavior the component actually depends on.\n\n**Why this is asked :** Interviewers ask this to assess whether you can write tests that are reliable, meaningful, and focused on observable behavior rather than implementation details.\n\n**Breaking it down :** `jest.mock()` replaces a module with a mock implementation for a test. This can isolate a component from a network client, analytics module, or other dependency. Good tests mock external boundaries deliberately rather than mocking every internal function, and the mock should reflect the behavior the component actually depends on.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the `act` helper and when should you use it?", "act-helper", "Explain `act` from React.", "**Direct answer :** `act` ensures React updates caused by an interaction or asynchronous work are processed before assertions are made. React Testing Library wraps many common operations in `act` automatically, so manual `act` calls are often unnecessary. If an act warning appears, it usually indicates the test is not waiting for an update or an asynchronous user interaction to finish.\n\n**Why this is asked :** Interviewers ask this to assess whether you can write tests that are reliable, meaningful, and focused on observable behavior rather than implementation details.\n\n**Breaking it down :** `act` ensures React updates caused by an interaction or asynchronous work are processed before assertions are made. React Testing Library wraps many common operations in `act` automatically, so manual `act` calls are often unnecessary. If an act warning appears, it usually indicates the test is not waiting for an update or an asynchronous user interaction to finish. A useful test should verify the observable result, wait for asynchronous updates explicitly, and avoid coupling assertions to internal implementation details.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between `getBy` and `queryBy` in RTL?", "getby-vs-queryby", "Compare query methods.", "**Direct answer :** `getBy*` expects the element to exist and throws when it cannot find it, making failures immediate and descriptive. `queryBy*` returns `null` when the element is absent, which is useful for asserting non-existence. For asynchronous appearance or disappearance, use the appropriate `findBy*` or `waitFor` pattern instead of polling manually.\n\n**Why this is asked :** Interviewers ask this to assess whether you can write tests that are reliable, meaningful, and focused on observable behavior rather than implementation details.\n\n**Breaking it down :** `getBy*` expects the element to exist and throws when it cannot find it, making failures immediate and descriptive. `queryBy*` returns `null` when the element is absent, which is useful for asserting non-existence. For asynchronous appearance or disappearance, use the appropriate `findBy*` or `waitFor` pattern instead of polling manually.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],

  // ==================== SERVER‑SIDE RENDERING (10) ====================
  ["React", "What is server‑side rendering (SSR) and why use it?", "ssr", "Explain SSR.", "**Direct answer :** Server-side rendering generates the initial React UI on the server and sends HTML to the browser, after which the client can hydrate it to become interactive. SSR can improve perceived loading and provide crawlable HTML, but it also adds server work and hydration complexity. SEO is not automatically guaranteed simply because a page uses SSR; the rendered content and metadata still need to be correct.\n\n**Why this is asked :** Interviewers ask this to test your understanding of where React rendering happens, how server-rendered HTML becomes interactive, and what performance, caching, and framework trade-offs follow from that choice.\n\n**Breaking it down :** Server-side rendering generates the initial React UI on the server and sends HTML to the browser, after which the client can hydrate it to become interactive. SSR can improve perceived loading and provide crawlable HTML, but it also adds server work and hydration complexity. SEO is not automatically guaranteed simply because a page uses SSR; the rendered content and metadata still need to be correct. The important design questions are where the data is fetched, when the HTML is produced, how it is cached, and how the client becomes interactive.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is Next.js and how does it differ from plain React?", "nextjs", "Explain the framework.", "**Direct answer :** Next.js is a React framework that provides application-level features such as routing, server and client rendering models, data fetching patterns, optimization, and deployment support. It is not simply 'React with SSR'; modern Next.js uses React features such as Server Components and its own routing and build system. APIs such as `getStaticProps` and `getServerSideProps` belong to the older Pages Router model.\n\n**Why this is asked :** Interviewers ask this to test your understanding of where React rendering happens, how server-rendered HTML becomes interactive, and what performance, caching, and framework trade-offs follow from that choice.\n\n**Breaking it down :** Next.js is a React framework that provides application-level features such as routing, server and client rendering models, data fetching patterns, optimization, and deployment support. It is not simply 'React with SSR'; modern Next.js uses React features such as Server Components and its own routing and build system. APIs such as `getStaticProps` and `getServerSideProps` belong to the older Pages Router model.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is `ReactDOMServer.renderToString`?", "rendertostring", "Explain the function.", "**Direct answer :** `renderToString` renders a React tree to an HTML string on the server. It can be used for server rendering, but modern applications may prefer streaming APIs such as `renderToPipeableStream` or `renderToReadableStream` when the environment supports them. The choice depends on the server architecture and whether streaming is useful.\n\n**Why this is asked :** Interviewers ask this to test your understanding of where React rendering happens, how server-rendered HTML becomes interactive, and what performance, caching, and framework trade-offs follow from that choice.\n\n**Breaking it down :** `renderToString` renders a React tree to an HTML string on the server. It can be used for server rendering, but modern applications may prefer streaming APIs such as `renderToPipeableStream` or `renderToReadableStream` when the environment supports them. The choice depends on the server architecture and whether streaming is useful.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between SSR and CSR (Client‑Side Rendering)?", "ssr-vs-csr", "Compare the two.", "**Direct answer :** With CSR, the browser receives an application shell and JavaScript that renders the UI on the client. With SSR, the server generates HTML for the initial request and the client can hydrate it. SSR can improve initial content visibility and crawlability, while CSR can simplify some architectures and reduce server rendering work. Modern frameworks can combine server and client rendering rather than choosing one globally.\n\n**Why this is asked :** Interviewers ask this to test your understanding of where React rendering happens, how server-rendered HTML becomes interactive, and what performance, caching, and framework trade-offs follow from that choice.\n\n**Breaking it down :** With CSR, the browser receives an application shell and JavaScript that renders the UI on the client. With SSR, the server generates HTML for the initial request and the client can hydrate it. SSR can improve initial content visibility and crawlability, while CSR can simplify some architectures and reduce server rendering work. Modern frameworks can combine server and client rendering rather than choosing one globally. The important design questions are where the data is fetched, when the HTML is produced, how it is cached, and how the client becomes interactive.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is hydration in React?", "hydration", "Explain the process.", "**Direct answer :** Hydration is the client-side process of attaching React behavior to HTML that was already rendered on the server. The client must produce a compatible tree for the server output; mismatches can cause warnings or force React to recover by re-rendering parts of the UI. Hydration is different from creating a completely new client-rendered DOM tree.\n\n**Why this is asked :** Interviewers ask this to test your understanding of where React rendering happens, how server-rendered HTML becomes interactive, and what performance, caching, and framework trade-offs follow from that choice.\n\n**Breaking it down :** Hydration is the client-side process of attaching React behavior to HTML that was already rendered on the server. The client must produce a compatible tree for the server output; mismatches can cause warnings or force React to recover by re-rendering parts of the UI. Hydration is different from creating a completely new client-rendered DOM tree. The important design questions are where the data is fetched, when the HTML is produced, how it is cached, and how the client becomes interactive.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking what causes hydration mismatches and how to diagnose them."],
  ["React", "What is `ReactDOM.hydrateRoot`?", "hydrateroot", "Explain the API.", "**Direct answer :** `hydrateRoot` is the React DOM API used to hydrate server-rendered HTML. It creates a client React root while reusing the existing server-generated DOM. It should be used when the container already contains HTML produced by React on the server; `createRoot` is used for a normal client-rendered root.\n\n**Why this is asked :** Interviewers ask this to test your understanding of where React rendering happens, how server-rendered HTML becomes interactive, and what performance, caching, and framework trade-offs follow from that choice.\n\n**Breaking it down :** `hydrateRoot` is the React DOM API used to hydrate server-rendered HTML. It creates a client React root while reusing the existing server-generated DOM. It should be used when the container already contains HTML produced by React on the server; `createRoot` is used for a normal client-rendered root.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is static site generation (SSG) in Next.js?", "ssg", "Explain SSG.", "**Direct answer :** Static site generation means producing HTML ahead of the request, typically at build time, so it can be served efficiently from a CDN or static infrastructure. In Next.js, the exact APIs depend on the router: `getStaticProps` belongs to the Pages Router, while the App Router uses different data-fetching and caching mechanisms. SSG is useful when content can be generated ahead of requests.\n\n**Why this is asked :** Interviewers ask this to test your understanding of where React rendering happens, how server-rendered HTML becomes interactive, and what performance, caching, and framework trade-offs follow from that choice.\n\n**Breaking it down :** Static site generation means producing HTML ahead of the request, typically at build time, so it can be served efficiently from a CDN or static infrastructure. In Next.js, the exact APIs depend on the router: `getStaticProps` belongs to the Pages Router, while the App Router uses different data-fetching and caching mechanisms. SSG is useful when content can be generated ahead of requests. The important design questions are where the data is fetched, when the HTML is produced, how it is cached, and how the client becomes interactive.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is `getServerSideProps` in Next.js?", "getserversideprops", "Explain the function.", "**Direct answer :** `getServerSideProps` is a Pages Router API in Next.js that runs on the server for a request and returns props for the page. It is useful when page data must be resolved at request time. It is not part of the modern App Router API, so an interview answer should clarify that it applies to the Pages Router.\n\n**Why this is asked :** Interviewers ask this to test your understanding of where React rendering happens, how server-rendered HTML becomes interactive, and what performance, caching, and framework trade-offs follow from that choice.\n\n**Breaking it down :** `getServerSideProps` is a Pages Router API in Next.js that runs on the server for a request and returns props for the page. It is useful when page data must be resolved at request time. It is not part of the modern App Router API, so an interview answer should clarify that it applies to the Pages Router. The important design questions are where the data is fetched, when the HTML is produced, how it is cached, and how the client becomes interactive.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is `getStaticProps` in Next.js?", "getstaticprops", "Explain the function.", "**Direct answer :** `getStaticProps` is a Pages Router API in Next.js that generates page props at build time and can participate in Incremental Static Regeneration when configured with a revalidation strategy. It is suited to content that can be generated ahead of requests. The modern App Router uses different server data-fetching and caching primitives.\n\n**Why this is asked :** Interviewers ask this to test your understanding of where React rendering happens, how server-rendered HTML becomes interactive, and what performance, caching, and framework trade-offs follow from that choice.\n\n**Breaking it down :** `getStaticProps` is a Pages Router API in Next.js that generates page props at build time and can participate in Incremental Static Regeneration when configured with a revalidation strategy. It is suited to content that can be generated ahead of requests. The modern App Router uses different server data-fetching and caching primitives. The important design questions are where the data is fetched, when the HTML is produced, how it is cached, and how the client becomes interactive.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between `getStaticProps` and `getServerSideProps`?", "getstaticprops-vs-getserversideprops", "Compare the two.", "**Direct answer :** `getStaticProps` belongs to the Next.js Pages Router and generates props ahead of requests, optionally with revalidation through ISR. `getServerSideProps` also belongs to the Pages Router but runs on the server for each request. The choice is fundamentally about when data must be resolved and cached, not a simple claim that one is always faster.\n\n**Why this is asked :** Interviewers ask this to test your understanding of where React rendering happens, how server-rendered HTML becomes interactive, and what performance, caching, and framework trade-offs follow from that choice.\n\n**Breaking it down :** `getStaticProps` belongs to the Next.js Pages Router and generates props ahead of requests, optionally with revalidation through ISR. `getServerSideProps` also belongs to the Pages Router but runs on the server for each request. The choice is fundamentally about when data must be resolved and cached, not a simple claim that one is always faster. The important design questions are where the data is fetched, when the HTML is produced, how it is cached, and how the client becomes interactive.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],

  // ==================== SECURITY & BEST PRACTICES (10) ====================
  ["React", "How do you prevent XSS attacks in React?", "xss-prevention", "Explain security measures.", "**Direct answer :** React escapes text values inserted through normal JSX rendering, which reduces many XSS risks. That protection does not make all inputs safe: raw HTML insertion, unsafe URLs, third-party libraries, and server-side vulnerabilities still matter. Treat untrusted HTML as unsafe, sanitize it with a well-maintained sanitizer when raw HTML is genuinely required, and use secure server-side validation and authentication as well.\n\n**Why this is asked :** Interviewers ask this because frontend code crosses security and reliability boundaries. They want to know whether you understand what React protects, what it does not protect, and where enforcement must happen outside the UI.\n\n**Breaking it down :** React escapes text values inserted through normal JSX rendering, which reduces many XSS risks. That protection does not make all inputs safe: raw HTML insertion, unsafe URLs, third-party libraries, and server-side vulnerabilities still matter. Treat untrusted HTML as unsafe, sanitize it with a well-maintained sanitizer when raw HTML is genuinely required, and use secure server-side validation and authentication as well. The key boundary is that browser-side code improves UX but cannot replace server-side security controls or validation.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is `dangerouslySetInnerHTML` and why is it dangerous?", "dangerouslysetinnerhtml", "Explain the prop.", "**Direct answer :** `dangerouslySetInnerHTML` tells React to insert raw HTML rather than escaping it as text. It is dangerous when the HTML contains attacker-controlled content because it can create XSS vulnerabilities. Use it only when the source is trusted or has been correctly sanitized for the intended context, and never assume that simply removing `<script>` tags is sufficient sanitization.\n\n**Why this is asked :** Interviewers ask this because frontend code crosses security and reliability boundaries. They want to know whether you understand what React protects, what it does not protect, and where enforcement must happen outside the UI.\n\n**Breaking it down :** `dangerouslySetInnerHTML` tells React to insert raw HTML rather than escaping it as text. It is dangerous when the HTML contains attacker-controlled content because it can create XSS vulnerabilities. Use it only when the source is trusted or has been correctly sanitized for the intended context, and never assume that simply removing `<script>` tags is sufficient sanitization. The key boundary is that browser-side code improves UX but cannot replace server-side security controls or validation.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you handle authentication in React?", "react-authentication", "Explain authentication patterns.", "**Direct answer :** Authentication should be enforced by the backend; React mainly represents the authenticated state and protects the user experience. A common web pattern is an HttpOnly, Secure, appropriately SameSite cookie holding a server-managed session, which is less exposed to JavaScript than a token in `localStorage`. If tokens are used, storage, expiration, refresh, CSRF, XSS, and logout behavior must be designed explicitly. Client-side route guards are not a security boundary.\n\n**Why this is asked :** Interviewers ask this because frontend code crosses security and reliability boundaries. They want to know whether you understand what React protects, what it does not protect, and where enforcement must happen outside the UI.\n\n**Breaking it down :** Authentication should be enforced by the backend; React mainly represents the authenticated state and protects the user experience. A common web pattern is an HttpOnly, Secure, appropriately SameSite cookie holding a server-managed session, which is less exposed to JavaScript than a token in `localStorage`. If tokens are used, storage, expiration, refresh, CSRF, XSS, and logout behavior must be designed explicitly. Client-side route guards are not a security boundary. The key boundary is that browser-side code improves UX but cannot replace server-side security controls or validation.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking why a client-side route guard cannot enforce authorization."],
  ["React", "What are the best practices for folder structure in React?", "folder-structure", "Explain file organization.", "**Direct answer :** A useful React folder structure should make ownership and dependencies clear rather than following one universal template. Feature-based organization can keep a feature's components, hooks, tests, and API logic together, while truly shared UI and utilities can live in shared modules. Avoid creating folders such as `utils` or `components` as dumping grounds with no clear boundaries.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** A useful React folder structure should make ownership and dependencies clear rather than following one universal template. Feature-based organization can keep a feature's components, hooks, tests, and API logic together, while truly shared UI and utilities can live in shared modules. Avoid creating folders such as `utils` or `components` as dumping grounds with no clear boundaries.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you handle environment variables in React?", "env-variables", "Explain usage.", "**Direct answer :** Environment variables are configuration inputs, but values embedded into a browser bundle are not secrets because users can inspect them. Create React App exposes variables with its `REACT_APP_` convention, while Vite exposes client variables with `VITE_`; other tools use different conventions. Sensitive credentials must stay on trusted server infrastructure and should be accessed through secure server-side code.\n\n**Why this is asked :** Interviewers ask this because frontend code crosses security and reliability boundaries. They want to know whether you understand what React protects, what it does not protect, and where enforcement must happen outside the UI.\n\n**Breaking it down :** Environment variables are configuration inputs, but values embedded into a browser bundle are not secrets because users can inspect them. Create React App exposes variables with its `REACT_APP_` convention, while Vite exposes client variables with `VITE_`; other tools use different conventions. Sensitive credentials must stay on trusted server infrastructure and should be accessed through secure server-side code.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking whether a value exposed to the browser can be considered secret."],
  ["React", "What is the benefit of using TypeScript with React?", "typescript-react", "Explain advantages.", "**Direct answer :** TypeScript adds static checking to React applications. It can type component props, state, event handlers, refs, API responses, and custom Hooks, improving autocomplete and refactoring confidence. It does not replace runtime validation: data received from APIs or users should still be validated at runtime when correctness or security depends on it.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** TypeScript adds static checking to React applications. It can type component props, state, event handlers, refs, API responses, and custom Hooks, improving autocomplete and refactoring confidence. It does not replace runtime validation: data received from APIs or users should still be validated at runtime when correctness or security depends on it.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you handle errors in React?", "error-handling", "Explain error handling strategies.", "**Direct answer :** React error handling should match the failure source. Error Boundaries handle errors during rendering and related React lifecycle work in descendant components, while event handlers and asynchronous operations need their own error handling. API calls should model loading, success, and failure states, and production applications often send unexpected failures to an error-monitoring system without exposing sensitive details to users.\n\n**Why this is asked :** Interviewers ask this because frontend code crosses security and reliability boundaries. They want to know whether you understand what React protects, what it does not protect, and where enforcement must happen outside the UI.\n\n**Breaking it down :** React error handling should match the failure source. Error Boundaries handle errors during rendering and related React lifecycle work in descendant components, while event handlers and asynchronous operations need their own error handling. API calls should model loading, success, and failure states, and production applications often send unexpected failures to an error-monitoring system without exposing sensitive details to users.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the difference between `development` and `production` builds in React?", "dev-vs-prod-build", "Compare builds.", "**Direct answer :** Development builds prioritize diagnostics, readable errors, and a fast feedback loop, while production builds are optimized for delivery and execution. Production tooling commonly performs minification and dead-code elimination and removes development-only checks. Exact behavior depends on the build tool or framework, so 'React production build' should not be treated as one universal compiler pipeline.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** Development builds prioritize diagnostics, readable errors, and a fast feedback loop, while production builds are optimized for delivery and execution. Production tooling commonly performs minification and dead-code elimination and removes development-only checks. Exact behavior depends on the build tool or framework, so 'React production build' should not be treated as one universal compiler pipeline.\n\n**How to structure your spoken answer :** Start by stating the distinction in one sentence. Then compare the two approaches by purpose, lifecycle or data flow, practical usage, and trade-offs. Finish with a short example of when you would choose each option.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you manage dependencies in React projects?", "dependency-management", "Explain package management.", "**Direct answer :** Use the package manager supported by the project and commit the appropriate lockfile so installations are reproducible. Review dependency changes for breaking changes, security advisories, bundle impact, and transitive dependencies rather than updating blindly. Separate runtime dependencies from development-only tooling according to how the package is used in production.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** Use the package manager supported by the project and commit the appropriate lockfile so installations are reproducible. Review dependency changes for breaking changes, security advisories, bundle impact, and transitive dependencies rather than updating blindly. Separate runtime dependencies from development-only tooling according to how the package is used in production.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "What is the React Developer Tools and how do you use them?", "react-devtools", "Explain the browser extension.", "**Direct answer :** React Developer Tools lets developers inspect the component tree, inspect props and state, view context, and profile rendering behavior. The Profiler can help identify components and interactions associated with expensive rendering. DevTools is most useful when paired with measurement: use it to identify a bottleneck, make a targeted change, and verify whether the change improved the actual user experience.\n\n**Why this is asked :** Interviewers ask this to check whether you understand the React concept itself and can explain when to use it, how it behaves, and what trade-offs or pitfalls matter in production.\n\n**Breaking it down :** React Developer Tools lets developers inspect the component tree, inspect props and state, view context, and profile rendering behavior. The Profiler can help identify components and interactions associated with expensive rendering. DevTools is most useful when paired with measurement: use it to identify a bottleneck, make a targeted change, and verify whether the change improved the actual user experience.\n\n**How to structure your spoken answer :** Define the concept first, then explain how it works in React, give a practical example, and mention one important limitation or trade-off. Keep the explanation tied to real component behavior instead of listing APIs without context.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],

  // ==================== SCENARIO‑BASED (20) ====================
  ["React", "How would you fetch data in a React component?", "fetch-data", "Explain data fetching patterns.", "**Direct answer :** For simple client-side fetching, use `fetch` or another HTTP client and model loading, success, and error states. If the request is tied to an Effect, clean up or ignore stale results when the component's relevant inputs change, and use `AbortController` when cancellation is appropriate. For server-state concerns such as caching, deduplication, retries, and refetching, a dedicated data-fetching library or framework feature can reduce custom code.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** For simple client-side fetching, use `fetch` or another HTTP client and model loading, success, and error states. If the request is tied to an Effect, clean up or ignore stale results when the component's relevant inputs change, and use `AbortController` when cancellation is appropriate. For server-state concerns such as caching, deduplication, retries, and refetching, a dedicated data-fetching library or framework feature can reduce custom code. In production, also plan for loading and failure states, cancellation or stale responses, validation, and the security boundary between browser and server.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you optimize a React app that re‑renders frequently?", "optimize-frequent-renders", "Explain optimization techniques.", "**Direct answer :** Start by profiling to determine why renders are frequent and whether they are actually expensive. Then localize state, avoid unnecessary Effects, keep props stable where it matters, split components, and use `memo`, `useMemo`, or `useCallback` only where they address a measured problem. `useTransition` can help prioritize non-urgent UI work, but it is not a substitute for fixing excessive computation or a poor state architecture.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** Start by profiling to determine why renders are frequent and whether they are actually expensive. Then localize state, avoid unnecessary Effects, keep props stable where it matters, split components, and use `memo`, `useMemo`, or `useCallback` only where they address a measured problem. `useTransition` can help prioritize non-urgent UI work, but it is not a substitute for fixing excessive computation or a poor state architecture.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How would you implement a search filter with debouncing?", "debouncing", "Explain debouncing in React.", "**Direct answer :** Debouncing delays an operation until input has remained stable for a specified period. In React, a common implementation uses `useEffect` with `setTimeout` and cleanup to cancel the previous timer whenever the input changes. For API search, debounce the request trigger and also handle stale or out-of-order responses so an older request cannot overwrite newer results.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** Debouncing delays an operation until input has remained stable for a specified period. In React, a common implementation uses `useEffect` with `setTimeout` and cleanup to cancel the previous timer whenever the input changes. For API search, debounce the request trigger and also handle stale or out-of-order responses so an older request cannot overwrite newer results.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking how to prevent an older API response from overwriting newer search results."],
  ["React", "How do you handle forms with validation in React?", "form-validation", "Explain form validation.", "**Direct answer :** Form validation can combine controlled or uncontrolled fields with synchronous and asynchronous validation rules. Validate at appropriate interaction points such as submit or blur, provide accessible error messages, and keep server-side validation authoritative because client validation can be bypassed. For complex forms, libraries such as React Hook Form can reduce boilerplate, but the validation model should remain understandable.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** Form validation can combine controlled or uncontrolled fields with synchronous and asynchronous validation rules. Validate at appropriate interaction points such as submit or blur, provide accessible error messages, and keep server-side validation authoritative because client validation can be bypassed. For complex forms, libraries such as React Hook Form can reduce boilerplate, but the validation model should remain understandable. In production, also plan for loading and failure states, cancellation or stale responses, validation, and the security boundary between browser and server.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How would you implement a modal in React?", "modal-implementation", "Explain modal creation.", "**Direct answer :** A robust modal manages open state, renders an overlay in an appropriate DOM location, traps focus while open, supports Escape where appropriate, restores focus when closed, and exposes correct dialog semantics such as an accessible name. A portal can help avoid clipping and stacking-context problems. Outside-click behavior should be implemented carefully so it does not interfere with clicks inside the dialog.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** A robust modal manages open state, renders an overlay in an appropriate DOM location, traps focus while open, supports Escape where appropriate, restores focus when closed, and exposes correct dialog semantics such as an accessible name. A portal can help avoid clipping and stacking-context problems. Outside-click behavior should be implemented carefully so it does not interfere with clicks inside the dialog.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking about focus trapping, Escape handling, and restoring focus."],
  ["React", "How do you handle large lists in React?", "large-lists", "Explain virtualization.", "**Direct answer :** For large lists, first reduce unnecessary rendering and work per row. Virtualization renders only the visible portion of a list, which can dramatically reduce DOM nodes when thousands of items are present. Choose a virtualization library or framework appropriate to the row size and interaction model, and make sure keyboard navigation and accessibility remain correct.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** For large lists, first reduce unnecessary rendering and work per row. Virtualization renders only the visible portion of a list, which can dramatically reduce DOM nodes when thousands of items are present. Choose a virtualization library or framework appropriate to the row size and interaction model, and make sure keyboard navigation and accessibility remain correct. The final implementation should be measured against real user experience, device constraints, accessibility, and failure cases rather than optimized only for the happy path.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking when virtualization is useful and what accessibility issues it introduces."],
  ["React", "How would you implement infinite scrolling?", "infinite-scroll", "Explain infinite scroll.", "**Direct answer :** Infinite scrolling can use an `IntersectionObserver` watching a sentinel near the end of the list. When it becomes visible, fetch the next page, guard against duplicate requests, append results, and stop when the server reports no more data. Preserve stable keys and consider accessibility, loading feedback, and a fallback pagination mechanism for users who cannot or do not want to use continuous scrolling.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** Infinite scrolling can use an `IntersectionObserver` watching a sentinel near the end of the list. When it becomes visible, fetch the next page, guard against duplicate requests, append results, and stop when the server reports no more data. Preserve stable keys and consider accessibility, loading feedback, and a fallback pagination mechanism for users who cannot or do not want to use continuous scrolling. The final implementation should be measured against real user experience, device constraints, accessibility, and failure cases rather than optimized only for the happy path.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you manage global state with context and reducers?", "global-state-context-reducer", "Explain the pattern.", "**Direct answer :** A Context plus `useReducer` pattern can provide shared state and explicit actions without adding a separate state library. The provider owns the reducer and exposes state plus dispatch through context; descendants consume only what they need. For performance and maintainability, consider separating state and dispatch contexts or splitting providers so unrelated updates do not force a large subtree to re-render.\n\n**Why this is asked :** Interviewers ask this to see whether you can design predictable state flow, choose an appropriate state boundary, and explain the trade-offs of the approach in a real application.\n\n**Breaking it down :** A Context plus `useReducer` pattern can provide shared state and explicit actions without adding a separate state library. The provider owns the reducer and exposes state plus dispatch through context; descendants consume only what they need. For performance and maintainability, consider separating state and dispatch contexts or splitting providers so unrelated updates do not force a large subtree to re-render.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you implement a dark mode toggle in React?", "dark-mode", "Explain dark mode implementation.", "**Direct answer :** A dark-mode implementation can keep the theme choice in state, persist an explicit user preference, and apply a class or data attribute used by CSS variables. Respect the user's `prefers-color-scheme` setting when there is no explicit preference, and avoid a flash of the wrong theme during initial load by applying the initial theme as early as practical. Context is often sufficient for distributing the theme value.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** A dark-mode implementation can keep the theme choice in state, persist an explicit user preference, and apply a class or data attribute used by CSS variables. Respect the user's `prefers-color-scheme` setting when there is no explicit preference, and avoid a flash of the wrong theme during initial load by applying the initial theme as early as practical. Context is often sufficient for distributing the theme value.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  // Fixed the file uploads entry with proper escaping
  ["React", "How do you handle file uploads in React?", "file-uploads", "Explain file upload.", "**Direct answer :** Use a file input to obtain a `File` object, validate size and type on both client and server, and send the file with `FormData` for a multipart upload or use a direct-to-storage upload flow for larger files. The server must treat the file as untrusted input. For large uploads, resumable or pre-signed uploads can reduce load on the application server.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** Use a file input to obtain a `File` object, validate size and type on both client and server, and send the file with `FormData` for a multipart upload or use a direct-to-storage upload flow for larger files. The server must treat the file as untrusted input. For large uploads, resumable or pre-signed uploads can reduce load on the application server. In production, also plan for loading and failure states, cancellation or stale responses, validation, and the security boundary between browser and server.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How would you implement a drag‑and‑drop feature in React?", "drag-and-drop", "Explain drag‑and‑drop.", "**Direct answer :** Native HTML5 drag-and-drop can handle desktop pointer-style dragging, while a library such as `@dnd-kit` can provide richer sensors and interaction patterns. The implementation should track the dragged item's identity, calculate the intended drop position, update state immutably, and provide keyboard-accessible alternatives. Do not rely solely on color or hover state to communicate the drop target.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** Native HTML5 drag-and-drop can handle desktop pointer-style dragging, while a library such as `@dnd-kit` can provide richer sensors and interaction patterns. The implementation should track the dragged item's identity, calculate the intended drop position, update state immutably, and provide keyboard-accessible alternatives. Do not rely solely on color or hover state to communicate the drop target.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you handle session persistence in React?", "session-persistence", "Explain session management.", "**Direct answer :** Persisting a session should be designed around the authentication mechanism rather than simply saving a token in browser storage. A server session represented by an HttpOnly cookie is a common web approach. On startup, the React app can request the current session and model loading, authenticated, and unauthenticated states. Avoid putting sensitive session credentials in `localStorage` unless the security trade-offs are explicitly understood.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** Persisting a session should be designed around the authentication mechanism rather than simply saving a token in browser storage. A server session represented by an HttpOnly cookie is a common web approach. On startup, the React app can request the current session and model loading, authenticated, and unauthenticated states. Avoid putting sensitive session credentials in `localStorage` unless the security trade-offs are explicitly understood. In production, also plan for loading and failure states, cancellation or stale responses, validation, and the security boundary between browser and server.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you implement a multi‑language app in React?", "i18n", "Explain internationalization.", "**Direct answer :** Internationalization separates translatable content from UI code and accounts for locale-specific formatting of dates, numbers, currencies, pluralization, and text direction. A library such as `react-i18next` can manage translations and locale changes. Translation keys should be stable and contextual, and user-visible strings should not be assembled with brittle string concatenation when the grammar differs across languages.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** Internationalization separates translatable content from UI code and accounts for locale-specific formatting of dates, numbers, currencies, pluralization, and text direction. A library such as `react-i18next` can manage translations and locale changes. Translation keys should be stable and contextual, and user-visible strings should not be assembled with brittle string concatenation when the grammar differs across languages.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you handle loading states in React?", "loading-states", "Explain loading state management.", "**Direct answer :** Model loading explicitly, especially when requests can overlap. For a single request, an `isLoading` state may be enough; for concurrent work, use request-specific state or a data-fetching library rather than a global counter that can become inconsistent. Good loading UI preserves layout where possible, communicates progress for long operations, and distinguishes initial loading from background refetching.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** Model loading explicitly, especially when requests can overlap. For a single request, an `isLoading` state may be enough; for concurrent work, use request-specific state or a data-fetching library rather than a global counter that can become inconsistent. Good loading UI preserves layout where possible, communicates progress for long operations, and distinguishes initial loading from background refetching.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you implement a responsive design in React?", "responsive-design", "Explain responsive design.", "**Direct answer :** Responsive React applications normally rely on CSS for layout using media queries, Grid, Flexbox, container queries, and responsive sizing. JavaScript media-query hooks are useful only when the actual rendered behavior must differ, not merely to choose CSS styles. Prefer CSS for presentation so the layout responds naturally without duplicating responsive rules in component logic.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** Responsive React applications normally rely on CSS for layout using media queries, Grid, Flexbox, container queries, and responsive sizing. JavaScript media-query hooks are useful only when the actual rendered behavior must differ, not merely to choose CSS styles. Prefer CSS for presentation so the layout responds naturally without duplicating responsive rules in component logic. The final implementation should be measured against real user experience, device constraints, accessibility, and failure cases rather than optimized only for the happy path.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you use React with a backend API?", "react-backend-api", "Explain API integration.", "**Direct answer :** A React frontend typically communicates with a backend through HTTP APIs, GraphQL, or another protocol. Keep API access in a well-defined service/data layer, model loading and error states, and handle authentication according to the server's security model. A data-fetching library can provide caching, deduplication, retries, and invalidation. Never treat frontend authorization checks as a substitute for backend authorization.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** A React frontend typically communicates with a backend through HTTP APIs, GraphQL, or another protocol. Keep API access in a well-defined service/data layer, model loading and error states, and handle authentication according to the server's security model. A data-fetching library can provide caching, deduplication, retries, and invalidation. Never treat frontend authorization checks as a substitute for backend authorization. In production, also plan for loading and failure states, cancellation or stale responses, validation, and the security boundary between browser and server.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you implement a breadcrumb navigation in React?", "breadcrumbs", "Explain breadcrumb implementation.", "**Direct answer :** Breadcrumbs represent the user's position in a site hierarchy. A robust implementation derives labels and hierarchy from route metadata or a route configuration rather than blindly displaying URL segments. Each navigable ancestor should be a link, the current page should be marked as the current location, and the markup should use appropriate navigation semantics and accessibility labels.\n\n**Why this is asked :** Interviewers ask this to check that you understand client-side navigation as well as the browser and accessibility behavior behind it, not just the router API.\n\n**Breaking it down :** Breadcrumbs represent the user's position in a site hierarchy. A robust implementation derives labels and hierarchy from route metadata or a route configuration rather than blindly displaying URL segments. Each navigable ancestor should be a link, the current page should be marked as the current location, and the markup should use appropriate navigation semantics and accessibility labels.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you handle permissions and roles in a React app?", "permissions", "Explain role‑based access control.", "**Direct answer :** Role-based or permission-based UI can hide or disable actions the user cannot perform, but the backend must enforce authorization on every protected operation. A frontend can store the current user's permissions in context or another state layer and use a reusable permission check for presentation. For maintainability, model permissions as explicit capabilities rather than scattering role-name comparisons throughout the component tree.\n\n**Why this is asked :** Interviewers ask this to check that you understand client-side navigation as well as the browser and accessibility behavior behind it, not just the router API.\n\n**Breaking it down :** Role-based or permission-based UI can hide or disable actions the user cannot perform, but the backend must enforce authorization on every protected operation. A frontend can store the current user's permissions in context or another state layer and use a reusable permission check for presentation. For maintainability, model permissions as explicit capabilities rather than scattering role-name comparisons throughout the component tree. The key boundary is that browser-side code improves UX but cannot replace server-side security controls or validation.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking where authorization must be enforced and why UI checks are insufficient."],
  ["React", "How do you implement a notification system in React?", "notifications", "Explain toast/notification system.", "**Direct answer :** A notification system can keep a list of notification objects in state and expose operations to add, update, and dismiss them. Each notification should have a stable ID and, when appropriate, an accessible live-region strategy such as `role=\"status\"` or `role=\"alert\"`. Auto-dismiss timers need cleanup, and important errors should not disappear before the user can understand or act on them.\n\n**Why this is asked :** Interviewers ask this to evaluate practical React engineering: how you turn a requirement into maintainable components, handle edge cases, and keep the user experience reliable under real conditions.\n\n**Breaking it down :** A notification system can keep a list of notification objects in state and expose operations to add, update, and dismiss them. Each notification should have a stable ID and, when appropriate, an accessible live-region strategy such as `role=\"status\"` or `role=\"alert\"`. Auto-dismiss timers need cleanup, and important errors should not disappear before the user can understand or act on them.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
  ["React", "How do you optimize images in a React app?", "image-optimization", "Explain image optimization.", "**Direct answer :** Image optimization includes choosing appropriate dimensions and formats, compressing assets, serving responsive variants, and lazy-loading images that are below the fold. Use meaningful `alt` text for informative images and empty alt text for purely decorative ones. Frameworks such as Next.js can automate parts of this process, but the underlying goals remain reducing bytes, avoiding layout shifts, and serving the right image for the device.\n\n**Why this is asked :** Interviewers ask this to assess whether you understand React's rendering and performance model and can choose an optimization based on an actual bottleneck rather than applying memoization blindly.\n\n**Breaking it down :** Image optimization includes choosing appropriate dimensions and formats, compressing assets, serving responsive variants, and lazy-loading images that are below the fold. Use meaningful `alt` text for informative images and empty alt text for purely decorative ones. Frameworks such as Next.js can automate parts of this process, but the underlying goals remain reducing bytes, avoiding layout shifts, and serving the right image for the device. The final implementation should be measured against real user experience, device constraints, accessibility, and failure cases rather than optimized only for the happy path.\n\n**How to structure your spoken answer :** Answer in an implementation sequence: first describe the design, then the key React APIs or browser primitives involved, then explain state, cleanup, error handling, and accessibility or security where relevant. Finish with the main trade-off and how you would test or measure the result.\n\n**Follow-up angle to anticipate :** Expect a follow-up asking for a concrete example, an edge case, or the trade-off between this approach and a simpler alternative."],
] as const;

const prisma = new PrismaClient();

const buildWhyInterviewersAsk = (question: string, shortDescription: string) =>
  `Interviewers ask this to check whether you can explain React concepts clearly and connect them to practical engineering decisions. ${shortDescription} A strong response should address the purpose, the relevant trade‑offs, and how you would verify the result rather than reciting a command or definition.`;

const buildCommonMistakes = (question: string) => [
  `Giving a memorized definition without explaining how it applies to: ${question}`,
  "Listing React commands without explaining the safety, performance, or operational trade‑off.",
  "Ignoring security boundaries, persistence, failure handling, or how the solution would be tested.",
];

async function main() {
  const category = await prisma.category.upsert({
    where: { slug: "react" },
    update: { name: "React", group: "Technology", description: "React interview questions." },
    create: { name: "React", slug: "react", group: "Technology", description: "React interview questions." },
  });
  const subcategory = await prisma.subcategory.upsert({
    where: { categoryId_slug: { categoryId: category.id, slug: "react" } },
    update: {},
    create: { name: "React", slug: "react", categoryId: category.id },
  });

  for (let topicIndex = 0; topicIndex < topics.length; topicIndex += 1) {
    const [, question, slug, shortDescription, sampleAnswer] = topics[topicIndex];
    const commonMistakes = buildCommonMistakes(question);
    const followUpQuestions = [
      topics[(topicIndex + 1) % topics.length][1],
      topics[(topicIndex + 2) % topics.length][1],
      topics[(topicIndex + 3) % topics.length][1],
    ];
    await prisma.interviewQuestion.upsert({
      where: { slug },
      update: {
        question,
        categoryId: category.id,
        subcategoryId: subcategory.id,
        experienceLevel: ExperienceLevel.MID_LEVEL,
        difficulty: Difficulty.MEDIUM,
        interviewType: InterviewType.TECHNICAL,
        shortDescription,
        explanation: buildWhyInterviewersAsk(question, shortDescription),
        sampleAnswer,
        detailedAnswer: sampleAnswer,
        keyPoints: [],
        commonMistakes,
        followUpQuestions,
        tags: ["React"],
        isPublished: true,
      },
      create: {
        question,
        slug,
        categoryId: category.id,
        subcategoryId: subcategory.id,
        experienceLevel: ExperienceLevel.MID_LEVEL,
        difficulty: Difficulty.MEDIUM,
        interviewType: InterviewType.TECHNICAL,
        shortDescription,
        explanation: buildWhyInterviewersAsk(question, shortDescription),
        sampleAnswer,
        detailedAnswer: sampleAnswer,
        keyPoints: [],
        commonMistakes,
        followUpQuestions,
        tags: ["React"],
        isPublished: true,
      },
    });
  }

  console.log(`Imported ${topics.length} React questions.`);
}

main()
  .catch((error) => {
    console.error(error);
    process.exitCode = 1;
  })
  .finally(() => prisma.$disconnect());

export const slugify = (value: string) => value.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
