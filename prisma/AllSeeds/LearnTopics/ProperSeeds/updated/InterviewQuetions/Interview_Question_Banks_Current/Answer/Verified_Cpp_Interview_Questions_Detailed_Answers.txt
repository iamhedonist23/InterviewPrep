C++ INTERVIEW QUESTIONS — DETAILED ANSWERS WITH EXAMPLES

SOURCE-BASED STANDARD This file expands the supplied C++ interview bank.
The source explicitly separates [REPORTED] questions from [COMMON]
recurring interview questions and states that the wording is lightly
normalized rather than presented as verbatim quotations.
fileciteturn9file0L8-L16

IMPORTANT [REPORTED] means the supplied source says a published
interview experience explicitly reported that question/topic. [COMMON]
means the supplied source treats it as a recurring C++ interview
pattern, not company-specific evidence. The expanded answers below are
written for interview preparation; they do not convert a COMMON question
into a reported company question.

FORMAT Question: Answer: Example: Interview Tip: What Interviewer Is
Testing: Key Points to Remember: Common Follow-up Questions: Common
Mistakes:

QUALITY STANDARD Each answer aims for: precise definition → C++ behavior
→ practical relevance → example → edge case.

1.  [REPORTED] Question: What are virtual functions?

Answer: A virtual function enables dynamic dispatch. When a derived
class overrides a virtual function, a call through a base reference or
pointer can select the implementation associated with the object’s
dynamic type. The language defines the dispatch semantics; compilers
commonly implement them with a vtable and a hidden vptr. Virtual
dispatch is the standard C++ mechanism for runtime polymorphism.

Example: struct Base { virtual void show(){ std::cout << “Base”; } };
struct Derived: Base { void show() override { std::cout << “Derived”; }
}; Base& b = d; b.show(); // Derived

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

2.  [REPORTED] Question: What is virtual inheritance?

Answer: This question is part of the supplied C++ interview bank. A
strong interview answer should define the concept precisely, explain how
it works in C++, state when it is useful, mention an important
limitation or edge case, and give a small practical example. The exact
source supports the question/topic; the detailed explanation below is an
interview-preparation expansion rather than a claim that every follow-up
detail was explicitly reported by the cited interview.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

3.  [REPORTED] Question: What is the difference between call by
    reference and call by value?

Answer: C++ supports pass-by-value and reference parameters. A value
parameter receives its own value, while a reference parameter aliases
the caller’s object and can modify it unless the reference is const.
const T& is commonly used to avoid copying while preventing
modification.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

4.  [REPORTED] Question: What is an inline function?

Answer: inline permits a function to be defined in multiple translation
units under the C++ One Definition Rule when the definitions are
equivalent, which is why it is common for functions defined in headers.
It is also historically associated with an optimization hint, but modern
compilers decide independently whether to inline a call. Therefore
inline does not guarantee machine-code inlining.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

5.  [COMMON] Question: What is the difference between C and C++?

Answer: C is primarily procedural and gives direct, low-level control
over memory and data representation. C++ supports procedural programming
as well but adds classes, inheritance, polymorphism, templates,
exceptions, RAII, the standard library, and modern resource-management
facilities. C++ also retains much of C’s low-level capability, which is
why it is widely used when both abstraction and performance are
important.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

6.  [COMMON] Question: What is a pure virtual function?

Answer: A virtual function enables dynamic dispatch. When a derived
class overrides a virtual function, a call through a base reference or
pointer can select the implementation associated with the object’s
dynamic type. The language defines the dispatch semantics; compilers
commonly implement them with a vtable and a hidden vptr. Virtual
dispatch is the standard C++ mechanism for runtime polymorphism.

Example: struct Base { virtual void show(){ std::cout << “Base”; } };
struct Derived: Base { void show() override { std::cout << “Derived”; }
}; Base& b = d; b.show(); // Derived

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

7.  [COMMON] Question: What is a constructor?

Answer: A constructor initializes an object’s state and establishes its
invariants. Constructors can be overloaded and are selected according to
the argument list. Initialization of bases and members happens before
the constructor body, following the class declaration order for members.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

8.  [COMMON] Question: What is a copy assignment operator?

Answer: The copy assignment operator assigns an existing object from
another existing object, commonly T& operator=(const T&). Unlike a copy
constructor, both objects already exist. A resource-owning
implementation must handle self-assignment, release or replace the old
resource safely, and maintain a valid state if an operation fails.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

9.  [COMMON] Question: What is const correctness?

Answer: Const correctness means expressing which objects and operations
are allowed to modify state. A const member function promises not to
modify the object’s ordinary state, and const references/pointers can
provide read-only access. Good const correctness prevents accidental
mutation and lets APIs work with genuinely const objects.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

10. [REPORTED] Question: What is the difference between malloc and
    calloc?

Answer: malloc allocates a requested number of bytes without
initializing the storage. calloc allocates an array-like block and
initializes all allocated bytes to zero. Both return a pointer or NULL
on failure and are released with free. In C++, these functions are
usually avoided for ordinary object ownership in favor of RAII
containers and smart pointers.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

11. [REPORTED] Question: What is the difference between new and malloc?

Answer: new is a C++ language operation that allocates storage and
initializes an object, invoking constructors for class types. delete
destroys the object and releases its storage. malloc is a C library
allocation function that returns raw storage and does not call C++
constructors. Consequently, new must be matched with delete and malloc
with free; mixing these allocation families is undefined behavior.

Example: Widget* a = new Widget; delete a; int* b =
static_cast<int*>(std::malloc(sizeof(int))); std::free(b);

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

12. [REPORTED] Question: What is virtual memory?

Answer: Virtual memory is an operating-system mechanism that gives
processes a virtual address space and maps virtual addresses to physical
memory or other backing storage. It provides isolation and allows the OS
to manage memory beyond a process’s immediately resident physical pages.
Exact implementation is platform-dependent.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

13. [REPORTED] Question: Explain how memory is allocated when code runs.

Answer: This question is part of the supplied C++ interview bank. A
strong interview answer should define the concept precisely, explain how
it works in C++, state when it is useful, mention an important
limitation or edge case, and give a small practical example. The exact
source supports the question/topic; the detailed explanation below is an
interview-preparation expansion rather than a claim that every follow-up
detail was explicitly reported by the cited interview.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

14. [REPORTED] Question: Explain copy constructors and object copying.

Answer: A copy constructor initializes a new object from another object
of the same type, commonly Class(const Class&). The compiler can
generate one automatically, performing memberwise copy. If a class owns
a raw resource, memberwise copying may produce two objects referring to
the same resource, so a resource-owning class must define an appropriate
copy policy or, preferably, use RAII members that follow the Rule of
Zero.

Example: class Buffer { std::string data; public: Buffer(const Buffer&
other) : data(other.data) {} };

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

15. [COMMON] Question: What is the difference between new/delete and
    malloc/free?

Answer: This question is part of the supplied C++ interview bank. A
strong interview answer should define the concept precisely, explain how
it works in C++, state when it is useful, mention an important
limitation or edge case, and give a small practical example. The exact
source supports the question/topic; the detailed explanation below is an
interview-preparation expansion rather than a claim that every follow-up
detail was explicitly reported by the cited interview.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

16. [COMMON] Question: Why must new be paired with delete and malloc
    with free?

Answer: This question is part of the supplied C++ interview bank. A
strong interview answer should define the concept precisely, explain how
it works in C++, state when it is useful, mention an important
limitation or edge case, and give a small practical example. The exact
source supports the question/topic; the detailed explanation below is an
interview-preparation expansion rather than a claim that every follow-up
detail was explicitly reported by the cited interview.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

17. [COMMON] Question: What happens if you use delete on memory
    allocated with malloc?

Answer: These allocation families must not be mixed. Memory returned by
malloc/calloc/realloc is released with free; objects allocated with new
are released with delete, and arrays from new[] with delete[]. Mixing
them is undefined behavior.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

18. [COMMON] Question: What happens if you use free on memory allocated
    with new?

Answer: These allocation families must not be mixed. Memory returned by
malloc/calloc/realloc is released with free; objects allocated with new
are released with delete, and arrays from new[] with delete[]. Mixing
them is undefined behavior.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

19. [COMMON] Question: Why should a polymorphic base class usually have
    a virtual destructor?

Answer: A base-class destructor should generally be virtual when the
class is intended to be used polymorphically and objects may be deleted
through a base pointer. Then deleting a Derived object through Base*
invokes the derived destructor before the base destructor. Without a
virtual base destructor, deleting a derived object through a base
pointer is undefined behavior. A virtual destructor is therefore
primarily an object-lifetime and ownership-safety requirement for
polymorphic bases.

Example: struct Base { virtual ~Base() = default; virtual void run() =
0; }; struct Derived : Base { ~Derived() override = default; void run()
override {} }; Base* p = new Derived; delete p;

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

20. [COMMON] Question: Why is RAII important in modern C++?

Answer: RAII, Resource Acquisition Is Initialization, binds resource
ownership to an object’s lifetime. The constructor acquires or
establishes ownership and the destructor releases it. This makes cleanup
deterministic even when functions return early or exceptions are thrown.
std::vector, std::string, and std::unique_ptr are common examples.

Example: std::unique_ptr<FILE, decltype(&fclose)>
f(std::fopen(“data.txt”,“r”), &fclose); // fclose runs automatically

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

21. [COMMON] Question: How does weak_ptr break a shared_ptr cycle?

Answer: std::weak_ptr is a non-owning observer of an object managed by
std::shared_ptr. It does not increase the reference count and can be
converted temporarily to shared_ptr through lock() if the object is
still alive. It is particularly useful for breaking shared_ptr ownership
cycles.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

22. [COMMON] Question: What is an lvalue?

Answer: An lvalue generally identifies an object with a persistent
location, while an rvalue is an expression representing a value/category
suitable for being moved from in many contexts. C++‘s value categories
are more precise than simply ’has an address’ versus ‘temporary’.
Understanding them is essential for references and move semantics.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

23. [COMMON] Question: Is std::move itself moving an object?

Answer: std::move does not itself move an object. It is essentially a
cast that converts an expression to an xvalue so overload resolution can
select move operations. The object’s move constructor or move assignment
operator performs the actual resource transfer.

Example: std::string a = “large text”; std::string b = std::move(a); //
std::move enables move overload; it does not itself move

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

24. [COMMON] Question: What is the Rule of Five?

Answer: The Rule of Five concerns destructor, copy constructor, copy
assignment, move constructor, and move assignment for resource-managing
classes. The Rule of Zero recommends avoiding manual resource-management
special members by storing resources in RAII types such as std::vector,
std::string, and std::unique_ptr, allowing the compiler-generated
operations to be correct.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

25. [REPORTED] Question: Questions focused mainly on virtual functions
    and virtual destructors.

Answer: A base-class destructor should generally be virtual when the
class is intended to be used polymorphically and objects may be deleted
through a base pointer. Then deleting a Derived object through Base*
invokes the derived destructor before the base destructor. Without a
virtual base destructor, deleting a derived object through a base
pointer is undefined behavior. A virtual destructor is therefore
primarily an object-lifetime and ownership-safety requirement for
polymorphic bases.

Example: struct Base { virtual ~Base() = default; virtual void run() =
0; }; struct Derived : Base { ~Derived() override = default; void run()
override {} }; Base* p = new Derived; delete p;

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

26. [REPORTED] Question: What happens when the virtual keyword is used?

Answer: This question is part of the supplied C++ interview bank. A
strong interview answer should define the concept precisely, explain how
it works in C++, state when it is useful, mention an important
limitation or edge case, and give a small practical example. The exact
source supports the question/topic; the detailed explanation below is an
interview-preparation expansion rather than a claim that every follow-up
detail was explicitly reported by the cited interview.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

27. [REPORTED] Question: Explain polymorphism.

Answer: Polymorphism allows one interface to represent different
implementations. Compile-time polymorphism includes overloading and
templates; runtime polymorphism commonly uses virtual functions and
overriding through a base pointer or reference.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

28. [COMMON] Question: How does runtime polymorphism work?

Answer: Polymorphism allows one interface to represent different
implementations. Compile-time polymorphism includes overloading and
templates; runtime polymorphism commonly uses virtual functions and
overriding through a base pointer or reference.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

29. [COMMON] Question: What is a vtable?

Answer: A vtable/vptr is the common compiler implementation model for
virtual dispatch, not a layout mandated by the C++ standard. A
polymorphic object commonly contains or is associated with a hidden
pointer to a table of virtual-function targets. The exact
representation, number of tables, and object layout are
implementation-dependent.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

30. [COMMON] Question: vector vs list?

Answer: std::vector stores elements contiguously and provides fast
random access, excellent cache locality, and amortized O(1) push_back.
std::list is a linked list with O(1) insertion/erasure at a known
position but poor locality and no random access. In practice vector is
usually the default sequence container unless list-specific semantics
are required.

Example: std::vector v{1,2,3}; // contiguous storage and fast indexing
std::list l{1,2,3}; // linked nodes

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

31. [COMMON] Question: set vs multiset?

Answer: map and set enforce unique keys/elements, while multimap and
multiset permit multiple equivalent keys/elements. All ordered
associative containers provide logarithmic complexity requirements for
key operations.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

32. [COMMON] Question: emplace_back() vs push_back()?

Answer: vector::push_back appends an element. Its complexity is
amortized O(1), although an individual operation can be O(n) when
capacity must grow and existing elements are moved/copied. Reallocation
can also invalidate pointers, references, and iterators to elements.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

33. [COMMON] Question: What is the difference between lower_bound and
    upper_bound?

Answer: lower_bound returns the first position whose value is not less
than the target; upper_bound returns the first position whose value is
greater than the target. Together they are useful for finding insertion
ranges and counting equivalent values in sorted ranges.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

34. [COMMON] Question: How would you find duplicates using STL?

Answer: This question is part of the supplied C++ interview bank. A
strong interview answer should define the concept precisely, explain how
it works in C++, state when it is useful, mention an important
limitation or edge case, and give a small practical example. The exact
source supports the question/topic; the detailed explanation below is an
interview-preparation expansion rather than a claim that every follow-up
detail was explicitly reported by the cited interview.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

35. [COMMON] Question: What is a function template?

Answer: Templates let C++ write type-generic code that is instantiated
for concrete types. Function templates generalize functions; class
templates generalize types. They enable compile-time polymorphism and
can provide strong type safety and performance without virtual dispatch.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

36. [COMMON] Question: Why use templates?

Answer: Templates let C++ write type-generic code that is instantiated
for concrete types. Function templates generalize functions; class
templates generalize types. They enable compile-time polymorphism and
can provide strong type safety and performance without virtual dispatch.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

37. [COMMON] Question: Full specialization vs partial specialization?

Answer: Template specialization provides an alternate implementation for
particular template arguments. Full specialization defines behavior for
a complete set of template arguments; partial specialization is
available for class templates and matches a family of types.
Specialization is useful when a generic implementation needs
type-specific behavior.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

38. [COMMON] Question: What is type deduction?

Answer: auto deduces a type from an initializer using rules related to
template argument deduction, with important differences such as
top-level cv/reference handling. decltype instead determines a type
based on the exact expression form and preserves reference information
in cases where auto would not.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

39. [COMMON] Question: When would you prefer templates over runtime
    polymorphism?

Answer: Templates let C++ write type-generic code that is instantiated
for concrete types. Function templates generalize functions; class
templates generalize types. They enable compile-time polymorphism and
can provide strong type safety and performance without virtual dispatch.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

40. [REPORTED] Question: What is a constant reference?

Answer: A const reference, such as const std::string&, provides
read-only access to an existing object without making a copy. It can
also bind to temporary values. It is a common C++ parameter type when a
function needs efficient read-only access.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

41. [COMMON] Question: What is a pointer?

Answer: This question is part of the supplied C++ interview bank. A
strong interview answer should define the concept precisely, explain how
it works in C++, state when it is useful, mention an important
limitation or edge case, and give a small practical example. The exact
source supports the question/topic; the detailed explanation below is an
interview-preparation expansion rather than a claim that every follow-up
detail was explicitly reported by the cited interview.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

42. [REPORTED] Question: Design an Array class using C++ features.

Answer: A custom array class should encapsulate its buffer, size, and
capacity and implement correct construction, destruction, copying,
moving, indexing, and resizing. If it owns dynamic memory, the class
must maintain the appropriate Rule of Five or, preferably, delegate
ownership to a standard RAII container. In production, std::vector is
normally preferred.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

43. [REPORTED] Question: Reverse a linked list.

Answer: Reverse the links iteratively using previous, current, and next
pointers. Save current->next, point current->next to previous, then
advance. The new head is previous. Complexity is O(n) time and O(1)
auxiliary space.

Example: Node* prev=nullptr; Node* cur=head; while(cur){ Node*
next=cur->next; cur->next=prev; prev=cur; cur=next; } head=prev;

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

44. [COMMON] Question: Reverse a singly linked list.

Answer: This question is part of the supplied C++ interview bank. A
strong interview answer should define the concept precisely, explain how
it works in C++, state when it is useful, mention an important
limitation or edge case, and give a small practical example. The exact
source supports the question/topic; the detailed explanation below is an
interview-preparation expansion rather than a claim that every follow-up
detail was explicitly reported by the cited interview.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

45. [COMMON] Question: Predict the output of code involving
    constructors/destructors.

Answer: A constructor initializes an object and establishes its class
invariants; a destructor runs when the object’s lifetime ends and
releases owned resources. Constructors can be overloaded because they
have different parameter lists. Destructors cannot be overloaded because
a type has one destructor signature and destruction is tied to object
lifetime.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

46. [COMMON] Question: Predict the output of code involving virtual
    functions.

Answer: A virtual function enables dynamic dispatch. When a derived
class overrides a virtual function, a call through a base reference or
pointer can select the implementation associated with the object’s
dynamic type. The language defines the dispatch semantics; compilers
commonly implement them with a vtable and a hidden vptr. Virtual
dispatch is the standard C++ mechanism for runtime polymorphism.

Example: struct Base { virtual void show(){ std::cout << “Base”; } };
struct Derived: Base { void show() override { std::cout << “Derived”; }
}; Base& b = d; b.show(); // Derived

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.

====================================================================================================

47. [COMMON] Question: Predict the output of code involving
    pointer/reference and const combinations.

Answer: For output-prediction questions, trace evaluation order, object
lifetime, overload/virtual dispatch, pointer values, and undefined
behavior. The key interview skill is not merely guessing the printed
output but identifying whether the C++ standard actually defines a
single result.

Example: Use a small class/function that demonstrates the concept
directly, then test the important edge case discussed in the interview.

Interview Tip: Start with the direct definition, then explain the
mechanism, why it matters, and one practical example. If the interviewer
asks a comparison, answer both sides and finish with when you would
choose each. For coding/output questions, explain the approach,
complexity, and edge cases before presenting code.

What Interviewer Is Testing: C++ language fundamentals, object lifetime,
memory/resource ownership, OOP reasoning, modern C++ knowledge, STL
understanding, and your ability to reason about code rather than
memorize definitions.

Key Points to Remember: Distinguish language guarantees from common
compiler implementations. Be precise about undefined behavior,
ownership, lifetime, constness, value categories, virtual dispatch, and
STL complexity guarantees.

Common Follow-up Questions: Why does this happen? What happens
internally? What is the complexity? What is the edge case? How does
modern C++ solve it? What happens if the object is destroyed or the
allocation fails?

Common Mistakes: Giving a one-line definition, mixing C and C++
allocation rules, confusing pointer/reference semantics, claiming inline
guarantees machine-code inlining, treating vtable layout as
standard-mandated, or ignoring object lifetime and ownership.
