"use client";
export function DraftActions({ id }: { id: string }) { async function update(action: "approve" | "publish") { const response = await fetch(`/api/admin/questions/drafts/${id}`, { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ action }) }); if (!response.ok) window.alert("Draft could not be updated."); else window.location.reload(); } async function remove() { if (!window.confirm("Delete this draft permanently?")) return; const response = await fetch(`/api/admin/questions/drafts/${id}`, { method: "DELETE" }); if (!response.ok) window.alert("Draft could not be deleted."); else window.location.reload(); } return <div className="flex flex-wrap gap-3 text-xs font-bold"><button type="button" onClick={() => update("approve")} className="text-coral">Approve</button><button type="button" onClick={() => update("publish")} className="text-coral">Publish</button><button type="button" onClick={remove} className="text-error">Delete</button></div>; }
