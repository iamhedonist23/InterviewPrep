import Link from "next/link";
import { QuestionImporter } from "@/components/admin/question-importer";
import { requireAdmin } from "@/lib/admin";
import { Container } from "@/components/ui/container";
export const dynamic = "force-dynamic";
export default async function QuestionImportPage() { await requireAdmin(); return <section className="py-16"><Container><Link href="/admin/questions" className="text-sm font-bold text-ink/55 hover:text-coral">← Questions</Link><h1 className="mt-6 font-display text-4xl font-bold">Bulk question import</h1><p className="mt-3 max-w-2xl text-ink/60">Upload up to 2,000 questions as CSV or XLSX. Files are validated and previewed before anything is written.</p><div className="mt-6 rounded-2xl bg-mint/60 p-5 text-sm leading-7"><p className="font-bold">Required columns</p><p className="mt-1 break-words">question, slug, category, subcategory, experienceLevel, difficulty, interviewType, shortDescription, explanation, sampleAnswer, keyPoints, commonMistakes, followUpQuestions, tags, seoTitle, seoDescription, isPublished</p><div className="mt-4 flex flex-wrap gap-4"><Link href="/api/admin/questions/template?format=csv" className="font-bold text-coral">Download CSV template</Link><Link href="/api/admin/questions/template?format=xlsx" className="font-bold text-coral">Download Excel template</Link></div></div><p className="mt-6 text-sm font-semibold text-warning">Review every imported row before publishing. The default duplicate policy is skip.</p><QuestionImporter /></Container></section>; }