import { getServerSession } from "next-auth";
import { redirect } from "next/navigation";
import Link from "next/link";
import { authOptions } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { Container } from "@/components/ui/container";
import { LogoutButton } from "@/components/auth/logout-button";

export const dynamic = "force-dynamic";
export default async function DashboardPage() { const session = await getServerSession(authOptions); if (!session?.user?.id) redirect("/login"); const userId = session.user.id; const [completed, sessions, saved, recent] = await Promise.all([prisma.practiceSession.count({ where: { userId, status: "COMPLETED" } }), prisma.practiceSession.findMany({ where: { userId }, orderBy: { startedAt: "desc" }, take: 5, select: { id: true, startedAt: true, status: true, score: true } }), prisma.savedQuestion.count({ where: { userId } }), prisma.interviewAnswer.findMany({ where: { userId }, orderBy: { updatedAt: "desc" }, take: 5, include: { question: { select: { question: true, slug: true } } } })]); const scores = sessions.flatMap(item => item.score === null ? [] : [item.score]); const average = scores.length ? Math.round(scores.reduce((total, score) => total + score, 0) / scores.length) : null; return <section className="py-16"><Container><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-coral">Your dashboard</p><h1 className="mt-3 font-display text-4xl font-bold">Welcome, {session.user.name?.split(" ")[0] ?? "there"}</h1><p className="mt-3 text-ink/60">A private space for your interview preparation.</p></div><LogoutButton /></div><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Questions practiced", completed], ["Practice sessions", sessions.length], ["Average score", average === null ? "Not scored" : `${average}%`], ["Saved questions", saved]].map(([label, value]) => <div className="rounded-2xl border border-ink/10 bg-white/70 p-5" key={label as string}><p className="text-sm text-ink/55">{label as string}</p><p className="mt-3 font-display text-3xl font-bold">{value}</p></div>)}</div><div className="mt-10 grid gap-8 lg:grid-cols-2"><div><div className="flex items-center justify-between"><h2 className="font-display text-2xl font-bold">Practice sessions</h2><Link href="/practice" className="text-sm font-bold text-coral">Start practice</Link></div><div className="mt-4 rounded-2xl border border-ink/10 bg-white/70 p-5">{sessions.length ? sessions.map(item => <div className="flex justify-between border-b border-ink/10 py-3 text-sm last:border-0" key={item.id}><span>{item.startedAt.toLocaleDateString()}</span><span className="font-bold">{item.score === null ? item.status.toLowerCase().replace("_", " ") : `${item.score}%`}</span></div>) : <p className="py-5 text-sm text-ink/55">Your completed practice sessions will appear here.</p>}</div></div><div><div className="flex items-center justify-between"><h2 className="font-display text-2xl font-bold">Recent activity</h2><Link href="/dashboard/saved" className="text-sm font-bold text-coral">Saved questions</Link></div><div className="mt-4 rounded-2xl border border-ink/10 bg-white/70 p-5">{recent.length ? recent.map(item => <Link href={`/questions/${item.question.slug}`} className="block border-b border-ink/10 py-3 text-sm last:border-0 hover:text-coral" key={item.question.slug}>{item.question.question}</Link>) : <p className="py-5 text-sm text-ink/55">Answer a question to see your recent activity.</p>}</div></div></div><div className="mt-10 rounded-2xl bg-mint/60 p-7"><h2 className="font-display text-2xl font-bold">Recommended practice</h2><p className="mt-2 text-ink/60">Keep building momentum with a focused session from the public question library.</p><Link href="/interview-questions" className="mt-5 inline-block rounded-full bg-ink px-5 py-3 text-sm font-bold text-paper">Explore questions</Link></div></Container></section>; }
