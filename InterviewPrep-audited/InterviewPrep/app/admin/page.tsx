import Link from "next/link";
import { requireAdmin } from "@/lib/admin";
import { prisma } from "@/lib/prisma";
import { Container } from "@/components/ui/container";
export const dynamic = "force-dynamic";
export default async function AdminPage() { await requireAdmin(); const [questions, categories, articles, faqs, users, contacts] = await Promise.all([prisma.interviewQuestion.count(), prisma.category.count(), prisma.article.count(), prisma.fAQ.count(), prisma.user.count(), prisma.contactMessage.count()]); const cards = [["Questions", questions, "/admin/questions"], ["Categories", categories, "/admin/categories"], ["Articles", articles, "/admin/articles"], ["FAQs", faqs, "/admin/faqs"], ["Users", users, "/admin/users"], ["Contact messages", contacts, "/admin/contacts"]]; return <section className="py-16"><Container><p className="text-xs font-bold uppercase tracking-[.18em] text-coral">Restricted workspace</p><h1 className="mt-3 font-display text-4xl font-bold">Admin dashboard</h1><p className="mt-3 text-ink/60">Manage the content and accounts behind InterviewPrep.</p><div className="mt-6 flex flex-wrap gap-3"><Link href="/admin/questions/import" className="rounded-full bg-coral px-5 py-3 text-sm font-bold text-white">Upload CSV / Excel</Link><Link href="/admin/questions/generate" className="rounded-full border border-ink/15 px-5 py-3 text-sm font-bold">Generate AI drafts</Link></div><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{cards.map(([label, count, href]) => <Link href={href as string} className="rounded-2xl border border-ink/10 bg-white/70 p-6 hover:border-coral" key={label as string}><p className="text-sm text-ink/55">{label as string}</p><p className="mt-3 font-display text-4xl font-bold">{count}</p><p className="mt-5 text-sm font-bold text-coral">Manage {label as string} →</p></Link>)}</div></Container></section>; }
