import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { QuestionCard } from "@/components/questions/question-card";
import { Button } from "@/components/ui/button";
import { Container } from "@/components/ui/container";
import { prisma } from "@/lib/prisma";

export const dynamic = "force-dynamic";
type Props = { params: Promise<{ category: string }> };
export async function generateMetadata({ params }: Props): Promise<Metadata> { const { category } = await params; const item = await prisma.category.findUnique({ where: { slug: category } }); return item ? { title: `${item.name} interview questions`, description: item.description ?? `Practice ${item.name} interview questions with useful answer guidance.`, alternates: { canonical: `/interview-questions/${item.slug}` } } : {}; }
export default async function CategoryPage({ params }: Props) { const { category } = await params; const item = await prisma.category.findUnique({ where: { slug: category }, include: { subcategories: true, questions: { where: { isPublished: true }, include: { category: true, subcategory: true }, take: 12, orderBy: { createdAt: "desc" } } } }); if (!item) notFound(); const [relatedCategories, relatedArticles] = await Promise.all([prisma.category.findMany({ where: { group: item.group, id: { not: item.id } }, orderBy: { name: "asc" }, take: 5 }), prisma.article.findMany({ where: { isPublished: true, categoryId: item.id }, orderBy: { publishedAt: "desc" }, take: 3 })]); const baseUrl = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"; const breadcrumb = { "@context": "https://schema.org", "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: "Home", item: baseUrl }, { "@type": "ListItem", position: 2, name: "Interview questions", item: `${baseUrl}/interview-questions` }, { "@type": "ListItem", position: 3, name: item.name, item: `${baseUrl}/interview-questions/${item.slug}` }] }; return <section className="py-16 sm:py-20"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(breadcrumb) }} /><Container><nav aria-label="Breadcrumb" className="text-sm text-ink/55"><Link href="/">Home</Link><span className="px-2">/</span><Link href="/interview-questions">Interview questions</Link><span className="px-2">/</span><span className="font-semibold text-ink">{item.name}</span></nav><p className="mt-12 text-xs font-bold uppercase tracking-[.18em] text-coral">Category</p><h1 className="mt-3 font-display text-4xl font-bold sm:text-5xl">{item.name} interview questions</h1><p className="mt-4 max-w-2xl text-lg leading-8 text-ink/60">{item.description}</p><Button href={`/practice?category=${item.slug}`} className="mt-7">Practice this category</Button>{item.subcategories.length > 0 && <div className="mt-14"><h2 className="font-display text-2xl font-bold">Explore topics</h2><div className="mt-5 flex flex-wrap gap-3">{item.subcategories.map(subcategory => <span key={subcategory.id} className="rounded-full border border-ink/15 px-4 py-2 text-sm font-semibold">{subcategory.name}</span>)}</div></div>}<h2 className="mt-14 font-display text-2xl font-bold">Popular questions</h2><div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{item.questions.map(question => <QuestionCard key={question.id} question={question} />)}</div>{relatedCategories.length > 0 && <div className="mt-16"><h2 className="font-display text-2xl font-bold">Related categories</h2><div className="mt-5 flex flex-wrap gap-3">{relatedCategories.map(related => <Link href={`/interview-questions/${related.slug}`} className="rounded-full border border-ink/15 px-4 py-2 text-sm font-bold hover:border-coral hover:text-coral" key={related.id}>{related.name}</Link>)}</div></div>}{relatedArticles.length > 0 && <div className="mt-16"><h2 className="font-display text-2xl font-bold">Related resources</h2><div className="mt-5 grid gap-4 md:grid-cols-3">{relatedArticles.map(article => <Link href={`/blog/${article.slug}`} className="rounded-2xl border border-ink/10 bg-white/70 p-5 hover:border-coral" key={article.id}><h3 className="font-display text-lg font-bold">{article.title}</h3><p className="mt-2 text-sm leading-6 text-ink/60">{article.excerpt}</p></Link>)}</div></div>}</Container></section>; }
