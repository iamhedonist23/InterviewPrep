"use client";
import { useState } from "react";
import { signIn } from "next-auth/react";
import { Button } from "@/components/ui/button";
export function LoginForm() { const [error, setError] = useState(""); const [busy, setBusy] = useState(false); async function submit(formData: FormData) { setBusy(true); setError(""); const result = await signIn("credentials", { email: formData.get("email"), password: formData.get("password"), redirect: false, callbackUrl: "/dashboard" }); if (result?.error) { setError("Email or password is incorrect."); setBusy(false); } else window.location.href = "/dashboard"; } return <form action={submit} className="grid gap-5"><label className="text-sm font-bold">Email<input required name="email" type="email" autoComplete="email" className="mt-2 h-12 w-full rounded-xl border border-ink/15 bg-paper px-4 font-normal" /></label><label className="text-sm font-bold">Password<input required name="password" type="password" autoComplete="current-password" className="mt-2 h-12 w-full rounded-xl border border-ink/15 bg-paper px-4 font-normal" /></label>{error && <p role="alert" className="text-sm font-semibold text-error">{error}</p>}<Button type="submit">{busy ? "Signing in..." : "Log in"}</Button><Button href="/api/auth/signin" variant="outline">Continue with Google</Button></form>; }
